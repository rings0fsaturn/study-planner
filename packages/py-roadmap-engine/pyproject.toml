[project]
name = "py-roadmap-engine"
version = "0.1.0"
description = "Pillar A constraint-based schedule generator"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/py_roadmap_engine"]

[tool.pytest.ini_options]
testpaths = ["tests"]
