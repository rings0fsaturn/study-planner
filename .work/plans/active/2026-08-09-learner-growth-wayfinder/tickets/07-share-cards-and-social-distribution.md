## Question

What does the shared artifact look like on social platforms? Decide:

- Public page structure (server-rendered for crawlers, Marginalia-branded).
- Open Graph + Twitter/X card metadata: title, description, image, URL.
- LinkedIn post/share behavior and prefilled text — without platform posting APIs.
- Web Share API support (mobile) and copy-link fallback.
- Which evidence appears in the card preview vs only on the page.
- How the share flow starts from an achievement (button placement, confirmation step with privacy choices).

## Context

Requirement: sharing should work on LinkedIn and "favourite social platforms". Recommended scope: a public URL + rich cards + share buttons, never platform APIs (kept out of scope on the map). Depends on the shareable-achievement model and the privacy contract.

---
Part of the Learner Growth map #__MAP__ · type: grilling (decision)

Blocked by: __BLOCKED_BY__
