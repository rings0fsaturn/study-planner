# Scratchpad – issue-42-coding-assessment-sandbox-grading

_state.md: active/issue-42-coding-assessment-sandbox-grading/state.md · Reset: 2026-09-16 (P2 session distilled)_

## Now / Next

- Doing: (no live session) P2 done and committed
- Next: P3 — generation coding arm (`coding_schema`/`build_coding_messages`/`validate_coding`, worker dispatch + `_coding_answer_block`, generation-time sandbox self-check, ingestion `has_code`/`code_languages` scan)
- Blocked: none
- Session-start decisions (read before P3): sandbox = Piston (D-02 amended); root compose `profiles: ["sandbox"]`, demand-start; P1 submit stub lifted in P2; output_prediction = numeric `{value}`/`acceptedValue` via `grade_objective` (grader label `objective`); server runs visible tests too, score hidden-only; `CodingAnswer.config.stdin` is client-advisory only

## Session log

- (reset — P2 session log distilled into state.md "Done so far" and "Pitfalls & rules")
