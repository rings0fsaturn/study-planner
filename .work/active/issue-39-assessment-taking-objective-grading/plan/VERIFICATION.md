# VERIFICATION — issue-39-assessment-taking-objective-grading

Running verification log for #39 (Assessment Taking and Objective Grading). Canonical ticket: GitHub [#39](https://github.com/rings0fsaturn/study-planner/issues/39); runbook: `plan/PLAN.md`.

## Status

| Item | State |
|---|---|
| Ticket #39 claimed (assignee `rings0fsaturn`) | ✅ done 2026-09-03 |
| Branch `phase2/issue-39` off `origin/project/phase-2` @ `f83b8c1` | ✅ done 2026-09-03 |
| Task folder + STATUS row + state.md bootstrap | ✅ done 2026-09-03 |
| Codebase surface verified → `research/implementation-surface-39.md` | ✅ done 2026-09-03 (first-hand; dispatched inspector died on 524s) |
| plan/PLAN.md (7 decisions, 6 phases, TDD seams) | ✅ done 2026-09-03 |
| Phase 1 — contract amendment (openapi + fixtures + contracts test) | ✅ done 2026-09-03 (15/15) |
| Phase 2 — migration 025 + grading domain + worker arm | ✅ done 2026-09-03 (grader 9/9, worker 7/7) |
| Phase 3 — service API routes | ✅ done 2026-09-03 (routes tests green; domain 48/48 incl. contracts) |
| Phase 4 — client data layer (Dexie v6, clients, offline queue) | ✅ done 2026-09-03 (14/14) |
| Phase 5 — taking UI | ✅ done 2026-09-03 (P5 suites 25/25: AttemptTaker 3/3, attemptFlow 7/7, dataLayer 7/7, AssessmentDetail 8/8) |
| Phase 6 — verification sweep + live pass + AC tick-down | ✅ done 2026-09-09 — static sweep green (as 2026-09-03); **live pass GREEN**: 026 fix → submit 201 → worker grades → browser walk 2/2 (online 8.7 s, offline-drain 12.1 s) + playwright-cli AC4 walk (Score 1.00, history graded/graded/queued, redaction CLEAN, evidence 2 PNGs) |
| AC1 cache envelope+visible-payload only (grep gate) | ✅ P4 `cacheAssessment` verbatim + P6 grep clean (only test/type files assert the words' absence) |
| AC2 per-question event + retry identity semantics | ✅ P4/P5 tests: QuestionAttempted answer-free, retry mints fresh clientAttemptId (attemptFlow 7/7, dataLayer 7/7); live: history append-only (+1 per attempt) |
| AC3 normalized score + perSkill, hidden answers never exposed | ✅ P3 routes never serialize answer; P6 grep clean; AssessmentDetail redaction DOM test green; live DOM check CLEAN |
| AC4 offline/queue/retry/restore/account-switch/event-order tests | ✅ P4 tests (drain/refresh/retry) + **live**: offline submit → `Queued offline` honest copy (row `queued` + reconnect flag) → reconnect retry submits → graded; console shows only the expected ERR_INTERNET_DISCONNECTED pair |
| Wayfinder resolution: comment + close #39 + map #4 line | ☐ pending |

## Log
- **2026-09-09** P6 live pass GREEN after Supabase recovery. DNS resolves locally + Google DoH; remote sat at 024 so `025_question_attempts.sql` was pushed (EXIT=0); table + both RPCs + `assessment_grade` queue confirmed live. Full-app restarted (both 200), worker_main (3 arms) polling with 200s. First browser run failed: submit POST → 500 `provider_unavailable`; direct RPC call exposed the real cause — 025 selects `q.user_id, q.format, q.assessment_id` into `v_question` but reads `v_question.material_id` (42703 `record "v_question" has no field "material_id"`). Route tests mock `submit_attempt`, so the SQL shape was never exercised. Repair migration `026_attempt_submit_material_id_fix.sql` (adds `q.material_id` to the SELECT) pushed EXIT=0; direct submit → 201 `{attemptId, jobId}` → worker graded `correct:true/grader:objective` within seconds. Browser walk 2/2 green (online 8.7 s, offline-drain 12.1 s). A second walk via playwright-cli (`--browser=chromium`; the `chrome` channel needs /opt/google/chrome which needs sudo — unavailable) on assessment `bab2b498` verified AC2/AC3/AC4 live: `Score 1.00 · correct` + per-skill `Exam structure: correct` / `Professional skills: correct`, redaction CLEAN, history `Attempt #1 graded 1.00 / #2 graded 1.00 / #3 queued + reconnect flag`, offline submit shows `Queued offline` honest copy, console shows only the expected ERR_INTERNET_DISCONNECTED pair. Evidence: `plan/evidence/live39-graded-chromium.png`, `live39-offline-reconnect-chromium.png`. Live spec `e2e/assessment-taking-39-live.spec.ts` (2 tests) kept as the durable artifact. Display note (not fixed, display-only): history lists in Dexie primary-key order, not chronological — no data impact. Probe attempt `fffe3090` (curl) left in the table per user deny on DELETE. Open: wayfinder resolution (comment + close #39 + map #4 line).

- **2026-09-03** Planning session. #34 branch merged (PR #59) and #34 closed; `phase2/issue-39` created off the updated parent. Ticket #39 claimed on GitHub (verified). Task opened via work-journal + global wayfinder flow (claim → resolve → record). Codebase inspection: dispatched subagent died after ~49 min on upstream 524s; the load-bearing seams were verified first-hand instead and recorded in `research/implementation-surface-39.md` (contract gap: no assessment-attempt submission route exists — AttemptSubmit is practice-run-only; durable QuestionAttempted payload has no answer field; Dexie at version(5); no attempts table; two-arm worker ready for a grading arm). plan/PLAN.md written: 7 decisions (server-owned attempts table, contract amendment, deterministic grading via third arm, Dexie v6 tables, spine-driven event flow, lean honest UI, restore via existing machinery), 6 phases with per-phase verification. Next: Phase 1 (contract amendment).
- **2026-09-03** Phases 1–4 implemented + verified (TDD, tests-first each seam). P1 contract amendment: openapi assessment-attempt routes/schemas + 4 fixtures + 3 contracts tests (15/15). P2: migration 025 (question_attempts + RLS + assessment_grade queue + submit_assessment_attempt/complete_attempt_grading RPCs), app/grading/grader.py (deterministic, 9/9), app/grading/worker.py (third arm, 7/7), repo methods, worker_main three arms. P3: service routes (submit 201/200-replay, list) + UserScopedClient RPC path + 5 route tests; my-domain suite 48/48 (api+grader+worker+contracts). P4: Dexie v6 (assessmentAttempts by clientAttemptId + assessmentContentCache), event constants + schema-exact payloads, AssessmentClient submit/list + transport DI, attemptFlow.ts coordinator; 14/14. Noted 7 pre-existing failures (calibration ×5 + sidecar probe ×2) are unrelated (verified via stash). Phase 5 started: AttemptTaker wired into AssessmentDetail; UI tests 1/3 (2 timeout — db-connection contention + poll budget; leads in state.md). Session ended mid-P5; next session resumes there.
- **2026-09-03** P5 completed (commit `7656506`). Root cause of the 2 "timeout" UI tests was NOT the recorded leads — AttemptTaker passed `serviceClient` (not `serviceClient.transport`) as the flow transport, so submit crashed (`transport.submitAttempt is not a function`) and the UI sat in `queued-offline` while pollGrade burned its 20×1s budget. Fixed the wiring; AttemptTaker 3/3 in 365 ms. Also fixed latent breakage surfaced by the P6 sweep: `attemptFlow.test.ts` still injected the removed `fetchLike` seam (transport refactor landed in the same WIP commit) → rewritten harness injects a real `AssessmentClient.transport` over the responder double → 7/7 and no more Dexie-handle-leak ConstraintErrors; `AssessmentDetail.test.tsx` redaction test banned `/correct|answer/i` which the wired AttemptTaker legitimately renders → now asserts the true AC1/AC3 boundary (no answer_key vocab in DOM); `attemptFlow.ts` + `attempts.dataLayer.test.ts` gained `as unknown` payload casts (tsc); AttemptTaker dropped a stale eslint-disable for the unloaded react-hooks rule. Sweep: app tsc clean, lint clean, build clean, app vitest 717 passed (2 = known WSL TZ seed tests, pass under `--pool=forks`), service pytest 428 passed (7 = known pre-existing), #39 domain 48/48, redaction grep clean (matches only test/type files asserting absence). AC1–AC3 ticked, AC4 test-matrix leg ticked.
- **2026-09-03** Live pass BLOCKED by external infra: `kabpmbhlvfbrhtbxjaua.supabase.co` returns NXDOMAIN from Google + Cloudflare public DNS (Status 3, only SOA in authority) — the Supabase project host no longer resolves anywhere, consistent with a paused free-tier project. WSL DNS is not the cause (github/google resolve). Browser sign-in fails `net::ERR_NAME_NOT_RESOLVED`; the intelligence log shows only stale Supabase-backed 200s (last real assessment GET `/v1/assessments/bab2b498-…` predates the outage). A grading-only arm runner was written (`tools/grading_only.py`, GradingWorker loop only) but could not reach Supabase for the same reason; removed after the diagnosis. Unblock: restore/pause-lift the Supabase project in the dashboard (owner `iamrohitsaji@gmail.com`), then rerun the live walk (restart `./full-app`, start the grading-only arm or full worker, take the ready assessment online + offline queue drain). No code change is expected.
