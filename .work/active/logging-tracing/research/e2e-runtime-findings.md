# Logging/tracing — e2e + runtime findings (2026-09-15)

Source: Explore subagent sweep of `e2e/`, lifecycle scripts, compose.

- `e2e/playwright.config.ts`: `reporter: 'list'` only (no html/json/junit); `use: { trace: 'on-first-retry' }`, no screenshot/video opts (defaults: screenshot only-on-failure, video off); `retries: CI ? 2 : 0` — trace only materializes on CI retries, and there is no CI. `webServer` top-level (keep it there per rule 10): marketing `:4321`, `pnpm dev:full` `:5173/study/sign-in`. Projects: marketing, app (Desktop Chrome), app-mobile (Pixel 5, roadmap only).
- No custom fixtures / shared log-capture helper. No `fixtures.ts`, no `test.extend`, no `testInfo.attach`. `test-results/` exists but empty + gitignored. Capture is ad-hoc per spec: `material-ingestion-live.spec.ts:51-58` `watchErrors(page)` (pageerror + console error, assert empty — reused pattern, not shared); `material-viewer-live.spec.ts:312-317` (pageerror + pdf.js `Unable to decode image` warning) + `console.log('[viewer] ...')` metric dumps; skip/cleanup noise elsewhere.
- Screenshots: manual `page.screenshot` to scattered paths — `e2e/__screens__/roadmap/`, `.work/plans/active/.../screenshots`, `.work/active/<task>/plan/evidence/` (live-spec convention), one bare cwd-relative png (`assessment-taking-39-live.spec.ts:74-75`).
- Full-app logs: `full-app` → `scripts/full_app.py` (`STATE_DIR`/`LOG_DIR` = `.dev/full-app[/logs]`, `log_path() = LOG_DIR/{name}.log`, `Popen(stdout=log, stderr=STDOUT, start_new_session=True)`). Services: intelligence :8000, app :5173, marketing :4321. Live logs: `.dev/full-app/logs/{app,intelligence,marketing,ingestion-worker}.log`. ingestion-worker NOT managed by full_app.py — appended via `scripts/run-detached-ingestion-worker.sh:6`. `dev-intelligence.mjs` spawns uvicorn with `stdio: inherit` → lands in `intelligence.log`. `.dev/` + `*.log` gitignored.
- Docker compose (98 lines; web, intelligence, ingestion-worker): no `logging:` stanza (default json-file, unbounded), only healthchecks + worker `restart: unless-stopped`.
- CI: none. No `.github/`, no other CI configs. `test:e2e` script exists; no artifact-upload step anywhere. `.playwright-cli/*.yml` are interactive session snapshots, not e2e tracing.
