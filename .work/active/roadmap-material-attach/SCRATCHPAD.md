# Scratchpad – roadmap-material-attach · session 2026-09-12 (b)
_state.md: active/roadmap-material-attach/state.md · Updated: 2026-09-12

## Now / Next
- Doing: P1 finished in the worktree — implementation + unit verification done, live pass pending.
- Next: P1 live click-through on the dev stack (attach a library material from the roadmap "+", confirm it in the directory, the New session picker and session setup); then commit P1 and start P2.
- Blocked: none.

## Session log
- P0 DONE  filed GitHub #63 "Roadmap material attachment (library material to roadmap set)" (labels `wayfinder:phase2` + `ready-for-agent`), claimed, branch `phase2/issue-63-roadmap-material-attach` cut off `2091d8e` in a fresh worktree `/mnt/d/study/git/study-planner-web-issue-63`; ticket number written into the plan header, VERIFICATION.md, state.md and the STATUS row. Untracked `.work/active/roadmap-material-attach/` copied into the worktree; `pnpm install` 733 pkgs.
- P1 FOUND  the app `test` script is already `vitest run`, so `pnpm --filter app test -- --run <path>` (the form the plan and STATUS use) passes a literal `--` and vitest reads the path as a name filter, matching nothing and hanging on a whole-suite scan. Correct form: `pnpm --filter app test <path>`. Plan runbook corrected.
- P1 DONE  `sync/types.ts`: `MaterialAttachedPayload extends MaterialAddedPayload {roadmapCreatedAt}` + `MaterialDetachedPayload`. `progress/mapEvents.ts`: exported `roadmapMaterialPayloads(events, payload, roadmapCreatedAt)` (declared union attached, masked by detach, array order = insertion order) + `materialTitleIndex(events)`; `mapMaterialsForRoadmap` delegates to it; `effectiveEstimatedDuration` now takes the payload. `roadmap/roadmapProgress.ts` and `roadmap/replan/mapToRegenerateRequest.ts` dropped their local joins onto the shared reader (the progress reader also gained the slot fallback it never had).
- P1 DONE  `session/types.ts` `MaterialKind` gained `'file'`; `PreSessionSetup.iconFor/kindLabel` and `MaterialStrip.getIconLabel` got the `'file'` case (PDF / PDF file). The widening stayed inside those three files, so D-07's `file → article` fallback was **not** needed. `materials/types.ts` added `toMaterialKind`.
- P1 DONE  `MaterialPicker`: `excludeIds`, `MaterialPickerSelection` callback, all-excluded empty state ("Every library material is already on this roadmap."), rows render from the filtered list.
- P1 FOUND  the selection-record callback broke **three** call sites the plan had not indexed: `MaterialLibrary.tsx`, `MaterialDetail.tsx:537`, `PracticeThis.tsx:212`. `tsc` found them; all now map back to ids. Plan index updated.
- P1 DONE  `RoadmapCalendar`: `addMaterialOpen` + `handleAttachMaterials` (one `MaterialAttached` per selection, `role: 'foundation'`, `estimatedDuration: Math.max(15, plan?.minutes ?? 60)`, `kind` via `toMaterialKind`), `materialsById` swapped to the global `materialTitleIndex` (bubble labels survive a detach), header count now `materialPayloads.length`, `dir-head` split into a row of `.dir-head-toggle` + `.dir-add` (no nested button), picker mounted. `roadmap.css` moved the header rules onto `.dir-head-toggle` and added `.dir-add`.
- P1 DEVIATION  skipped the extra "Add a material" button in the empty directory body (D-05). The "+" is in the header, which renders in the empty state too, so it is already one click away; a second control is the same action twice. Recorded in the plan notes for the user to veto.
- P1 VERIFIED  `typecheck` clean · `lint` clean · `pnpm --filter app test` **800/802** (the 2 = the documented WSL TZ pair in `dev/seedTestData.test.ts`, 2/2 green under `--pool=forks`) · AC4 grep: the `MaterialAdded` join now exists only in `progress/mapEvents.ts` (`Step4Confirm.tsx:31` is a single-material preview lookup, not the roadmap set).
- P1 TESTS  mapEvents +5 (16) · roadmapProgress +1 (3) · mapToRegenerateRequest +1 (8) · materials/types.test.ts new (1) · MaterialPicker +2 (10) · RoadmapCalendar +3 (12). All written red-first.
- P1 NEXT  live click-through on the dev stack, then commit, then P2.
- P1 COMMIT  `d2f3176 feat(app): #63 P1 - attach a library material to a roadmap` (25 files incl. the .work records).
- P1 LIVE  stopped the main checkout's stack, copied its gitignored `apps/app/.env.local` + `services/intelligence/.env` into the worktree, started `./full-app start full` there. Verified the browser is served the WORKTREE code (`curl localhost:5173/study/src/progress/mapEvents.ts | grep -c roadmapMaterialPayloads` = 3) before believing anything.
- P1 LIVE PASS GREEN at 1280 (temporary spec, deleted after): signed in with the shared dev account, clicked the directory "+", attached `grokking-algorithms-2nd-edition-2nd_compress`, header `1 MATERIALS` -> `2 MATERIALS`, directory row appeared, New session -> Attach listed it, session setup's chooser showed it as `Foundations` (the P1 role default), zero page errors. Transcript: `research/2026-09-12-p1-live-verification.md`.
- P1 FOUND  375 leg: the picker opens and honours `excludeIds`, but by then every library material was already attached, so the run exercised the all-attached empty state instead of a fresh attach. Deliberately not re-run with a throwaway material: an attach cannot be taken off a roadmap until P3 ships `MaterialDetached`, and the runs have already left 3 attached materials on the shared dev account. Recorded, deferred.
- P1 FOUND  `e2e/playwright.config.ts` cannot run in a worktree: its marketing webServer (`pnpm --filter @study-tracker/marketing dev`, port 4321) never becomes reachable, so `config.webServer` times out at 120 s and every project fails before a single test runs. A temporary config declaring only the app webServer (reuse) worked. Also `app-mobile` is pinned to `testMatch: /roadmap\.spec\.ts/`, so `--project=app-mobile` runs zero tests for any other spec.
- P1 TICKED  AC1, AC3, AC4, AC7 and the P1 phase gate in `plan/VERIFICATION.md`, each with its evidence and its caveat. AC2/AC5/AC6 wait on P2/P5/P3.
- P1 NEXT  commit the records, push the branch, then P2 (minutes + role chosen at attach time).
- P2 DONE  `MaterialPicker` `withPlan`: a selected row shows a minutes number input (seed `estimatedMinutes ?? 60`, min 15, step 15) and a role select off `ROLE_TO_LABEL`; `onContinue` sends the plan keyed by the selected ids only. The controls live in a `.checkbox-entry` wrapper BESIDE the row `<label>` — nested in the label, clicking one would also toggle the checkbox. `RoadmapCalendar` passes `withPlan`; its handler already read `plan?.[id]` from P1.
- P2 DEVIATION  dropped the planned `defaultMinutes?: Record<string, number>` prop: the calendar holds no library rows and never sees `estimatedMinutes`, while the picker already has the `MaterialRecord`. Seeding from the record replaced the prop. Recorded in the plan notes.
- P2 VERIFIED  typecheck clean · lint clean · MaterialPicker 14/14 (+4) · RoadmapCalendar 13/13 (+1), all red-first.
- P2 LIVE  restarted the app first (rule 53) and confirmed the served module carries `withPlan`/`checkbox-plan` (7 hits). Green: a fresh plain-text library material attached at **90 minutes as `practice`** → header `3 MATERIALS` → `4 MATERIALS`, directory row `0m of 1h 30m`, session setup chooser `Practice`, zero page errors. Needed a fresh material because every library material was already attached to the active roadmap and P3 (detach) has not shipped.
- P2 NOTE  OQ-07 (per-row controls vs one batch role at 375) is **unmeasured** — the picker had no rows to render at 375. The decision waits on the user's hands-on pass, which is what the plan designated it for.
- P2 CLEANUP  the failed first attempt left one stray library material; the strays were deleted (a first cleanup spec deleted it but failed its own assertion, a re-run confirmed zero strays). The material the passing run attached is kept so its roadmap row stays coherent.
- P2 NEXT  commit + push, then P3 (detach).
- P3 DONE  on the user's instruction the detach rides the row's material badge: `.dir-icon` wrapper + `.dir-detach` overlay, `opacity: 0` until hover/focus, `title` = "Detach material from roadmap?" plus an upcoming-session count, one click logs `MaterialDetached {roadmapCreatedAt, materialId}`. No confirm sheet (drops the plan's `MaterialRemoveSheet`) and no cascade, so OQ-06 resolves as "no". The control renders only when not `readOnly`. P1's reader mask already dropped the material from the directory, the pickers, progress and Replan, so no other file moved.
- P3 VERIFIED  typecheck clean · lint clean · `src/roadmap` + `src/progress` 122/122 (mapEvents 20, roadmapProgress 4, RoadmapCalendar 16), red-first. Two pre-existing unscoped `getByRole('button', {name: /Operating Systems/})` queries broke on the new aria-label and were scoped with `within(...)` — recorded as a pitfall.
- P3 LIVE  green at 1280: `×` measured `opacity: 0` before hover and `1` after; tooltip `Detach material from roadmap?`; click dropped the row and the header count; re-attaching through the picker restored both; zero page errors. Not exercised live: the booked-bubble label (no detached material had a booking) — unit-covered by `materialTitleIndex`'s detach test.
- P3 CLEANUP  the same live run stripped the test residue: the three leftover attaches are off the roadmap and both `E2E P2 attach …` library materials are deleted. The roadmap is back to its own two.
- P3 CORRECTION  the running header count read one low all along (a fresh browser reporting a partly-restored event log), so the roadmap started with **two** materials of its own, not one. My earlier "3 of 4 were test residue" was one short: it was 3 of 5.
- P3 NEXT  commit + push, then P4.
