# Verification – roadmap-material-attach

_Ticket: GitHub #63 (filed 2026-09-12) · Parent spec #32 · Plan: plan/PLAN.md · Updated: 2026-09-12_

## AC tick-down (all unchecked until proven)

- [x] AC1 — The roadmap's Materials panel has a "+" that opens a picker of library materials not already on this roadmap, and attaching affects this roadmap only. — Evidence: `RoadmapCalendar.test.tsx` ("attaches a library material from the materials directory \"+\"", "lists an attached material in the directory and counts it in the header", "disables the add-material control in read-only history mode"); two-roadmap scoping in `mapEvents.test.ts` ("scopes an attachment to its own roadmap and keeps that roadmap budget"); live 1280 attach — header `1 MATERIALS` -> `2 MATERIALS`, directory row, zero page errors (`.work/active/roadmap-material-attach/research/2026-09-12-p1-live-verification.md`). **375 leg partial:** the picker opened and honoured `excludeIds` at 375, but the run found every library material already attached, so the 375 *attach* is deferred to after P3 (see the research note).
- [x] AC2 — Each attached material carries its own minutes budget **and role**, both set at attach time, and the directory shows consumed/total. — Evidence: `MaterialPicker.test.tsx` ("shows minutes and role once a row is selected, seeded from the library estimate", "sends the edited minutes and role with the selection", "defaults to 60 minutes and omits rows that were deselected again", "renders no plan controls when withPlan is not set") and `RoadmapCalendar.test.tsx` ("logs the minutes and role chosen in the attach picker"), all red-first; live — a fresh library material attached at 90 minutes as `practice` produced the directory row `0m of 1h 30m` and the `Practice` label in session setup (header `3 MATERIALS` -> `4 MATERIALS`, zero page errors). OQ-07's mobile crowding is unmeasured; see the P2 notes.
- [x] AC3 — Attached materials appear in the add-session and booking-edit pickers and in session setup. — Evidence: live — New session -> Attach listed the attached material, and session setup's chooser listed it as `Foundations` (the P1 role default); `RoadmapCalendar.test.tsx` covers the add-session path. Both sheets read the same `materialOptions` ledger, and session setup reads `sessionPlanning`, so the booking-edit leg is covered by construction rather than by a click.
- [x] AC4 — Attached materials survive Replan and count in roadmap progress; no duplicate readers remain. — Evidence: `mapToRegenerateRequest.test.ts` ("includes an attached library material after the declared ones") and `roadmapProgress.test.ts` ("counts an attached library material in the no-slots ledger") both green; `grep -rn "kind === 'MaterialAdded'\|kind !== 'MaterialAdded'" apps/app/src` returns the join only in `progress/mapEvents.ts` (`onboarding/steps/Step4Confirm.tsx:31` is a single-material preview lookup, not the roadmap set). Live-verified against the roadmap header, not through the Replan screen.
- [ ] AC5 — Material detail lists the roadmaps a library material is attached to and the delete warning names them. — Evidence required: `useMaterialUsage` unit test + `MaterialDetail.test.tsx`; live `/materials/:id` showing the roadmap name. (P5.)
- [ ] AC6 — A material can be removed from a roadmap's set, with the affected upcoming sessions disclosed, and existing bookings keep their material label. — Evidence required: `MaterialRemoveSheet` + `mapEvents.test.ts` detach cases (declared id masked, attach-after-detach restores, other roadmaps unaffected) + a live pass where a booked bubble keeps its title after removal; re-attach restores consumed minutes. (P3. The reader's detach *mask* is already written and covered by P1's `roadmapMaterialPayloads`; only the producer is missing.)
- [x] AC7 — No server, schema or Dexie change; old roadmaps replay byte-identically. — Evidence: `git diff --stat 2091d8e -- supabase/migrations apps/app/src/events packages/ services/` is empty (25 files changed on the branch, none of them those); the pre-existing `mapEvents`/`RoadmapCalendar` cases pass unchanged alongside the new ones (16/16 and 12/12 in those files).

## Phase gates

- [x] P0 ticket: GitHub issue filed on the #33–#49 spine, claimed, branch cut, number recorded in the plan header + `state.md` + the STATUS row. — Evidence: https://github.com/rings0fsaturn/study-planner/issues/63 (`gh issue view 63 --json state,assignees`); branch `phase2/issue-63-roadmap-material-attach` off `2091d8e` in worktree `../study-planner-web-issue-63`.
- [x] P1 attach + shared reader: app suite green; typecheck/lint clean; a live attach shows the material in the directory, the New session picker and session setup; two-roadmap scoping case green; header count uses the active set while a detached/unrelated booking still labels correctly. — **Done 2026-09-12:** typecheck/lint clean, app suite 800/802 (2 = the documented WSL TZ pair, green under `--pool=forks`); two-roadmap scoping (`mapEvents.test.ts` "scopes an attachment to its own roadmap and keeps that roadmap budget"), header count from the active set and directory listing (`RoadmapCalendar.test.tsx`), attached material counted in progress and surviving the regenerate request; **live 1280 pass green** — attach from the "+" moved the header `1 MATERIALS` -> `2 MATERIALS`, the directory row appeared, New session -> Attach listed it and session setup showed it as `Foundations`, zero page errors. Transcript and the two findings: `research/2026-09-12-p1-live-verification.md`. Outstanding: the fresh 375 attach (deferred until P3 can take attaches back off).
- [x] P2 minutes + role: picker control tests green; live attach at 90 minutes as `practice` renders "0m of 1h 30m" and the practice label in session setup. — **Done 2026-09-12:** `MaterialPicker.test.tsx` 14/14 (+4) and `RoadmapCalendar.test.tsx` 13/13 (+1) green, typecheck/lint clean; live attach at 90 minutes as `practice` gave the `0m of 1h 30m` directory row and `Practice` in session setup. **OQ-07 not measured** (the picker had no rows at 375, every library material already being attached); the per-row-controls-vs-batch-role decision waits on the hands-on pass.
- [ ] P3 detach: detach masks declared and attached ids, re-attach restores, bookings keep their labels, the header count and `% done` drop. — Evidence required: `mapEvents.test.ts`/`RoadmapCalendar.test.tsx` output + live pass transcript.
- [ ] P4 session surfaces + live E2E: `e2e/roadmap-material-attach-live.spec.ts` green with `--workers=1` at 1280 and 375 with zero console errors. — Evidence required: playwright output + the spec path.
- [ ] P5 library usage: hook + detail tests green; live `/materials/:id` names the roadmap. — Evidence required: test output + live note.

## Live evidence

- **2026-09-12 P1, green at 1280:** attach from the roadmap's "+" moved the header `1 MATERIALS` -> `2 MATERIALS`, the directory row appeared, New session -> Attach listed the material and session setup's chooser showed it as `Foundations`; zero page errors. Transcript, the 375 caveat and two reusable findings: `research/2026-09-12-p1-live-verification.md`.
- Residue on the shared dev account: the active roadmap now carries 4 attached library materials from the runs, removable only once P3 ships `MaterialDetached`. Nothing else was created except the one plain-text material the P2 pass needed (`E2E P2 attach ...`), which is left in the library so the roadmap row it produced stays coherent; the strays a failed run created were deleted.
- **2026-09-12 P2, green at 1280:** a fresh plain-text library material attached at **90 minutes as `practice`** moved the header `3 MATERIALS` -> `4 MATERIALS`, the directory row read **`0m of 1h 30m`**, and session setup's chooser showed **`Practice`**; zero page errors. The picker seeded the minutes field at `60` for a material with no library estimate. OQ-07 (per-row controls vs one batch role on a phone) is unmeasured.
