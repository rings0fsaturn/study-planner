# Verification – roadmap-material-attach

_Ticket: GitHub #63 (filed 2026-09-12) · Parent spec #32 · Plan: plan/PLAN.md · Updated: 2026-09-12_

## AC tick-down (all unchecked until proven)

- [x] AC1 — The roadmap's Materials panel has a "+" that opens a picker of library materials not already on this roadmap, and attaching affects this roadmap only. — Evidence: `RoadmapCalendar.test.tsx` ("attaches a library material from the materials directory \"+\"", "lists an attached material in the directory and counts it in the header", "disables the add-material control in read-only history mode"); two-roadmap scoping in `mapEvents.test.ts` ("scopes an attachment to its own roadmap and keeps that roadmap budget"); live 1280 attach — header `1 MATERIALS` -> `2 MATERIALS`, directory row, zero page errors (`.work/active/roadmap-material-attach/research/2026-09-12-p1-live-verification.md`). **375 leg closed in P4:** the phone scenario of `e2e/roadmap-material-attach-live.spec.ts` attaches a library material from the "+" at 375x812 on the live stack and asserts the directory row (`of 1h 30m`), then detaches it.
- [x] AC2 — Each attached material carries its own minutes budget **and role**, both set at attach time, and the directory shows consumed/total. — Evidence: `MaterialPicker.test.tsx` ("shows minutes and role once a row is selected, seeded from the library estimate", "sends the edited minutes and role with the selection", "defaults to 60 minutes and omits rows that were deselected again", "renders no plan controls when withPlan is not set") and `RoadmapCalendar.test.tsx` ("logs the minutes and role chosen in the attach picker"), all red-first; live — a fresh library material attached at 90 minutes as `practice` produced the directory row `0m of 1h 30m` and the `Practice` label in session setup (header `3 MATERIALS` -> `4 MATERIALS`, zero page errors). OQ-07's mobile crowding is **measured and resolved in P4**: at 375x812 the minutes field and role select are 158 px each inside a 375 px sheet, the longest role label (`Foundations`) renders in full, and `sheet.scrollWidth === sheet.clientWidth`; the per-row controls stay (no batch-role fallback), evidence `research/2026-09-12-p4-oq07-375.png`.
- [x] AC3 — Attached materials appear in the add-session and booking-edit pickers and in session setup. — Evidence: live — New session -> Attach listed the attached material, and session setup's chooser listed it as `Foundations` (the P1 role default); `RoadmapCalendar.test.tsx` covers the add-session path. Both sheets read the same `materialOptions` ledger, and session setup reads `sessionPlanning`, so the booking-edit leg is covered by construction rather than by a click. P4 added the empty-state CTA (`MaterialPickerSheet.onRequestAddMaterial` → the roadmap's "+" picker) with three picker cases and two calendar wiring cases; the CTA itself is not exercised live because every dev-account roadmap already has materials.
- [x] AC4 — Attached materials survive Replan and count in roadmap progress; no duplicate readers remain. — Evidence: `mapToRegenerateRequest.test.ts` ("includes an attached library material after the declared ones") and `roadmapProgress.test.ts` ("counts an attached library material in the no-slots ledger") both green; `grep -rn "kind === 'MaterialAdded'\|kind !== 'MaterialAdded'" apps/app/src` returns the join only in `progress/mapEvents.ts` (`onboarding/steps/Step4Confirm.tsx:31` is a single-material preview lookup, not the roadmap set). Live-verified against the roadmap header, not through the Replan screen.
- [x] AC5 — Material detail lists the roadmaps a library material is attached to and the delete warning names them. — Evidence: `mapEvents.test.ts` `materialUsageLabels` (two roadmaps, detach drops and re-attach restores, unrelated/undefined id empty); `MaterialDetail.test.tsx` (empty state, and the usage + delete-dialog naming a roadmap); live — the desktop scenario of `e2e/roadmap-material-attach-live.spec.ts` attaches a library material, opens its `/materials/:id` page and asserts the `Used by` list names the active roadmap, then cleans up. **Done 2026-09-12 (P5).**
- [x] AC6 — A material can be removed from a roadmap's set, with the affected upcoming sessions disclosed, and existing bookings keep their material label. — Evidence: `mapEvents.test.ts` (detach masks an attached id and a declared id, attach-after-detach restores it, another roadmap's detach is ignored, `materialTitleIndex` still labels a detached material), `roadmapProgress.test.ts` ("drops a detached material back out of the ledger"), `RoadmapCalendar.test.tsx` ("detaches a material from its icon, naming what stays behind", "warns that upcoming sessions keep their label", "keeps the detach control out of read-only history mode"); live — the `×` was `opacity: 0` before hover and `1` after, one click dropped the row and the header count, and re-attaching restored both. Disclosure is the tooltip, not a confirm sheet (see the P3 notes). **Booked-bubble leg closed live in P4:** the desktop scenario of `e2e/roadmap-material-attach-live.spec.ts` books a session on the attached material, detaches it, and asserts the `Booked: <title>` bubble is still there afterwards (then removes the throwaway booking).
- [x] AC7 — No server, schema or Dexie change; old roadmaps replay byte-identically. — Evidence: `git diff --stat 2091d8e -- supabase/migrations apps/app/src/events packages/ services/` is empty (25 files changed on the branch, none of them those); the pre-existing `mapEvents`/`RoadmapCalendar` cases pass unchanged alongside the new ones (16/16 and 12/12 in those files).

## Phase gates

- [x] P0 ticket: GitHub issue filed on the #33–#49 spine, claimed, branch cut, number recorded in the plan header + `state.md` + the STATUS row. — Evidence: https://github.com/rings0fsaturn/study-planner/issues/63 (`gh issue view 63 --json state,assignees`); branch `phase2/issue-63-roadmap-material-attach` off `2091d8e` in worktree `../study-planner-web-issue-63`.
- [x] P1 attach + shared reader: app suite green; typecheck/lint clean; a live attach shows the material in the directory, the New session picker and session setup; two-roadmap scoping case green; header count uses the active set while a detached/unrelated booking still labels correctly. — **Done 2026-09-12:** typecheck/lint clean, app suite 800/802 (2 = the documented WSL TZ pair, green under `--pool=forks`); two-roadmap scoping (`mapEvents.test.ts` "scopes an attachment to its own roadmap and keeps that roadmap budget"), header count from the active set and directory listing (`RoadmapCalendar.test.tsx`), attached material counted in progress and surviving the regenerate request; **live 1280 pass green** — attach from the "+" moved the header `1 MATERIALS` -> `2 MATERIALS`, the directory row appeared, New session -> Attach listed it and session setup showed it as `Foundations`, zero page errors. Transcript and the two findings: `research/2026-09-12-p1-live-verification.md`. Outstanding: the fresh 375 attach (deferred until P3 can take attaches back off).
- [x] P2 minutes + role: picker control tests green; live attach at 90 minutes as `practice` renders "0m of 1h 30m" and the practice label in session setup. — **Done 2026-09-12:** `MaterialPicker.test.tsx` 14/14 (+4) and `RoadmapCalendar.test.tsx` 13/13 (+1) green, typecheck/lint clean; live attach at 90 minutes as `practice` gave the `0m of 1h 30m` directory row and `Practice` in session setup. **OQ-07 not measured** (the picker had no rows at 375, every library material already being attached); the per-row-controls-vs-batch-role decision waits on the hands-on pass.
- [x] P3 detach: detach masks declared and attached ids, re-attach restores, bookings keep their labels, the header count and `% done` drop. — **Done 2026-09-12:** all four `mapEvents.test.ts` detach cases green, plus the ledger and calendar cases; live at 1280 the hover-× dropped the row and the header count and re-attaching restored both. The `% done` movement is the same `materialLedger` change the count reflects. Booked-bubble label is unit-covered only.
- [x] P4 session surfaces + live E2E: `e2e/roadmap-material-attach-live.spec.ts` green with `--workers=1` at 1280 and 375 with zero console errors. — **Done 2026-09-12:** the CTA (`onRequestAddMaterial` on `MaterialPickerSheet`, passed through both booking sheets and wired to the roadmap's "+" picker) landed red-first — `MaterialPickerSheet.test.tsx` 7/7, `RoadmapCalendar.test.tsx` 18/18, app suite 818/820 (2 = the documented WSL TZ pair); the new spec ran **2 passed** at 1280 and 375 with zero page errors, and it closes all three deferrals (375 attach, OQ-07 measured at 158 px + screenshot, AC6's booked-bubble leg). Both scenarios undo their own writes; the shared roadmap ends with only its own two materials and its own two bookings.
- [x] P5 library usage: hook + detail tests green; live `/materials/:id` names the roadmap. — **Done 2026-09-12:** `materialUsageLabels` in `progress/mapEvents.ts` (reuses `deriveRoadmapLifecycle` + `roadmapMaterialPayloads`; no third attach/detach join) with three `mapEvents.test.ts` cases; `materials/useMaterialUsage.ts` is a thin `useEventStore` + `useLiveQuery` wrapper; `MaterialDetail.tsx` drops the `const usage: string[] = []` stub for the hook (the `DeleteConfirm` and `Used by` markup needed no change). `mapEvents.test.ts` 23 passed, `MaterialDetail.test.tsx` 14 passed, typecheck + lint clean. Live: `e2e/roadmap-material-attach-live.spec.ts` 2/2 with `--workers=1` including the new AC5 leg (the detail page names the roadmap), then cleans up.

## Live evidence

- **2026-09-12 P1, green at 1280:** attach from the roadmap's "+" moved the header `1 MATERIALS` -> `2 MATERIALS`, the directory row appeared, New session -> Attach listed the material and session setup's chooser showed it as `Foundations`; zero page errors. Transcript, the 375 caveat and two reusable findings: `research/2026-09-12-p1-live-verification.md`.
- Residue the runs created (all cleared by the P3 pass): the roadmap briefly carried three test attaches. The running count read one low throughout because a fresh browser reported a partly-restored event log, so the roadmap actually started with **two** materials of its own, not one.
- **2026-09-12 P2, green at 1280:** a fresh plain-text library material attached at **90 minutes as `practice`** moved the header `3 MATERIALS` -> `4 MATERIALS`, the directory row read **`0m of 1h 30m`**, and session setup's chooser showed **`Practice`**; zero page errors. The picker seeded the minutes field at `60` for a material with no library estimate. OQ-07 (per-row controls vs one batch role on a phone) is unmeasured.
- **2026-09-12 P3, green at 1280:** the badge `×` measured `opacity: 0` before hover and `1` after, its tooltip read `Detach material from roadmap?`, one click dropped the row and moved the header count, and re-attaching the same material through the picker restored both. The same run then used the control to take the leftover test attaches off the dev account and deleted the throwaway library material, so **the shared account carries no test residue any more** - the roadmap holds only the two materials it started with (`Introduction to Spring Data JPA …` and `Designing-Data-Intensive-Applications`).
- **2026-09-12 P4, green at 1280 + 375 (2 passed, ~29 s):** `pnpm exec playwright test -c e2e/playwright.config.ts e2e/roadmap-material-attach-live.spec.ts --project=app --workers=1`. Desktop: attach a library material at 90 minutes as `practice` → directory `of 1h 30m` → New session → Attach lists it → book → detach from the badge → the `Booked:` bubble keeps its label → remove the booking. Phone (375x812): the same attach with the plan controls measured at **158 px each**, sheet right edge 375, `scrollWidth === clientWidth`, then detach. Zero page errors in both; the run needed no throwaway Playwright config (the repo config's marketing + app webServers came up in this worktree — the P1 note did not reproduce). Screenshot: `research/2026-09-12-p4-oq07-375.png`.
- **Residue found, not created:** the account's *library* still carries two `E2E P2 attach <epoch-ms>` materials from the P2 live session (the P3 note that "the throwaway library material" was deleted is true for one of them). The P4 runs detached the leftover roadmap attaches and removed the leftover booking; the two library rows were left alone and are flagged in `state.md` for the user.
