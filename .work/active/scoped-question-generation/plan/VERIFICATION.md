# Verification – scoped-question-generation (#18)

_Ticket: GitHub #62 (filed + claimed 2026-09-11) · Parent spec #32 · Plan: plan/PLAN.md · Updated: 2026-09-12_

## AC tick-down (all unchecked until proven)

- [x] AC1 — Generation grounds in the chosen chapter or page range, and exam/syllabus/document meta cannot be asked even without a scope. — Evidence required: prompt blocklist asserted in both templates; a live scoped generation whose question is about the material's content; an unscoped generation that still refuses exam-format framing. **P1 (2026-09-11) landed the blocklist half:** both templates carry `META_BLOCKLIST`, the title is out of the steer, and a real A/B generation showed exam framing gone with a front-matter steer and a body-grounded question with a chapter steer (`research/2026-09-11-p1-live-verification.md`). **P4 (2026-09-11) closed the grounding half:** a Chapter 5 scope (pdf 156..213) generated a question about the budget definitions on page 158, citing only Chapter 5 body chunks, and the unscoped arm of the same request path produced a supply-chain question from a body chunk rather than front matter. Both halves measured on the authenticated API and re-confirmed in the browser at 1280 and 375 (`research/2026-09-11-p4-live-verification.md` §2/§4).
- [x] AC2 — Chunks carry `page_start`/`page_end` and `match_content_chunks` filters on a page range. — Evidence required: unit tests for propagation, boundary straddling and filter inclusivity; migration 029 applied on dev Supabase; live re-ingest with every chunk paged. **P2 (2026-09-11) landed the whole AC:** migration 029 pushed, the APM material re-ingested to 754/754 paged chunks (1..572, 372 straddling, 0 out of range), the RPC's page bounds verified inclusive by overlap on single-page ranges, and the pre-P2 chunk texts proven identical on the real fixture. Details: `research/2026-09-11-p2-live-verification.md`. **P4 consumed it:** every scoped run returned only chunks overlapping the requested pages (a Chapter 5 scope's citations sit at pages 158 and 202, inside 156..213).
- [x] AC3 — The outline is derived at ingestion and matches the document's own contents page. — Evidence required: unit tests for the three TOC layouts; the APM outline compared line-by-line against the printed contents page (16 chapters, offset -33); deterministic re-run on SICP/CSAPP/DDIA. **P3 (2026-09-11) landed the whole AC:** the three layouts are pinned by `tests/test_outline.py` (plus the offset rules, bookmark path, refused-outline and fallback gating), and the live re-ingest stored an outline equal entry-for-entry to the offline parse of the same fixture — 16 chapters, `page_count` 572, `page_offset` -33, source `contents` (no provider call). Details: `research/2026-09-11-p3-live-verification.md`. The SICP/CSAPP/DDIA re-run was not repeated this session (those PDFs are not in the checkout); the parser core is unchanged from the prototype that was measured on all four. **P4 consumed it:** the config page's chapter chips come from that outline, and the Chapter 5 chip resolves to pdf 156..213 as the contents page implies.
- [x] AC4 — The learner picks a chapter or a typed page range and can open the material in a viewer. — Evidence required: config picker fed by `materials.outline`; live click-through at 1280 and 375; viewer page number visible and consistent with the range control. **P4 (2026-09-11) landed the picker half:** chapter chips render from `materials.outline`, fill both page inputs (156/213 for Chapter 5), "Whole material" clears them, a typed range is validated client- and server-side, and the click-through passes at 1280 and 375. **P5 (2026-09-11) closed the viewer half:** `/materials/:materialId/view` renders the private PDF from a 600 s signed URL, the page number is visible and drives the range control (`Set first page` / `Set last page` → `Assess pages 156–213` → the config's inputs hold 156/213), and `e2e/material-viewer-live.spec.ts` passes 2/2 at 1280 and 375 with a real-ink canvas assertion. `research/2026-09-11-p5-live-verification.md` §4.
- [x] AC5 — Every citation of a scoped generation falls inside the chosen scope. — Evidence required: live gate — pick a chapter, generate, assert each cited chunk's page lies in the range. **P4 (2026-09-11):** the authenticated gate ran the Chapter 5 scope and joined each citation's `chunkId` to `content_chunks` under the service role — ordinals 204 (page 158) and 269 (page 202), both inside 156..213, warnings `[]`. The deliberate thin-scope counter-case (page 156..156) widened to 151..161 with a `scope_widened` warning and cited page 161, inside the widened range — the documented D-06 behaviour, not a scope violation. `research/2026-09-11-p4-live-verification.md` §2/§3.
- [x] AC6 — Existing materials need no backfill migration. — Evidence required: re-ingest path exercised on the APM material with no scripted backfill, and the page columns populated as a result. **P2/P3 (2026-09-11):** the APM material was re-ingested twice through `retry_material_ingestion` (no backfill script) and came back fully paged with an outline. **P4 adds nothing to migrate at all:** the scope rides `assessments.recipe`, an existing jsonb column the read route already echoes, so the whole phase shipped with zero schema change.

## Phase gates

- [x] P1 prompt + steer: `uv run pytest services/intelligence/tests -q` green with updated prompt/context assertions; the retrieval probe returns technical ordinals for a chapter steer; one real generation reads as content, not exam framing. — **Done 2026-09-11:** focused generation+assessment suites 139 passed; service suite 509 passed / 7 failed with the identical 7 failing on the untouched base; probe chapter steer -> ordinals 203/271/204/269/202 (Chapter 5 body) vs the pre-fix title steer -> 753/0/67/49/24 (front matter); real DeepSeek A/B produced a body-grounded question for the chapter steer and no exam question in either arm. Transcript: `research/2026-09-11-p1-live-verification.md`. Rode along: D-08 (empty steer refused) and the worker's non-retryable context path now fails the assessment instead of leaving it `generating`.
- [x] P2 page provenance: focused service tests green; `content_chunks.page_start/page_end` populated for all 754 chunks after re-ingest; RPC range filter returns an inclusive, correct subset; existing embeddings not invalidated (text byte-identical). — **Done 2026-09-11:** focused suites 126 passed; whole service 515 passed / 7 failed with the identical 7 failing on the untouched base (clean `git worktree` at `c30cd43`); offline on the real 572-page fixture the paragraph stream (1257) and all 754 chunk texts are byte-identical to the pre-P2 path, with 754/754 paged and 372 straddling; live re-ingest of the APM material gives `total=754 paged=754 min_p=1 max_p=572 straddling=372 out_of_range=0 embedded=754`; the RPC returns only overlapping chunks (page 203 excluded from a 124..170 range) and keeps straddling chunks on single-page ranges. Transcript: `research/2026-09-11-p2-live-verification.md`.
- [x] P3 outline: outline unit tests green (three layouts + permissive gating); live APM outline equals the printed contents page; no model call on the deterministic path. — **Done 2026-09-11:** focused suites 123 passed (14 new outline tests + 2 extractor + 2 worker); migration 030 pushed after a dry-run that listed exactly 030; the APM material re-ingested with the outline stored in the same call as the `chunking` transition (`pages=572 offset=-33 entries=16`, `ready` in ~80 s with 754 re-embeddings); the stored outline equals the offline parse of the same fixture entry for entry, `source=contents` so the DeepSeek fallback never ran. Offset measured at 99.3% agreement (282/284 voting pages on -33); the strict rule is what keeps it trustworthy (the loose rule reads 64.8%). Whole service 533 passed / 7 failed with the identical 7 pre-existing failures. Transcript: `research/2026-09-11-p3-live-verification.md`.
- [x] P4 scope + picker: contracts green; route matrix green (out-of-range, inverted range, zero-chunk, thin-range widen-and-warn); app tests green; live scoped generation with every citation in range. — **Done 2026-09-11:** `contracts/phase2` 22 passed (scope schema + fixture); route matrix live — over-572 → 409, inverted → 400, pageless material scoped → 409, a single page widens to 151..161 with `scope_widened`, a 3-page range does *not* widen (5 chunks = `CONTEXT_TOP_K`), and a page-only scope with no label spreads instead of embedding an empty query; whole app suite 762 passed / 2 failed (the documented WSL TZ pair, 2/2 green under `--pool=forks`), `pnpm typecheck` + `pnpm lint` clean, service `tests/` minus the 7 known failures 518 passed; live gate — Chapter 5 (pdf 156..213) → `ready`, warnings `[]`, citations at pages 158 and 202, both inside the range; browser click-through 3/3 at 1280 and 375. Transcript: `research/2026-09-11-p4-live-verification.md`.
- [x] P5 viewer: live at 1280 and 375 — renders, chapter jump, range select, hands off to the config, no console errors, no blank canvas (worker wired). — **Done 2026-09-11:** `e2e/material-viewer-live.spec.ts` 2/2 with `--workers=1`; both scenarios gate on canvas ink, not just a mounted element, so a missing `workerSrc` (blank canvas, console warning only) fails the run. Measured at 1280: toolbar 2.58 s after navigation, first page painted at 6.32 s, canvas 1080×1545 CSS px fit-to-width, 1,668,600 dark pixels on page 1, chapter jump to the Chapter 5 opener (page 156) repainting with 67,051 dark pixels, zero console/page errors. Range label `Assess pages 156–213` handed over as `?from=156&to=213` with the config's inputs holding both and no chapter chip selected; the handoff generates and the citations render. The authenticated API gate on the same label-less scope returns `ready`, warnings `[]`, citation on pages (202,202) — inside 156..213. `pnpm typecheck`/`pnpm lint` clean; `materialClient.test.ts` 30 passed, `AssessmentConfig.test.tsx` 12 passed, whole app suite 768 passed / 2 failed (the documented WSL TZ pair, 2/2 green under `--pool=forks`). Transcript: `research/2026-09-11-p5-live-verification.md`. **Open finding (recorded, not a defect):** the viewer pulls the whole 22.9 MB per open because Supabase Storage does not expose `Accept-Ranges` over CORS, so pdf.js never enables range requests; an explicit `PDFDataRangeTransport` was implemented, measured as worse (123 requests / 30.8 MB / 12.9 s) and reverted. See §5 of the transcript for the numbers and the three upgrade paths.

## Live evidence

- 2026-09-11 (P1): retrieval probe + real-model A/B on the ACCA APM material — `research/2026-09-11-p1-live-verification.md`.
- 2026-09-11 (P1): empty-steer measurement (ordinals 393/259/90/532/344, fragments) that produced D-08.
- 2026-09-11 (P1): **authenticated API run** — password grant -> `POST /v1/assessments/generate` with `skillTags: ["Chapter 5 Budgeting and control"]` -> `202` -> poll -> `ready`; the question is Chapter 5 content (ZBB) citing chunk ordinal 203. One `citation_unverified` warning (paraphrase quote, pre-existing gate behaviour).
- Not run (P1): the browser click-through — P1 changed no UI (the config tag box is the steer surface until P4), so the authenticated API run is the real request path.
- 2026-09-11 (P2): page identity offline on the real fixture + live re-ingest of the APM material + page-bounded RPC probes — `research/2026-09-11-p2-live-verification.md`.
- 2026-09-11 (P3): outline offline on the real fixture (16 chapters, offset -33 at 99.3% header agreement) + migration 030 applied + live re-ingest storing an outline equal entry-for-entry to the offline parse — `research/2026-09-11-p3-live-verification.md`.
- 2026-09-11 (P4): **authenticated scoped gate** — `recipe.scope = {156, 213, "Chapter 5 Budgeting and control"}` → `202` → `ready`, warnings `[]`, every citation joined to `content_chunks` under the service role lands inside 156..213 (ordinals 204 page 158, 269 page 202); the recipe round-tripped through the jsonb verbatim with no migration.
- 2026-09-11 (P4): **route matrix** — 3-page range does not widen (5 chunks = `CONTEXT_TOP_K`), single page widens to 151..161 with `scope_widened`, over-572 → 409, inverted → 400, pageless `url` material → 409, page-only scope with no label spreads and still cites inside the range.
- 2026-09-11 (P4): **browser click-through** 3/3 with `--workers=1` — unscoped generation at 1280, chapter chip → page range → generate → citations at 1280, chip fill/reset at 375.
- 2026-09-11 (P4): found and fixed a **stale spec**: `assessment-generation-live.spec.ts` waited for `Difficulty band` and read citations from `.card-large`, both of which the detail page stopped rendering at the #40/#41 rework, so the spec had been failing independently of this phase.
- 2026-09-11 (P5): **storage precheck under the service role** — the raw PDF (22,919,258 bytes) is present at `<uid>/<materialId>/<file>`, a signed URL answers 200, and a range GET answers 206; no storage work was needed for the viewer.
- 2026-09-11 (P5): **viewer live at 1280 and 375** — `e2e/material-viewer-live.spec.ts` 2/2 (`--workers=1`): real-ink canvas assertion (1,668,600 dark pixels), chapter jump to page 156, `Assess pages 156–213` handed to the config as `?from=156&to=213`, then a generated question with citations. Toolbar at 2.58 s, first paint at 6.32 s, canvas 1080×1545 CSS px, zero console/page errors.
- 2026-09-11 (P5): **measured the whole-file download and rejected the obvious fix** — pdf.js sends one plain GET (no `Range`), 22,919,258 bytes per open, 4.6-7.5 s; the cause is `Accept-Ranges` not being CORS-exposed by Supabase Storage (verified: browser `HEAD` exposes only `cache-control`, `content-length`, `content-type`, `expires`, `last-modified`; a browser `Range: bytes=0-65535` gets 206/65,536 bytes). An explicit `PDFDataRangeTransport` was implemented and measured *worse* (123 requests, 30,807,072 bytes, 12.9 s, continued fetching while idle) and reverted.
- 2026-09-11 (P5): **authenticated API gate for a viewer-shaped scope** — `recipe.scope = {156, 213}` with no `sectionLabel` → `202` → `ready`, warnings `[]`, recipe echoed verbatim, the single citation (ordinal 269) on pages (202,202), inside the range; question about zero-based budgeting citing "ZBB requires each cost element to be justified".
- 2026-09-11 (P5): **neighbouring live paths re-run green** after the `AssessmentConfig` (search-param seeding) and `MaterialDetail` (new action) changes: `e2e/assessment-generation-live.spec.ts` and `e2e/material-library-live.spec.ts`.

## Wayfinder resolution (exit)

- [x] File the GitHub ticket on the #33-#49 spine, claim it, and add the map #4 line. — **Ticket #62 filed + assigned 2026-09-11** (`wayfinder:phase2` + `ready-for-agent`). Map #4 line still to add at resolution.
- [ ] Resolution comment; issue closed; STATUS row flipped to Done; folder archived.

## Revision acceptance criteria (R1–R4, raised 2026-09-12 from a hands-on pass)

Added after the AC1–AC6 tick-down, which stays valid as written. These four cover the viewer revision (Phases 6-7); they do not re-open the ticket's own criteria.

- [x] R1 — The page fits the window: at 1280 and at 375 the rendered page is fully visible horizontally (`frame.scrollWidth === frame.clientWidth`), and re-fitting survives a viewport change (resize 1280 → 375 re-renders and the canvas CSS width stays within the frame). — Evidence required: the two live assertions in `e2e/material-viewer-live.spec.ts`, including the resize step that fails on `c803be5`/`ff3ab07`. **P6 (2026-09-12):** measured live — 1280x720 Fit: canvas 1080 px inside a 1080 px frame, `scrollWidth === clientWidth`; 375x812 Fit: canvas 351 px inside a 351 px frame, `scrollWidth === clientWidth`; a 1280 → 375 `setViewportSize` re-fits the canvas to 351 px in the same run and back to >500 px. The one width source is a `ResizeObserver` on `.pdf-frame` (`pdfView.displayScale`), which is the fix for the measure-once crop. `research/2026-09-12-p6-live-verification.md` §1/§5.
- [x] R2 — Reading is a vertical scroll, not paging: every page has a slot, the next page's ink appears by scrolling, and a chapter jump or page entry scrolls to that page's slot. Prev/Next are gone. — Evidence required: the live spec scroll step + the chapter jump + the page input. **P6 (2026-09-12):** 572 `.pdf-page` slots at load with `rendered 1` (windowing, one frame-height of margin); scrolling the frame to page 2's slot moves the page readout to `2` and rasterises page 2; the Chapter 5 jump scrolls to pdf page 156 with the readout and the chapter's ink on screen; typing `213` in the PAGE field scrolls there and `Set last page` captures it. Prev/Next are deleted. The window is computed from slot rects (`pdfView.pagesInWindow` / `pageAtTop`) instead of an `IntersectionObserver` — a recorded deviation from R-D-01, same observable behaviour, unit-testable. `research/2026-09-12-p6-live-verification.md` §2.
- [ ] R3 — The material is readable on a phone: at 375 with `Fit` the whole page is visible, and the zoom stepper produces sharp text at 1.25x-3x by re-rendering the bitmap. — Evidence required: measured body-text size at Fit vs 1.5x, the user's own phone pass, and a spec assertion that zoom changes the canvas bitmap width (`canvas.width`), not only its CSS width. **P6 (2026-09-12): two of three.** Measured live at 375 on the corpus: Fit — canvas and bitmap 351 px, median text line **8 px** (0.59 px per PDF unit); 1.5x — canvas and bitmap 526 px, median text line **12 px**; the bitmap grows with the CSS width, so text is re-rendered rather than stretched (`canvasBitmapWidth(1.5x) > 1.3 * Fit` is asserted, plus `aria-pressed`). Fit is the default and the whole page width is visible (`scrollWidth === clientWidth`). **Still open: the user's own phone pass** — the person who reported "not readable in mobile screen format" has to confirm the fix, so R3 stays unticked.
- [x] R4 — Opening the viewer stops paying for the whole file every time: the same-origin route serves byte ranges (observed 206s) and a re-open within the TTL is served from the browser cache; the session document cache makes a repeat open ~0.1 s. — Evidence required: the probe's before/after phase table (baseline recorded: first ink 6,761 ms, 22,922,615 B in one GET), a repeat-open byte count, and the live viewer spec green with the route on and with the direct signed-URL fallback. — **P7 done 2026-09-20, with a corrected mechanism.** The same-origin range route was built and measured: it served 206s but did **not** move first ink (6,789 ms vs 6,772 ms), because the corpus is not linearised, and range-only mode was worse (134 requests, first ink 15.8 s). It was reverted also because the production deploy is Vercel static (no nginx), so the route would have broken the deployed viewer. The delivered mechanism is the **session document cache + prefetch**: a repeat open drops from one 22.9 MB GET to 2 requests / 3,433 B and paints in **897 ms** (from 6.8 s), and prefetch-on-intent moves the record fetch from 1,888 ms to 897 ms. First open stays ~6.8 s (recorded as a falsified expectation, not hidden). The viewer spec is 2/2. Transcript: `research/2026-09-20-p7-live-verification.md`.
- [x] R5 — No page silently loses its artwork: every image pdf.js meets decodes (no `Unable to decode image` console warning), including the formats that need the wasm decoders. — Added 2026-09-12 after the hands-on report "not a single image is seen in the doc". **Done 2026-09-12:** the user's material (315 pages) carries 587 JBIG2 images across 260 pages and rendered with zero of them; with `wasmUrl` set, the worker fetches `/study/pdfjs-wasm/jbig2.wasm` and `openjpeg.wasm` (both 200) and pages 104/96/35/26 show 0 decode warnings, with the diagrams visible on screen (screenshot-checked). `e2e/material-viewer-live.spec.ts` now fails on any `Unable to decode image` warning. Transcript: `research/2026-09-12-p6-live-verification.md` §7.

- [x] R6 — The viewer opens on a **complete page**: at 1280x720 and 375x812 the whole page is inside the frame at the default zoom (all four edges — page height ≤ frame height, `scrollWidth === clientWidth`), and `Width` re-fits to today's fit-to-width in one click. — Evidence required: the default-zoom assertion measured live at both viewports, plus the `Width` toggle re-rendering. **Done 2026-09-12 (P8):** measured live — 1280x720, frame 1080x480: `Page` gives a **335x479** px page (scale 0.57498) with all four edges inside, `Width` gives **1080x1545** with the frame scrolling sideways; 375x812, frame 351x492: `Page` gives **343x492**. A 1280 → 375 `setViewportSize` re-fits to 342x484 in the same run, and the poll demands a real raster (an idle canvas is 300x150). Transcript: `research/2026-09-12-p8-live-verification.md` §2.
- [x] R7 — The page is a **real reader**: text is selectable and copyable (the selection round-trips through `window.getSelection()`), a plain drag pans when the page overflows while a drag over text selects, and zoom in / zoom out re-rasterise the bitmap (the canvas bitmap width changes, not only its CSS width) — with no page errors. — Evidence required: the live selection, pan and zoom assertions; `pdfView.test.ts` for the mode arithmetic and the stepper. **Done 2026-09-12 (P8), with the contract corrected in flight:** plain drag **pans** (measured **220 px** both from the page's own margin and from a drag starting **on a glyph**, with `window.getSelection()` empty) and **Shift+drag selects** the run under the pointer (`"Valid for September 2025, December 2025, March 2026 and June"`, with an assertion that the viewer's header copy is *not* swept in); the cursor is a text caret while Shift is held and `grab`/`grabbing` otherwise; zooming re-rasterises the bitmap (`canvas.width` grows, `1.25x` then back to `1x`); no page errors, no decode warnings. `pdfView.test.ts` pins the mode arithmetic and `stepZoom` clamping. Four defects were found and fixed while making these assertions real (the 2% text-layer overflow, the blank re-entering slot, the pan losing the pointer stream, and Shift+drag selecting from the top). Transcript §3/§4.
- [x] R8 — The material detail page **no longer renders extracted content**: no `Extracted content` heading, no `.material-preview-text`, and no request to `/v1/materials/{id}/content` when the page opens, with the rest of the page (status badge, actions, Used by, footer actions) unchanged. — Evidence required: the updated `MaterialDetail.test.tsx` cases and the updated `e2e/material-ingestion-live.spec.ts` assertion; the service route itself stays (R-D-10). **Done 2026-09-12 (P8) as `7bd2f6d`:** the effect, the card and the failure banner are gone with the client, its test, the type and the CSS; `MaterialDetail.test.tsx` 13/13 green with the three preview cases collapsed into one absence assertion; the live ingestion spec asserts the heading and the class are absent. Transcript §1.

## Phase gates (revision)

- [x] P6 viewer revision: `pnpm typecheck` + `pnpm lint` clean; app suite green except the two documented WSL TZ flakes; `e2e/material-viewer-live.spec.ts` 2/2 at 1280 and 375 with `--workers=1`, including the no-overflow and resize assertions; the user's phone pass. — **Done 2026-09-12, except the phone pass (the user's own, and R3's last piece).** `pdfView.test.ts` 18 passed (written before the module: red → green); whole app suite 787 passed / 2 failed (the documented WSL TZ pair in `src/dev/seedTestData.test.ts`, 2/2 green under `--pool=forks`); `pnpm typecheck` + `pnpm lint` clean; `pnpm build` emits both apps with the viewer chunk at 489.8 kB and the worker at 1,265.4 kB, main chunk unchanged; the live spec **2/2** with `--workers=1` (desktop 1280x720 27.1 s, mobile 375x812 11.4 s, no page errors) including `scrollWidth === clientWidth`, the resize 1280 → 375 re-fit, the scroll-reveals-next-slot step and the zoom bitmap assertion; the neighbouring live specs (`assessment-generation-live` 3/3, `material-library-live` 2/2) re-run green. Two defects were found and fixed while making the assertions real, and one was found *in this spec*: the P5-era file-level `test.use({viewport: 375})` meant the "desktop" scenario never ran at 1280 — it does now. Transcript: `research/2026-09-12-p6-live-verification.md`.
- [x] P6 fix image decoders: every image decodes on a material that needs the wasm decoders; no `Unable to decode image` console warning; the assets ship in dev and in `dist/`. — **Done 2026-09-12:** the user's 315-page material (587 JBIG2 images across 260 pages) renders with zero decode warnings on pages 104/96/35/26 and shows its diagrams (screenshot-checked); the worker fetches `/study/pdfjs-wasm/jbig2.wasm` and `openjpeg.wasm` (200); `pnpm --filter app build` emits `dist/pdfjs-wasm/`; `e2e/material-viewer-live.spec.ts` 2/2 with `--workers=1` including the new warning assertion; typecheck + lint clean. Transcript: `research/2026-09-12-p6-live-verification.md` §7.
- [x] P7 streaming: the probe's phase table after the change (expect first ink ~1.0-1.5 s, 50-200 KB per additional page); ranges observed as 206s through the route; repeat-open bytes served from cache; the fallback path still green; the throwaway probe deleted in the phase commit. — **Done 2026-09-20, with the expectation corrected by measurement.** The route was built, served 206s, and was reverted (no first-open win on a non-linearised corpus; would break the Vercel deploy). Shipped instead: the session document cache + prefetch-on-intent - repeat open 22.9 MB/6.8 s -> 3.4 KB/897 ms; `e2e/material-viewer-live.spec.ts` 2/2; throwaway probe deleted. A viewer defect found on the way was fixed: the whole-page fit now uses the document's largest page box, so mixed-size pages no longer overflow. Transcript: `research/2026-09-20-p7-live-verification.md`.
- [x] P8 viewer interaction round 2 + preview removal: `pdfView.test.ts` extended (mode arithmetic + stepper) and written before the code; the live viewer spec 2/2 with `--workers=1` at 1280 and 375 including the complete-page default, the selection round-trip, the drag pan and the zoom bitmap growth; the updated `e2e/material-ingestion-live.spec.ts` green with the preview assertion inverted; `MaterialDetail.test.tsx` green with the preview cases replaced by an absence assertion; app suite green except the two documented WSL TZ flakes; `pnpm typecheck` + `pnpm lint` + `pnpm build` clean; a hands-on pass on the user's own material. — **Done 2026-09-12.** `pdfView.test.ts` 23 passed (extended red → green); `MaterialDetail.test.tsx` 13/13; live spec **2/2** with `--workers=1` (46.6 s, then 49.0 s after a final listener-leak fix: `1280 page fit 335x479 / bitmap 335 in a 1080x480 frame, slots 572, rendered 3`, `1280 width fit 1080x1545`, `shift-drag select "Valid for September 2025, December 2025, March 2026 and June"`, `drag pan 220px from the page margin, 220px over text (selection "")`, `375 refit 342x484 in a 351x492 frame`, `mobile page fit 343x492, line 8px | 1.5x 515x738, line 11px`, no page errors, no decode warnings); whole app suite **781 passed / 2 failed** (the same documented WSL TZ pair in `src/dev/seedTestData.test.ts`); `pnpm typecheck` + `pnpm lint` clean across the workspace; `pnpm build` green (viewer chunk 492.73 kB, worker 1,265.41 kB, css 144.15 kB); the hands-on pass is the user's own and confirmed both gestures mid-phase, with their two corrections driving the final contract (R-D-11). **One thing this gate does not carry:** the scenario no longer generates a question (R-D-13) — the generation path is covered by `e2e/assessment-generation-live.spec.ts` against the managed stack, and it could not run in this session because the sibling worktree owns port 8000 and an ad-hoc service answers `500 {"detail":"auth not configured"}`. Transcript: `research/2026-09-12-p8-live-verification.md`.

## Live evidence (revision)

- 2026-09-12 (P8): **viewer interaction round 2 verified live** — `e2e/material-viewer-live.spec.ts` 2/2 with `--workers=1` (49.0 s, no page errors, no `Unable to decode image` warnings), both arms measured: 1280x720 `page fit: canvas 335x479px / bitmap 335px in a 1080x480px frame, slots 572, rendered 3` and `width fit: canvas 1080x1545px / bitmap 1080px`; `shift-drag select: "Valid for September 2025, December 2025, March 2026 and June"` (an assertion also pins that the viewer's header copy is *not* in the selection); `drag pan: 220px from the page margin, 220px over text (selection "")`; a `setViewportSize` 1280 → 375 re-fits to `342x484 in a 351x492px frame`; 375x812 `page fit: canvas 343x492px / bitmap 343px, line 8px | 1.5x: canvas 515x738px / bitmap 515px, line 11px`. Four defects were found and fixed while making those assertions real (the text-layer overflow on a mixed-page-size corpus, the blank re-entering slot, the pan losing the pointer stream, and Shift+drag selecting from the top of the document); the app build emits the viewer chunk at 492.73 kB. Transcript: `research/2026-09-12-p8-live-verification.md`.
- 2026-09-12 (P8): **the extracted-content preview is gone** — `MaterialDetail.test.tsx` 13/13 (three preview cases collapsed into one absence assertion) and the live ingestion spec asserts the `Extracted content` heading and `.material-preview-text` are absent; committed as `7bd2f6d` (7 files, −328 lines). The service route `GET /v1/materials/{id}/content` stays (R-D-10).
- 2026-09-12 (P8): **the environment had to be corrected before any live number above counts** — the sibling worktree `../study-planner-web-issue-63` holds ports 5173 and 8000, so earlier runs in this session had been exercising *that* checkout's app. This checkout ran on app 5174 / service 8001 with `E2E_APP_URL`, and the ad-hoc service needs `CORS_ORIGINS` to include the new origin or every request fails its preflight with 400. Full account: `research/2026-09-12-p8-live-verification.md` §6.

- 2026-09-12: **Phase-7 baseline measured** with `e2e/tmp-viewer-timings.spec.ts` (throwaway, untracked; CDP Network timeline, real corpus material, desktop 1280): route 26 ms, material record 1,884 ms, signed-URL POST 278 ms, body GET 22,922,615 B in 4,283 ms, parse 6,749 ms, first ink 6,761 ms. Response headers observed on the body: `accept-ranges: bytes`, `content-length: 22919258`, `cf-cache-status: MISS`, `etag`, and **no `Cache-Control`** — server-side range support with CORS-hidden headers, exactly as P5 recorded.
- 2026-09-12: **drift recorded** — HEAD `ff3ab07` (unpushed) adds the derived `view-<contentVersion>.pdf` copy; the repaired copy is larger than its source (2,998,611 B → 3,420,085 B). The viewer therefore loads a derived object for some materials, which Phase 7's route must serve first.
- 2026-09-12 (P6): **viewer revision verified live** — `e2e/material-viewer-live.spec.ts` 2/2 with `--workers=1`: desktop 1280x720 27.1 s and mobile 375x812 11.4 s, both with no page errors. Measured: 1280 Fit — canvas/bitmap 1080 px in a 1080 px frame, `scrollWidth === clientWidth`, `slots 572, rendered 1`; 375 Fit — canvas/bitmap 351 px in a 351 px frame, median text line 8 px; 375 1.5x — canvas/bitmap 526 px, median text line 12 px; a `setViewportSize` 1280 → 375 re-fits the canvas to 351 px (the assertion that fails on `ff3ab07`). Scrolling to page 2's slot moves the readout to `2` and rasterises it; the Chapter 5 jump lands on pdf 156 with ink. Details, the three defects found on the way (blank-canvas cancellation, the near-empty page 2, the never-desktop P5 scenario) and the deliberate simplifications: `research/2026-09-12-p6-live-verification.md`.
- 2026-09-12 (P6): **found the P5 "desktop" run was not at desktop.** A file-level `test.use({ viewport: 375 })` applies to every test in the file, so both of the P5 scenarios ran at 375x812 and the "2/2 at 1280 and 375" line in `research/2026-09-11-p5-live-verification.md` was 375 twice. The viewport is now scoped to the mobile `describe`; the desktop scenario asserts a fact that only holds at 1280 (`[data-page="2"] .pdf-canvas` count 0).
- 2026-09-12 (P6): **unit tests** — `apps/app/src/materials/pdfView.test.ts` 18 passed, written before `pdfView.ts` existed (18 red, then green); whole app suite 787 passed / 2 failed (the documented WSL TZ pair, green under `--pool=forks`); `pnpm typecheck` + `pnpm lint` clean; `pnpm build` green (viewer chunk 489.8 kB, worker 1,265.4 kB, main chunk unchanged); neighbouring live specs re-run green (`assessment-generation-live` 3/3, `material-library-live` 2/2).
