# Scratchpad – scoped-question-generation · session 2026-09-11
_state.md: active/scoped-question-generation/state.md · Updated: 2026-09-11T11:05_

## Now / Next
- Doing: **P4 is DONE and verified end to end** — the checklist below was run in full this session (mapper test, typecheck/lint/suites, worker+runtime restart, live AC1/AC5 gate, route matrix, browser pass, records). Nothing is left open in the worktree.
- Next: **P5 (pdf.js viewer)** — `pdfjs-dist` with `workerSrc`, `createSignedUrl` on `MaterialStorageLike`, canvas viewer, chapter jump from `materials.outline`, range select → "Assess these pages", route `/materials/:materialId/view`; it closes AC4, then the wayfinder exit.
- Blocked: none.

### P4 checklist (CLOSED 2026-09-11 — all 7 steps run and verified; detail in the session log below, `state.md`, and `research/2026-09-11-p4-live-verification.md`)

Everything listed under "already on disk" exists and its unit tests pass. The remaining work is steps 1–7.

**Already on disk (uncommitted, 16 modified files + 1 new fixture; do not rewrite):**
- `contracts/phase2/openapi.yaml` — `AssessmentRecipe.scope` + new `AssessmentScope` schema (`pageStart`/`pageEnd` required, `sectionLabel` optional, `additionalProperties:false`).
- `contracts/phase2/fixtures/assessment-recipe-scoped.json` (new) + `fixtures/README.md` row + 2 contract tests in `contracts/phase2/tests/test_contracts.py` → **22 passed**.
- `app/routers/assessments.py` — `_validate_scope` (shape: integers ≥1, start ≤ end, non-empty label, unknown keys rejected) and `_scope_range_failure` (409 when the material has no `page_count`, or `pageEnd > page_count`).
- `app/generation/models.py` — `AssessmentScope` (`from_recipe`, `widened`, `to_dict`) + `GenerationBlueprint.scope`.
- `app/generation/context.py` — `build_context(material_id, skill_tags, scope, supabase_url, service_key, client)`: steer = `sectionLabel` + tags; page bounds into the RPC; **the empty-steer branch now spreads** (`_spread_context`: id/ordinal listing → evenly spaced picks → text fetch) instead of refusing (D-08's "no query" branch finally landed).
- `app/generation/worker.py` — scope off the recipe; empty context → non-retryable `validation_failed`; `_widen_thin_context` (D-06: pad 5 pages/round, 2 rounds, `scope_widened` warning kept through repair); **`ContextBuilder` is now a required 3-arg callable — no default**, so the production builder cannot be silently dropped again.
- `app/worker_main.py` — the adapter passes `scope` through.
- Tests: `tests/test_generation_context.py` (+6), `tests/test_generation_worker.py` (+4), `tests/test_assessments_api.py` (+6) → **518 passed** for `tests/` minus the 7 known pre-existing failures.
- App: `src/assessments/types.ts` (`AssessmentScope` on the recipe), `src/materials/types.ts` (`OutlineEntry`/`MaterialOutline`/`outlineFromRow` + `outline`/`pageCount`/`pageOffset` on `MaterialRecord`), `src/materials/materialClient.ts` (maps them), `src/pages/assessments/AssessmentConfig.tsx` (chapter chips → range = own page to next chapter's page − 1, two number inputs, "Whole material" reset, tag box and the `['core']` default **deleted**), `AssessmentConfig.test.tsx` → **70 passed** with `src/materials`.

1. **Finish the client-mapper test** (interrupted mid-edit): add a `materialClient.test.ts` case asserting `outline`/`pageCount`/`pageOffset` map from the row, and `outlineFromRow` returning null for junk. Its `row()` helper does not yet carry the three new keys.
2. **Typecheck + lint + full app suite**: `pnpm typecheck`, `pnpm lint`, `pnpm --filter app test`. The Python side is done (`uv run pytest services/intelligence/tests -q` → expect 533+18/7 with the same 7); the app suite has NOT been run whole since the picker rewrite.
3. **Restart the detached ingestion worker** (rule 53 — no hot reload; it currently runs the *P3* code, and P4 changed `generation/context.py` + `worker.py`). `bash -c 'pkill -f "dev-ingestion-worker[.]mjs|app[.]worker_main"; sleep 2; setsid nohup bash scripts/run-detached-ingestion-worker.sh >/dev/null 2>&1 &'`. Also restart the managed runtime so uvicorn picks up the router change (`./full-app restart full`), and confirm the sidecar `embedder-embedder-1` is up on :8200 (it is; stop it when idle).
4. **Live API gate (AC1 grounding + AC5 citations-in-scope).** Password grant (strip the backticks in `.work/specs/test-login-cred.txt`), then `POST /v1/assessments/generate` with `recipe.scope = {pageStart: 156, pageEnd: 213, sectionLabel: "Chapter 5 Budgeting and control"}` on the APM material `80c8b138-b544-4095-8dc0-1c390ac70da2`; poll `/v1/assessments/{id}`; **assert every citation's chunk page lies inside 156–213** by joining `content_chunks` on the citation `chunkId` (service role — a user token returns 0 rows for `content_chunks`). Compare against the P2-measured truth: Chapter 5 body is pdf 156–213 and the unscoped chapter steer returns ordinals 203/271/204/269/202 with pages (157,158)(203,203)(158,158)(202,202)(156,157) — every one of those is inside the range, so a correctly scoped run should look like this list minus anything outside it.
5. **Route matrix live-checks** (unit coverage exists; the live DB is what proves the RPC accepts the two new params): a 3-page range (expect `scope_widened` in `assessment.warnings`), a range over 572 (409), a page-only scope with no tags (must spread, not embed an empty query), and a `url` material scoped (409 — no page numbering).
6. **Browser click-through** at 1280 and 375: chapter chip fills both page inputs, "Generate question" lands on the detail page, question reads as material content. Leave the runtime + worker + sidecar up for the user's hands-on pass.
7. **Records + commit + push**: P4 notes in `plan/PLAN.md` (mark ⬜ → ✅ with the measured numbers), VERIFICATION AC1/AC4/AC5 notes + the P4 gate line + a live-evidence line, `state.md` (Current/Done/Files/Pitfalls/Decisions/Open), STATUS row, evidence file `research/2026-09-11-p4-live-verification.md`, then one commit (`feat(generation,app): #62 P4 - scoped generation`) and push. `graphify update .` after.

**Decisions taken during P4 that the records must capture:**
- Scope pages are **PDF page numbers everywhere** (recipe, chunk columns, viewer); the printed→PDF conversion happens once at ingestion (P3), never again.
- **No new migration for P4**: the scope rides `assessments.recipe` (jsonb, echoed by the read route), so retry/restore round-trip it with no plumbing — this is the D-05 bet paying off.
- The unscoped-thin case does **not** widen (nothing to widen into); only a chosen range widens and warns.
- `ContextBuilder` lost its default deliberately (a required arg beats a silent fallback to the wrong arity).
- `AssessmentConfig.tsx` no longer sends `skillTags` at all. The tag box is gone; the steer is `scope.sectionLabel`. This closes the P1 wart, and it means an unscoped browser request now relies on the spread branch.

## Session log
- 06:2x RESUMED  P1 closed and pushed; scratchpad/state agree with the worktree, no disk-vs-record drift this time.
- 06:3x DECIDED `PypdfTextReader.extract` now returns `list[str]` (one entry per page) instead of the joined string: the page is only knowable at the reader, and the file branch rebuilds `"\n\n".join(pages)` so `ExtractedContent.text` stays byte-identical. Two tiny test-double edits beat a second read of a 23 MB PDF.
- 06:3x EDIT   `models.py` – `TextSegment.page`, `ContentChunk.page_start/page_end` (both NULL-able) – verified by tests. `to_schema_dict` deliberately untouched: pages are retrieval metadata, not part of the chunk contract (its schema test still passes).
- 06:4x EDIT   `extractors.py` – `_page_segments()` builds one page-tagged paragraph per blank-line block from per-page cleaning, alongside the unchanged single-clean `text` – verified.
- 06:4x EDIT   `chunking.py` – `_page_bounds()`; `flush()` stamps `page_start/page_end`, the overlap tail carries `page_end`, the recursive re-split carries `segment.page` – verified.
- 06:5x EDIT   `repository.py` + `ingestion_doubles.py` – chunk rows carry the two page columns; `list_chunks` selects them.
- 06:5x DECIDED the resume-on-retry reuse key (`worker.py:391-397`) now includes the page bounds: text-only reuse would re-publish NULL-page rows after any retry, which is the silent path to an unscopable material. Widened the comparison rather than adding a separate guard.
- 06:5x EDIT   `tests/test_extractors.py` (+2 page tests, FakePdfReader takes pages), `tests/test_chunking.py` (+4 propagation tests), `tests/test_repository.py` (page columns in the row assertion) – 126 focused tests pass.
- 06:5x EDIT   `apps/app/supabase/migrations/029_page_scoped_chunks.sql` (new) – two chunk columns; the 016 hybrid body copied verbatim; two page params (DEFAULT NULL) and two overlapped-range predicates in both CTEs; `page_start/page_end` in RETURNS TABLE. The 4-arg overload is DROPPED first (CREATE OR REPLACE cannot change params/returns) and the 6-arg signature's defaults keep every existing 4-named-arg caller resolving to it.
- 07:0x FOUND  offline fixture check (`/tmp/p2_check.py`, throwaway, on the real 572-page PDF): fulltext 963,136 chars (pre-P2 number unchanged), paragraphs 1257 both paths **identical**, chunks 754 vs 754 with **identical texts**, 754/754 carry pages inside 1..572, monotone, 372 straddle a boundary (page_start != page_end), first chunk (1,2) last (571,572). The two-column decision is now data-backed.
- 07:0x EDIT   `generation/models.py` (`RetrievedChunk.page_start/page_end`) + `generation/context.py` (maps them, `_optional_int`) – the RPC returns them so P4's citation-in-scope check (AC5) needs no second migration.
- 07:1x DECIDED `GenerationBlueprint.scope` deferred to P4, not P2 as the plan's file index says: no consumer exists until the scope contract does, and an unused field is dead code. Recorded in the P2 plan notes.
- 07:1x START sidecar up (`services/embedder/docker-compose.yml up -d`); `/health` reports cuda_available true, RX 9070 XT, both models loaded.
- 07:2x DONE   migration 029 dry-run listed exactly 029, then the real push applied it. Live signatures: legacy 3-arg untouched + the new 6-arg returning page columns; the 4-arg overload is gone.
- 07:3x DONE   live re-ingest of the APM material: chunk rows deleted (D-07), retry RPC 200 (job 892af4f2), detached worker restarted first (rule 53) -> extracting -> embedding -> ready in ~45 s.
- 07:3x FOUND  `content_chunks` returns 0 rows to a *user* token (service-owned table, no client policy) — the watcher's chunk counters read 0 while the table was full. Verify chunk state with SQL/service role, never with the client key.
- 07:3x DONE   live SQL: `total=754 paged=754 min_p=1 max_p=572 straddling=372 out_of_range=0 embedded=754` — every chunk paged, in range, re-embedded.
- 07:3x DONE   live RPC probes: unscoped chapter steer -> ordinals 203/271/204/269/202 (P1's exact number, so the rebuilt function is a regression-free replacement); range 124..170 -> 5 hits all in range with page 203 (ordinal 271) excluded; single-page ranges keep straddling chunks (158..158 -> (157,158) and (158,159); 1..3 -> (3,4)); pageless url material scoped -> 0 hits, unscoped -> 1.
- 07:4x FOUND  the whole-service suite's 7 failures are pre-existing: checked in a clean `git worktree` at `c30cd43`, the identical 7 test ids fail (2 probe-test import errors, 5 calibration golden-fixture drifts). 515 passed = P1's 509 + the 6 new P2 tests.
- 07:4x DONE   records: PLAN Phase 2 status + notes (incl. the 029-vs-030 correction for P3), VERIFICATION AC2 note + P2 gate ticked + live-evidence line, state.md (current/Done/Files/Pitfalls/D-03/Open), STATUS row. Evidence file `research/2026-09-11-p2-live-verification.md`.
- 07:4x FOUND  a stale `.git/index.lock` (dated 06:10, no git process alive) blocked the first commit attempt; removed after checking `pgrep -af git` was empty (the same pitfall P1 recorded).
- 07:4x DONE   committed `2ea2d77` (18 files) and pushed to `phase2/issue-62-scoped-question-generation` (c30cd43..2ea2d77); `graphify update .` re-run.
- 10:0x RESUMED  P3+P4 requested. Worktree check: `git status --short` shows only `graphify-out/` untracked; scratchpad and state agree with disk (P2 committed at `e02fa52` docs / `2ea2d77` code).
- 10:1x FOUND  the offset must come from the running headers, and the *strict* reading is the only safe one: a standalone number (not part of `3.2`/`P.5`/`20X9`) on one of the first two lines, ≤6 words, 1..page_count, and a page votes only when all its candidates agree. Measured on the 572-page fixture: 282 of 284 voting pages agree on **-33** (99.3%); the loose rule (all head/footer numbers) reaches only 64.8%, too close to a threshold to trust. Tail lines were dropped: body prose carries numbers and cost 34 votes.
- 10:2x DECIDED  entries persist as **PDF page numbers** (`printed - page_offset`), the numbering chunk pages and the viewer both use; and `build_outline` returns None when no offset is derivable, because a chapter range off by the offset would silently scope the right chapter to the wrong chunks (the typed page range still works, since `page_count` is always the true PDF count).
- 10:2x EDIT   `app/ingestion/outline.py` (new, from `scripts/pdf_outline.py`: deterministic core verbatim + `derive_page_offset` + `build_outline` + the OpenRouter fallback wired to the app adapter) – 14 unit tests green.
- 10:3x EDIT   `models.py` (`ExtractedContent.pages`/`bookmarks`), `extractors.py` (`PypdfTextReader.bookmarks`, optional-capability adapter so a reader without it is not an error, pages/bookmarks on the file branch), `worker.py` (outline computed right after extraction, persisted with the chunking transition), `repository.py` + `ingestion_doubles.py` (three new columns) – focused suites green.
- 10:3x EDIT   `apps/app/supabase/migrations/030_material_outline.sql` (new): `outline JSONB`, `page_count INT` (≥1), `page_offset INT`, plus 008's server-owned guard re-created with the three columns added, so a client token cannot forge a derived outline.
- 10:3x FOUND  the fake repo broke on `Material(**row)` once the outline columns were written: the double now pops them, matching the production row mapper, which ignores columns it does not map.
- 10:3x EDIT   `tests/test_outline.py` (new, 14 tests: three real contents layouts, permissive gating, offset rules, bookmarks, refused outline, LLM gating + failure, implausible entries), `tests/test_extractors.py` +2, `tests/test_ingestion_worker.py` +2, `scripts/pdf_outline.py` rewritten as a thin CLI over the module (its `--self-check` assertions are the unit test now) – focused 123 passed.
- 10:4x DONE   migration 030 dry-run listed exactly 030, then the real push applied it.
- 10:4x DONE   worker restarted (pids 35066/35074/35077), chunk rows deleted (D-07), retry RPC 200 (job `e6be81a0-7401-4dfd-9dda-8f985125e6a5`); the `chunking` transition already carries `pages=572 offset=-33 entries=16`, `ready` in ~80 s with 754 re-embeddings.
- 10:4x DONE   stored `materials.outline` asserted against the offline parse of the same fixture: 16/16 entries identical, same offset and page count, source `contents` (so the DeepSeek fallback was never called). Evidence: `research/2026-09-11-p3-live-verification.md`.
- 10:4x DONE   whole service 533 passed / 7 failed — the identical 7 pre-existing failures (2 probe-test imports, 5 calibration golden drifts).
- 10:4x DONE   P3 records written (PLAN P3 ✅ + notes, VERIFICATION AC3 note + P3 gate ticked + live-evidence line, STATUS row, evidence file `research/2026-09-11-p3-live-verification.md`); committed **`13f25b9`** (20 files). Not pushed yet — P4 will push both together, or push now if the branch is wanted clean.
- 10:5x START  P4. Contract first: `AssessmentRecipe.scope` + `AssessmentScope` in `openapi.yaml`, the `assessment-recipe-scoped.json` fixture, 2 contract tests (22 passed) — `additionalProperties:false` on both, so the free-text-era request shape stays valid while an unknown scope key is rejected.
- 10:5x DECIDED  the scope shape is `{pageStart, pageEnd, sectionLabel?}` in **PDF page numbers**, validated against `materials.page_count`; a chapter chip resolves to its own page and the next chapter's page − 1 (the last chapter runs to `page_count`). No new migration: it rides `assessments.recipe`, which the read route already echoes, so retry/restore round-trip it for free (D-05's bet).
- 10:5x EDIT   `routers/assessments.py` — `_validate_scope` (integers ≥1, start ≤ end, non-empty label, unknown keys) + `_scope_range_failure` (409 when the material has no page numbering or `pageEnd > page_count`); 6 new route tests, whole-file suite green.
- 10:6x EDIT   `generation/models.py` (`AssessmentScope` with `from_recipe`/`widened`/`to_dict`, `GenerationBlueprint.scope`) and `generation/context.py` — the steer is `sectionLabel` then the tags, the page bounds ride into `match_content_chunks` as `p_page_start`/`p_page_end`, and **the no-steer case now spreads** (`_spread_context`: listing → evenly spaced picks → text fetch, bounded by the scope) instead of refusing. This is D-08's long-deferred "no query → spread inside the range" branch; the refuse-and-fail path it replaces is gone from the unscoped case, but an empty *material* still fails at the worker as non-retryable `validation_failed`.
- 10:6x EDIT   `generation/worker.py` — scope off the recipe into the blueprint; empty context → non-retryable failure; `_widen_thin_context` implements D-06 (pad 5 pages, max 2 rounds, `scope_widened` warning; the warning survives a repair round because it is re-prepended); `ContextBuilder` is now **required** and 3-arg, and `run_once`'s default builder was removed so a wrong arity fails loudly at construction instead of silently at generation.
- 10:7x EDIT   `app/worker_main.py` passes the scope through; `tests/test_generation_context.py` rewritten around a queueing FakeRest (+6 tests: scope bounds in the RPC payload, label-then-tags steer, spread evenness, spread bounded by the scope, spread failure retryable); `tests/test_generation_worker.py` +4 (scope reaches the builder, thin context widens and warns, unscoped thin context does not widen, zero-chunk scope fails the assessment). `tests/` minus the 7 known failures = **518 passed**.
- 10:8x EDIT   app side: `AssessmentScope` on `AssessmentRecipe`; `OutlineEntry`/`MaterialOutline`/`outlineFromRow` + `outline`/`pageCount`/`pageOffset` on `MaterialRecord`; `materialClient` maps them; `AssessmentConfig.tsx` rewritten with chapter chips, the two page inputs (client-side validation mirrors the server's), a "Whole material" reset, and **no skill-tags box** — which also deletes the `skillTags: ['core']` default (the P1 wart).
- 10:9x FOUND  rewriting `AssessmentConfig.tsx` silently dropped the retryable-error Retry button; the existing test caught it, and it is restored. Lesson: the picker's banner block is load-bearing behaviour, not decoration.
- 11:0x DONE   `pnpm --filter app exec vitest run src/pages/assessments/AssessmentConfig.test.tsx src/materials` → **70 passed** (10 config tests: chapter range + label, typed range with no label, inverted/out-of-range refusal, page-less material, plus the pre-existing quota/retry/double-submit cases).
- 11:0x NEXT   P4 checklist above: finish the `materialClient.test.ts` outline-mapping case, typecheck/lint/the whole app suite, restart worker + runtime, live AC1/AC5 gate, records, commit, push.
- 12:2x RESUMED  P4 verification session. Disk-vs-record check first: everything the scratchpad listed as "already on disk" is present and only step 1 (the mapper test) was genuinely undone; no drift beyond that.
- 12:3x EDIT   `apps/app/src/materials/materialClient.test.ts` – `row()` now carries `outline`/`page_count`/`page_offset`, plus one case asserting the mapping and a junk outline reading as none – 26 passed.
- 12:3x DONE   `pnpm typecheck` clean (5 projects) and `pnpm lint` clean.
- 12:3x DONE   whole app suite `pnpm --filter app test` -> **762 passed / 2 failed**; the 2 are the documented WSL TZ pair in `src/dev/seedTestData.test.ts` and re-run `--pool=forks` -> **2/2 green**, so not P4 fallout.
- 12:4x FOUND  the scratchpad's `pkill` recipe self-terminates the tool shell: `pkill -f "dev-ingestion-worker[.]mjs|app[.]worker_main"` matches the shell's own command line (rule 53). The worker did restart (new pids), so the effect was right and the mechanism was lucky — kill by explicit pid instead.
- 12:4x DONE   detached ingestion worker restarted (new pids, P4 generation code) and `./full-app restart full` so uvicorn picks up the router change; sidecar `embedder-embedder-1` healthy on :8200.
- 12:4x DONE   live AC1/AC5 gate (`/tmp/p4_live_gate.py`): Chapter 5 scope (pdf 156..213, label) -> `202` -> `ready`, warnings `[]`, recipe echoed verbatim, citations joined to `content_chunks` under the service role at pages (158,158) and (202,202) — **both inside the range**.
- 12:4x FOUND  the checklist's thin-range expectation was wrong: 156..158 returns exactly 5 chunks (= `CONTEXT_TOP_K`) so it does **not** widen; a single page (156..156) returns 4 and widens to 151..161 with `scope_widened`, citing page 161 inside the widened range. Recorded as a correction, not a defect.
- 12:4x DONE   route matrix live: over-572 -> 409, inverted -> 400, pageless `url` material scoped -> 409, page-only scope with no label spreads and cites page (177,178) inside the range.
- 12:5x EDIT   `e2e/assessment-generation-live.spec.ts` – fixed two stale assertions (the detail page renders `Difficulty 3` + a `Citations` heading, not `Difficulty band`, and the question is no longer inside `.card-large`), added a scoped chapter-chip scenario and mobile 375 chip-fill/reset assertions, extracted the retry loop into `waitForQuestion`. The spec had been failing independently of P4 since the #40/#41 rework.
- 12:5x DONE   browser pass **3/3** with `--workers=1`: unscoped generation at 1280, chapter chip -> 156/213 -> generate -> citations at 1280, chip fill/reset at 375.
- 12:5x DECIDED  no new migration for P4 — the scope rides `assessments.recipe` (jsonb) and the read route echoes it; six live runs round-tripped it verbatim, so D-05's bet is confirmed rather than assumed.
- 12:6x DONE   records: PLAN P4 notes + status, VERIFICATION (AC1/AC2/AC3/AC5/AC6 ticked, AC4 keeps the viewer half for P5, P4 gate ticked, live-evidence lines), state.md (Current/Done/Files/Pitfalls/Decisions/Open), STATUS row, evidence file `research/2026-09-11-p4-live-verification.md`.
- 12:6x NEXT   commit `feat(generation,app): #62 P4 - scoped generation` + push, then `graphify update .`.
