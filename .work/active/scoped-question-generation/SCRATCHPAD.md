# Scratchpad – scoped-question-generation · session 2026-09-11 (P5)
_state.md: active/scoped-question-generation/state.md · Updated: 2026-09-11T14:40_

## Now / Next
- Doing: **P5 (pdf.js viewer) is implemented and verified** — `pdfjs-dist@6.3.289`, signed-URL load, `PdfViewer.tsx` on `/materials/:materialId/view`, range handoff to the config. Live 2/2 at 1280/375, AC4 ticked, **all six ACs now closed**. Nothing is left open in the worktree. Records written (PLAN P5 notes + status, VERIFICATION AC4 + P5 gate + evidence lines, state.md, STATUS row, `research/2026-09-11-p5-live-verification.md`).
- Next: commit `feat(app): #62 P5 - pdf.js viewer` + push, then `graphify update .` — then the wayfinder exit (push branch, resolution comment, close #62, map #4 line, STATUS flip to Done, archive).
- Blocked: none.
- Carried finding (recorded, deliberately not fixed): every viewer open downloads the whole 22.9 MB because Supabase Storage does not expose `Accept-Ranges` over CORS, so pdf.js never enables range requests; the explicit `PDFDataRangeTransport` fix measured **worse** (123 requests / 30.8 MB / 12.9 s) and was reverted. Options in the P5 transcript §5.

## Session log
- 13:0x RESUMED  P5 requested. Disk-vs-record check: P4 committed at `f23ebf0`, only `graphify-out/` untracked, scratchpad/state matched the worktree. P5 = the last phase in the plan; P4's checklist closed, its session content already distilled into state.md → scratchpad reset for this session.
- 13:0x READ  rules 10/12/15/16/50, the live specs, `AssessmentConfig`/`MaterialDetail`/`materialClient`/`App.tsx`; live stack already up (app 5173, intelligence 8000, sidecar healthy).
- 13:1x FOUND  the raw PDF is still in private storage and the path is derivable from the record alone (`<ownerId>/<materialId>/<source>`, `source` = the uploaded file name) — so the signed URL needs no extra auth round trip. Service-role probe: 22,919,258 bytes, sign 200, range GET 206, `%PDF-`.
- 13:1x EDIT   `materialClient.ts` – `MaterialStorageLike.createSignedUrl` + `MaterialClient.getMaterialFileUrl(material)` (600 s TTL) – verified by 4 new unit cases (30 passed).
- 13:2x EDIT   `PdfViewer.tsx` (new) – canvas render fit-to-width with a DPR transform, Previous/Next + page input + chapter `<select>` from `materials.outline`, `Set first/last page` → `Assess these pages` → `/materials/:id/assessments/new?from=&to=`; a `cancelled`-guarded load effect that destroys its task (StrictMode double-invokes).
- 13:2x DECIDED  the route is **lazily imported** so pdf.js (488 kB) + worker (1.27 MB) stay out of the main bundle and out of the jsdom test graph; and **no client-side `pdf.getOutline()`** — ingestion already owns the cascade and a material without an outline has no `page_count` (both are written together, `worker.py:420-429`), so the client list would be unreachable.
- 13:2x EDIT   `App.tsx` (lazy route + Suspense), `MaterialDetail.tsx` (`Open in viewer` for `kind === 'file'`), `AssessmentConfig.tsx` (seeds the range from `?from=&to=`, clears it when `page_count` is null, links to the viewer), `materials.css` (`.pdf-*`), `fakeMaterialClient.ts` – verified by 2 new config cases (12 passed).
- 13:2x DECIDED  a viewer handoff is a **typed range** (numbers, no `sectionLabel`): only a chapter chip carries a label. The config must clear a seeded range when the material has no `page_count`, else it would ship an unvalidated scope past inputs that are not rendered.
- 13:2x FOUND  `pdfjs-dist@6.3.289` types: `PDFPageProxy.render` takes `{ canvas, viewport }` (`canvasContext` deprecated), `PDFDataRangeTransport` is exported from the main entry, `PDFDocumentProxy` has no `destroy` (the loading task owns the document).
- 13:3x DONE   `pnpm typecheck` clean (5 projects), `pnpm lint` clean, focused suites green, `pnpm build` emits `PdfViewer-*.js` (488 kB) + `pdf.worker.min-*.mjs` (1.27 MB) as separate chunks with the main chunk unchanged.
- 13:3x DONE   whole app suite `pnpm --filter app test` → **768 passed / 2 failed** (the documented WSL TZ pair in `src/dev/seedTestData.test.ts`; `--pool=forks` → 2/2 green).
- 13:4x EDIT   `e2e/material-viewer-live.spec.ts` (new) – 1280 full flow + 375 handoff, both gating on **real canvas ink** (a missing `workerSrc` paints blank with only a console warning).
- 13:4x FOUND  the first spec run failed on a strict-mode collision: my `aria-label={`Page ${page}`}` on the canvas matched `getByLabel('Page')` alongside the page input. Renamed the canvas label to `Rendered page N` and used `getByRole('spinbutton', { name: 'Page' })`.
- 13:4x DONE   live viewer **2/2** with `--workers=1` — render with ink, chapter jump to pdf 156, `Assess pages 156–213` → config → grounded question with citations. Measured at 1280: toolbar 2.58 s, first paint 6.32 s, canvas 1080×1545 CSS px, 1,668,600 dark pixels on page 1, jump repaint 67,051, zero console errors.
- 13:5x DONE   authenticated API gate for the same label-less scope (`156..213`): `202` → `ready`, warnings `[]`, recipe verbatim, citation ordinal 269 on pages (202,202) — inside the range. Question: zero-based budgeting.
- 13:5x FOUND  **the plan's range-fetch rationale does not hold.** pdf.js sends one plain GET with no `Range` header and pulls all 22,919,258 bytes per open (4.6-7.5 s, no `Cache-Control`, so every open repeats). From the browser the same signed URL answers `Range: bytes=0-65535` with **206/65,536**, but the exposed header set is only `cache-control, content-length, content-type, expires, last-modified` — `Accept-Ranges` is not readable, so `validateRangeRequestCapabilities` never enables ranges.
- 13:5x DECIDED  implement the explicit fix (`fetchPdfLength` + `fetchPdfRange` + `PDFDataRangeTransport`) rather than only recording the gap, because the server side was proven to work.
- 13:6x FOUND  the explicit transport is **worse**: 123 requests, 30,807,072 bytes, 12,900 ms, and it keeps fetching while idle. pdf.js walks the file backwards from the trailer, and v6's `rangeTransport` branch (`build/pdf.mjs:15537`) passes only `disableRange`/`disableStream` into `PDFDataTransportStream` — `disableAutoFetch` never reaches it, so there is no supported off-switch.
- 13:6x EDIT   reverted the transport and deleted `pdfRange.ts`/`pdfRange.test.ts` rather than ship a slower path; the load path is back to `getDocument({ url })` with the measurements recorded in the code comment and the transcript §5.
- 13:6x DONE   live viewer **2/2 green again** after the revert (21.2 s / 11.1 s).
- 13:7x DONE   neighbouring live paths re-run after the config/detail changes: `e2e/assessment-generation-live.spec.ts` + `e2e/material-library-live.spec.ts`.
- 13:7x DONE   records: PLAN P5 ✅ + notes (incl. the range-fetch correction and the dropped `getOutline()`), VERIFICATION AC4 + P5 gate + 5 live-evidence lines, state.md (Current/Done/Files/Pitfalls/P5 decisions/Open), STATUS row, evidence file `research/2026-09-11-p5-live-verification.md`.
- 13:7x NEXT   commit `feat(app): #62 P5 - pdf.js viewer` + push, `graphify update .`, then the wayfinder exit.
