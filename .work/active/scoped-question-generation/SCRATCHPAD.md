# Scratchpad – scoped-question-generation · session 2026-09-12 (P8 implemented and verified; the P8-planning and P6 sessions follow)
_state.md: active/scoped-question-generation/state.md · Updated: 2026-09-12_

## Now / Next
- Doing: **P8 is complete and verified** — the extracted-content card is deleted (`7bd2f6d`) and the viewer opens on a whole A4 page with zoom, plain-drag panning, Shift+drag selection and a pdf.js text layer. Live spec **2/2** at 1280x720 and 375x812, the user's hands-on pass confirmed both behaviours, records written.
- Next: **commit P8 + records and push** (the gates are recorded in `plan/VERIFICATION.md`), then Phase 7 (streaming, R4) in a fresh session: same-origin range route, prefetch on intent, session document cache, then the before/after probe table and `research/2026-09-12-p7-live-verification.md`.
- Blocked: nothing for P7. R3's phone pass is still the one item needing the user. Note the environment: the sibling worktree `../study-planner-web-issue-63` owns 5173/8000, so this checkout runs on 5174/8001 with `E2E_APP_URL`.

## Session log
- 03:40 RESUMED  P5 session ledger read in full; every open item already distilled into `state.md`, so this session took P6, the first phase of the 2026-09-12 revision. Worktree: `ff3ab07` at the tip, `.work` records for the revision uncommitted, `e2e/tmp-viewer-timings.spec.ts` untracked (P7's baseline probe, kept).
- 03:42 READ  the plan's P6 items + R-D-01..R-D-06, rules 10/16/53, `PdfViewer.tsx`, `materials.css` (`.pdf-*`), `e2e/material-viewer-live.spec.ts`, the P7 timing probe, `e2e/playwright.config.ts` (app project = Desktop Chrome 1280x720). Runtime already healthy (app 5173, intelligence 8000, detached worker, sidecar).
- 03:45 DECIDED  split the viewer's geometry out of the component into `pdfView.ts` as pure functions and write the tests first: the viewer is lazily imported so pdf.js never enters the jsdom graph, which would otherwise leave every layout rule untested.
- 03:47 EDIT   `apps/app/src/materials/pdfView.test.ts` (new, 18 cases) – RED (`Failed to resolve import "./pdfView"`), then `pdfView.ts` – GREEN 18/18. One test was wrong on first run (`pageAtTop` case had page 1 still visible at the top edge), fixed in the test, not the code.
- 03:52 EDIT   `PdfViewer.tsx` rewritten – 572 `.pdf-page` slots with `aspect-ratio` from page 1's viewport, `ResizeObserver` → `containerWidth` (single width source), rAF-throttled scroll handler computing the rendered window and the topmost page, jump-only PAGE/CHAPTER controls (`frame.scrollTop += slot.top - frame.top`), Fit + `1.25/1.5/2/3` zoom buttons, `MAX_RASTER_SCALE = 2.5`.
- 03:55 EDIT   `materials.css` – `.pdf-frame` is the single scroll container (`calc(100dvh - 15rem)`, `overscroll-behavior: contain`), `.pdf-page` slots, `.pdf-zoom`, and the `.pdf-fit`-scoped `max-width: 100%` guard (unscoped it would defeat zoom).
- 03:58 FOUND  first live run failed on the windowing assertion: `[data-page="2"] .pdf-canvas` existed at what I thought was 1280. Instrumenting the spec showed `viewport: [375,812]` — a file-level `test.use({viewport})` applies to every test in the file, so **P5's "desktop" scenario had never run at 1280** (its evidence line "2/2 at 1280 and 375" was 375 twice). Moved the viewport inside the mobile `describe` and made the desktop scenario assert a 1280-only fact.
- 04:00 FOUND  then page 2's ink poll timed out at 0. Dumping the canvases showed the bitmap width set and `ink 0`: a `RenderTask` cancelled after `canvas.width` was assigned never paints again, and the "skip when `canvas.width` matches" shortcut made that permanent. Renders now go through one serial promise chain, are never cancelled mid-flight, and are marked drawn only on resolve.
- 04:05 FOUND  the next timeout was honest: page 2 of the corpus is a near-empty divider (33,886 dark pixels in the whole bitmap, ~76 in its visible band). Spec now has `visibleDarkPixels(page, n)` (exact visible band, used for pages 1 and 156) and `canvasDarkPixels(page, n)` (whole bitmap, used for "the revealed slot was rasterised").
- 04:20 DONE   live spec **2/2** with `--workers=1`: desktop 1280x720 27.1 s (`canvas 1080px / bitmap 1080px in a 1080px frame, slots 572, rendered 1`), mobile 375x812 11.4 s (`fit: canvas 351px / bitmap 351px, line 8px | 1.5x: canvas 526px / bitmap 526px, line 12px`), plus the 1280 → 375 re-fit reading (`canvas 351px in a 351px frame`). No page errors.
- 04:30 DONE   `pnpm --filter app typecheck` + `lint` clean; whole app suite **787 passed / 2 failed** (the documented WSL TZ pair in `src/dev/seedTestData.test.ts`, 2/2 green under `--pool=forks`); `pnpm build` green (viewer chunk 489.8 kB, worker 1,265.4 kB, main unchanged); neighbouring live specs green (`assessment-generation-live` 3/3, `material-library-live` 2/2); the throwaway `e2e/tmp-window-probe.spec.ts` used for diagnosis was deleted (P7's timing probe stays).
- 04:40 DONE   records: PLAN `plan/PLAN.md` (P1–P6 status, P6 ✅, notes + file index), `plan/VERIFICATION.md` (R1/R2 ticked, R3 two-of-three, P6 gate, four live-evidence lines), `state.md` (Current/Done/Files/Pitfalls/P6 decisions/Open), STATUS row, transcript `research/2026-09-12-p6-live-verification.md`.
- 04:42 NEXT   commit P6 + records, push, `graphify update .`; then P7 in a fresh session (R4: same-origin range route, prefetch, session cache).
- 04:55 DONE   committed `a9ec0d7` (P6 code + records), `0fa870f` (STATUS/scratchpad), `49e6ad9` (rule 16 viewport scoping); pushed all three plus `ff3ab07`. `graphify update .` finished (18,781 nodes / 33,875 edges / 1,182 communities).
- 05:0x FOUND  the user's hands-on report "Where are the images in the pdf, not a single image is seen in the doc", with a console of `Unable to decode image "img_p104_1": "Jbig2Error: JBig2 failed to initialize"`. Their material is **not the corpus**: `grokking-algorithms-2nd-edition-2nd_compress` (315 pages) with **587 JBIG2 images across 260 pages** (pypdf; page 104 has 3 = `img_p104_1..3`). The corpus has zero JBIG2 (432 DCTDecode + 943 FlateDecode), so nothing in P6 could see it.
- 05:1x FOUND  the trigger: pdf.js fetches `${wasmUrl}jbig2.wasm` at runtime and `wasmUrl` was never set, so the default relative `"wasm"` resolved against the page and 404'd — every JBIG2 image dropped with a warning while text and other images still painted, which is why ink assertions passed. (The raw page bytes contain no `JBIG2Decode`; the images live in the derived `view-<version>.pdf` copy's own object graph — the original upload carries the same 590 raw names.)
- 05:2x EDIT   `apps/app/scripts/sync-pdfjs-wasm.mjs` (new; copies `pdfjs-dist/wasm/` into the gitignored `apps/app/public/pdfjs-wasm/`), `apps/app/package.json` (`dev`/`build` run it), `PdfViewer.tsx` (`wasmUrl`), `.gitignore` — verified: `/study/pdfjs-wasm/jbig2.wasm` 200/104,852 B, build emits `dist/pdfjs-wasm/`, typecheck + lint clean.
- 05:3x EDIT   `e2e/material-viewer-live.spec.ts` — fails on any `Unable to decode image` console warning; ink sampling cannot see a page that lost only its artwork.
- 05:4x DONE   live verification on the user's material: worker fetches `jbig2.wasm` + `openjpeg.wasm` (200), **0 decode warnings** on pages 104/96/35/26, pages render 65,752 / 80,456 / 88,218 / 130,291 dark pixels, and the hash-function and partitioning diagrams are visible in the screenshots (checked). The corpus path never requests the wasm. Viewer spec 2/2 with `--workers=1` after the change.
- 05:5x NEXT   commit the fix + records, push; then P7.

## Session log — P8 planning (2026-09-12, second hands-on report)
- 06:30 RESUMED  the section above is the P6 session, kept whole. This session started from the user's screenshot of the material detail page ("the extracted content makes no sense … remove that section itself") and their viewer report ("the height feels very limited, make it the height of a A4, so that complete pdf page is visible, also make the pdf interactable, zoom in, drag the page, highlight etc."). Orchestrator phase = RESUME (`scoped-question-generation` is active with live scratchpad content); the two reports touch surfaces this ticket already owns, so they became a revision round rather than a new folder.
- 06:32 FOUND  the preview section is the first 4,000 characters of `fulltext.txt`, i.e. a book's cover/foreword/preface — the screenshot's text is that prefix (`routers/materials.py:75-88`, `text[:4000] + '…'`), fetched on every open of the page (`MaterialDetail.tsx:140-161`). Six places are wired, not one: fetch effect, card, failure banner, `previewClient.ts` + its test, `types.ts:117`, `.material-preview-text` (`materials.css:654`), plus the assertions in `MaterialDetail.test.tsx` and `e2e/material-ingestion-live.spec.ts:128-130`.
- 06:35 FOUND  the frame/page numbers that explain the report: `.pdf-frame` is `calc(100dvh - 15rem)` = **480 px** at 1280x720 against a fit-to-width page of 1080x1545 (31% of one page), while at 375x812 it is 492 px against 497 (99%). The desktop is the complaint; the phone needs no change. Median text line ≈ 0.0228 x page width (P6 measurement), so a whole-page fit at 1280 gives a 339x480 page with ≈7.7 px body text.
- 06:38 FOUND  `pdfjs-dist@6.3.289` exports `TextLayer` (`types/src/pdf.d.ts:69`), and the layer's geometry is driven by CSS variables the embedder must set: the library **reads** `--total-scale-factor` (`pdf.mjs:1533/1534`, `setLayerDimensions` `:1524`) and never sets it; spans are sized `calc(var(--text-scale-factor) * var(--font-height))`; the layer is `position:absolute; inset:0`. Also: `pdf_viewer.css` is 163,832 B, and no rule anywhere in the app/library splits pointer events, so pan and selection need `.pdf-text-layer { pointer-events: none }` + `span { pointer-events: auto }` to coexist.
- 06:40 DECIDED  zoom becomes a **mode + multiplier** (`Page` | `Width`, then the existing 1.25/1.5/2/3 ladder), because "complete page visible" and "readable text" cannot both hold in a 480 px frame, and the frame must keep the column width as the fit input — "let the frame hug the page" makes `ResizeObserver` → width → scale → slot width → frame width a feedback loop. Full reasoning in the research note.
- 06:42 DECIDED  "highlight" = the pdf.js **text layer** (selectable, copyable, browser-painted); persisted marks need their own ticket since nothing in the app stores an annotation. Pinch-to-zoom is a recorded skip (our zoom re-renders rather than stretching CSS).
- 06:44 ASKED  the three questions that change 8.2-8.4 (highlight shape, default zoom, which ticket rides what) with `clarify`; the form returned unanswered, so the plan went ahead on the recommended answer of each and the questions were left as the first item under `state.md` §Open.
- 06:50 EDIT   `research/2026-09-12-p8-exploration.md` (new) – the exploration: measurements, the two readings of "the height of an A4" (+ the feedback-loop trap), the per-item cost of zoom/pan/highlight read out of the installed pdf.js, the preview consumer list, the three questions – verified (read back).
- 06:52 EDIT   `plan/PLAN.md` – Phase 8 (8.1-8.5), R-D-07..R-D-10, sequencing (P8 before P7), and R-D-01 corrected to the rAF scroll handler P6 actually shipped – verified.
- 06:54 EDIT   `plan/VERIFICATION.md` – R6-R8 + the P8 gate, all unchecked – verified.
- 06:55 EDIT   `state.md` – Current state & next (P8 planned, P7 waits), Files affected, Pitfalls (text-layer vars, pointer-events split, frame-hug loop, the six preview sites), Decisions (P8), Open (the three questions) – verified.
- 06:56 DONE   the plan is complete and nothing is implemented. Next session: read the three answers, then 8.1 → 8.4 → 8.5, one commit per step, then P7.

## Session log — P8 implementation (2026-09-12, same day, after the answers)
- 07:0x RESUMED  the user answered the three questions in one line: **"1a,2a, 3 you can decide based on best match"** → text layer only (nothing stored), whole page on open, and the round rides #62 (assistant's choice, like P6). Started 8.1 immediately.
- 07:0x EDIT   **8.1** — `MaterialDetail.tsx` (preview effect, "Extracted content" card, "Content preview unavailable" banner), `types.ts` (`MaterialContentPreview`), `previewClient.ts` + `previewClient.test.ts` (deleted), `materials.css` (`.material-preview-text`), `MaterialDetail.test.tsx` (three preview cases → one absence assertion), `e2e/material-ingestion-live.spec.ts` (heading → `toHaveCount(0)`). One patch duplicated the effect instead of deleting it; caught on read-back and reverted with `replace_all`.
- 07:0x DONE   `MaterialDetail.test.tsx` 13/13 green, typecheck + lint clean. `git add` failed with exit 128 — a stale 0-byte `.git/index.lock` from 06:35 with no live git process; removed it and committed 8.1 as **`7bd2f6d`** (7 files, −328 lines).
- 07:1x EDIT   **8.2-8.4** tests-first: `pdfView.test.ts` extended (fit **mode** `Page`|`Width`, `fitPageScale`, `stepZoom` clamping, `ZOOM_LEVELS`) → RED, then `pdfView.ts` → GREEN (23 cases).
- 07:2x EDIT   `PdfViewer.tsx` rewritten for the new controls (`Page`/`Width`, `-`/`+` with the `× N` readout, ctrl/cmd+wheel through a non-passive native listener) with a `TextLayer` per rendered slot; `materials.css` gained the layer block (with `--total-scale-factor`, which nothing in pdf.js sets), the cursors and the `.pdf-fit` cap.
- 07:3x FOUND  first live run: Page fit 335x479 in a 1080x480 frame, but Width reported **22 px of horizontal overflow** for a page the learner could not scroll to. Root cause: slots are laid out from page 1's box (583.255 pt) while page 2+ are A4 (595.22 pt), so the library sized that layer 1102 px inside a 1080 px slot. The canvas already had `.pdf-fit { max-width: 100% }`; the layer now shares it.
- 07:3x FOUND  the same run exposed a P6-era latent bug: a slot that scrolled out of the window and back stayed **blank**, because "already drawn" and "has a layer" were keyed by page number while the elements unmount. Both keyed by element now (`WeakMap`); the spec scrolls to page 20 and back to page 1 to pin it.
- 07:4x FOUND  **the environment was wrong, not the code:** 5173/8000 are held by the sibling worktree `../study-planner-web-issue-63`, so earlier live runs had been exercising *that* checkout's app (hence a suspicious `canvas 1080x1545` in page mode). Moved this checkout to app **5174** / service **8001**, added `E2E_APP_URL` to the spec, and started marketing dev on 4321 (else Playwright's `webServer` times out at 120 s).
- 07:5x FOUND  the user's two gesture reports, in order: "by drag i want page to scroll based on drag direction, but instead it selcts the text" and "Shift + drag selects all lines from the top to current line, also the cursor remains as hand". Decisive instrumentation: `down={"shift":false,"target":"DIV.pdf-text-layer","moves":12}` with `userSelect: "auto"` — the browser was taking the pointer stream for a selection gesture, and the pan saw 18 px of the intended 220 even after `user-select: none`, because **pointer capture alone stops delivering moves once the drag leaves the frame** (which a glyph-start drag does immediately).
- 07:5x EDIT   the drag is tracked on **`window`** now (`pointermove`/`pointerup`/`pointercancel`, added per drag and removed on release), `user-select: text` only while Shift is held, a caret cursor while Shift is held and `grab`/`grabbing` otherwise, and a **caret anchored under the pointer on Shift+pointerdown** so the browser's extend gesture stops selecting from the top of the document.
- 08:0x DONE   live spec **2/2** with `--workers=1`: `1280 page fit: canvas 335x479px / bitmap 335px in a 1080x480px frame, slots 572, rendered 3`; `1280 width fit: canvas 1080x1545px`; `shift-drag select: "Valid for September 2025, December 2025, March 2026 and June"`; `drag pan: 220px from the page margin, 220px over text (selection "")`; `375 refit: canvas 342x484px in a 351x492px frame`; `mobile page fit: canvas 343x492px, line 8px | 1.5x: canvas 515x738px, bitmap 515px, line 11px`. No page errors, no decode warnings.
- 08:0x FOUND  two failures that are not the viewer's: the mobile arm failed once to render page 1 within 30 s (it passed in the neighbouring run — the sync gate / shared account, recorded pitfall), and the "generate a question" step returned `500 {"detail":"auth not configured"}` from the ad-hoc service on 8001. The viewer spec now ends at the **range handoff** (the generation half duplicated `assessment-generation-live.spec.ts` and made every viewer run depend on the queue, the service's auth config and the sidecar — R-D-13).
- 08:1x DONE   gates: `pnpm typecheck`, `pnpm lint`, `pnpm build` and the whole app suite (the two known WSL-TZ failures in `src/dev/seedTestData.test.ts` are the only reds) — recorded in `plan/VERIFICATION.md` with the commit.
- 08:2x DONE   records: transcript `research/2026-09-12-p8-live-verification.md` (new), `plan/PLAN.md` (Phase 8 ✅, R-D-09 corrected to point at R-D-11, R-D-11..R-D-13 added), `plan/VERIFICATION.md` (R6-R8 ticked with measurements, P8 gate), `state.md` (Current/Done/Files/Pitfalls/Decisions/Open), STATUS row, this scratchpad.
- 08:3x NEXT   commit P8 + records, push, `graphify update .`; then P7 in a fresh session (R4: same-origin range route, prefetch, session cache).
