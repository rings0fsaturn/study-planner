# VERIFICATION — 2026-07-31-phase2-wayfinder

Running log for the Phase 2 wayfinder chart. Canonical artifact is the GitHub map ([#4](https://github.com/rings0fsaturn/study-planner/issues/4)); this log tracks charting progress.

## Status

| Item | State |
|---|---|
| Map created (#4) | ✅ done |
| Frontier tickets #5–#12 | ✅ created (8) |
| Blocked tickets #13–#16 | ✅ created (4), wired |
| Research: open-notebook | ✅ `research/external/open-notebook-findings.md` |
| Research: code sandbox (#11) | ✅ resolved + closed — Judge0 CE |
| Execution order (waves) | ✅ set 2026-07-31 — see PLAN.md "Execution order" |
| Decision tickets #5–#16 | ☐ open (one per future session) |
| #5 App IA & navigation | ✅ resolved + closed 2026-07-31 |
| #6 Assessment types, formats & scoring | ✅ resolved + closed 2026-08-01 |
| #10 Persistence & local-first fit | ✅ resolved + closed 2026-08-01 — **Wave 0 complete** |
| #13 / #18 Generation-quality evaluation harness | ✅ resolved + closed 2026-08-11 — offline metrics and activation gates defined |
| #15 / #20 Assessment review / feedback surface UI/UX | ✅ resolved + closed 2026-08-11 — review IA, family feedback, retry, evidence, and partial-state contract defined |

## Log

- **2026-07-31** Charted the Phase 2 wayfinder map on GitHub Issues (repo `rings0fsaturn/study-planner`). Named destination via grilling = **spec + working prototypes**; established the **capstone / feature-rich / no-deferrals** standing principle. Locked 7 design decisions (guidance style, trigger, inline-hint surface, full-RAG grounding, full KT coupling, hybrid code execution, out-of-scope fence). Created wayfinder labels, map **#4**, 8 frontier tickets **#5–#12**, 4 blocked tickets **#13–#16** (blocking via `Blocked by:` body refs). Fired two research subagents: open-notebook reuse (report at `research/external/open-notebook-findings.md`; verdict = port patterns, don't run the service) and code-sandbox comparison.
- **2026-07-31** Resolved research ticket **#11** (Code execution sandbox selection): **self-host Judge0 CE** over HTTP (native grading verdicts, 90+ langs, `isolate` isolation; keep over-the-wire re GPL-3.0). Piston = fallback. Report at `research/external/code-sandbox-comparison.md`; resolution comment posted; issue closed; decision recorded on map #4.
  Next: work the frontier — suggested order is the infra trio (#7 content storage → #8 vector store → #9 AI backend, which unblock #13), or build the #12 inline-hint prototype first. Run `/wayfinder 4`.

- **2026-07-31** Set the tackle order as 4 waves (see PLAN.md "Execution order"): Wave 0 frame (#5 → #6 → #10), Wave 1 de-risk (#12 prototype, parallel), Wave 2 AI infra trio (#9 → #7 → #8, unblocks #13), Wave 3 downstream (#13 → #14 → #15 → #16). Rationale: blocking graph + prototype the fuzzy #12 guide early. Each frontier ticket tagged with its wave in the ticket tables.
  Next: **Wave 0 → #5 App IA & navigation**. Run `/wayfinder 4 5` (or `/wayfinder 4` to auto-pick). Parallel tracks available: #5/#6 · #12 · #9→#7→#8.
- **2026-07-31** Resolved Wave-0 ticket **#5 App IA & navigation** (grilling + domain-modeling, grounded in `App.tsx`, `NavBar.tsx`, `AppShell.tsx`, roadmap-engine types). Five decisions: (1) **two top-level tabs** Assessments + Practice, each a Roadmaps-style hub→detail dashboard; (2) items are **material-scoped, 1+ materials** — the selected set is the RAG grounding source (via #7 extracted content) and the KT mastery target (mastery updates each selected material); (3) mobile bottom bar **demotes Settings to a header/profile menu** → clean 6-tab bar (Home/Session/Week/Roadmaps/Assessments/Practice), desktop shows all; (4) **nested path-param routes** `/assessments`, `/assessments/new`, `/assessments/:id` (same for `/practice`) under the `/study` basename per `react-router-v7-basename`; (5) inside `ProtectedRoute→RequireOnboarding→AppShell` — records are **local-first** new event kinds (Dexie + existing SyncEngine), generation is **online-only** via the Intelligence Service (offline = review/redo cached). Resolution comment posted; #5 closed; decision recorded on map #4; new fog added (Material library/detail surface).
  Next: **Wave 0 continues** — #6 Assessment types/formats/scoring and #10 Persistence & local-first fit are both independent and takeable in parallel sessions. Run `/wayfinder 4 6` then `/wayfinder 4 10`. Wave-1 #12 prototype and the Wave-2 infra chain (#9→#7→#8) can also run concurrently. Frontier query: `gh issue list --label wayfinder:phase2 --state open`.
- **2026-08-01** Resolved Wave-0 ticket **#6 Assessment types, formats & scoring** (grilling + domain-modeling, grounded in roadmap-engine `Material`, app event kinds, `packages/progress`, and the open-notebook transformations note). Four decisions: (1) **the Question is the gradeable atom**; an Assessment is a material-scoped container of possibly-mixed-format questions — quiz/written/coding are question *families*, not the top-level tab (#5) nor a per-assessment type; Practice (#16) reuses the same Question primitives coached. (2) Three families with a closed `format` enum, all formats in scope (capstone breadth): **objective** = mcq, multi_select, true_false, cloze, numeric (auto-graded); **written** = short_answer, long_form (LLM rubric); **coding** = implement_fn, debug, output_prediction, complete_code (Judge0 CE #11; output_prediction objective-graded). (3) **Scoring contract**: each family scores its own way but every graded attempt reduces to a normalized `score∈[0,1]` **plus** a per-skill binary `correct` (`score ≥ τ`, default ≈0.6) — the `perSkill` tuple is the KT observation #14/#15 consume; the exact score→mastery math and final τ stay #14's job. Typed schema pinned for `Assessment` / `QuestionBase` + per-format discriminated payloads / `Attempt` / `GradedResult` (see resolution comment on #6). (4) **Difficulty** = authored ordinal **1..5** band (Bloom-ish) set by #13, plus a reserved-but-empty `observedDifficulty {b?, pCorrect?, n?}` slot #15 fills from attempt data without a migration — adaptive from the first session, refined over time. Resolution comment posted; #6 closed; decision recorded on map #4; new fog added (**Assessment feedback / review surface**).
  Next: **Wave 0 → #10 Persistence & local-first fit** — the last independent Wave-0 ticket (the event/data-model spine every record must obey; blocks #14 with #6). Run `/wayfinder 4 10`. Wave-1 #12 prototype and the Wave-2 infra chain (#9→#7→#8) remain concurrently takeable. Frontier query: `gh issue list --label wayfinder:phase2 --state open`.
- **2026-08-01** Resolved Wave-0 ticket **#10 Persistence & local-first fit** (grilling + domain-modeling, grounded in `EventStore.ts`, `EventStoreProvider.tsx`, `SyncEngine.ts`/`sync/types.ts`, migration `003`, and the eventstore/dexie/sync/supabase rules). **Correction found:** the live Dexie schema is **v5** (tables `events, sync_queue, sync_meta, onboardingDraft, activeSession, calibrationCache`), not v3 as `eventstore-architecture` claims — so the migration is **v5→v6**. Load-bearing constraint: the snapshot is a single JSON blob of the entire event log capped at **5 MB**, and Supabase `events` is the one source of truth. Five decisions: (1) **Content home = three-layer split** — thin local-first `AssessmentCreated` pointer event (synced spine) + server-owned Supabase `assessments`/`questions` rows (fat generated content incl. hidden answers/solutions; migration `004`, owned by #13) + non-synced Dexie `assessmentContentCache` (redacted/answer-free, offline read). Keeps the event log + snapshot lean; answer keys never leave the server. (2) **Attempt grain = per-Question, runId-grouped** — `QuestionAttempted`→`QuestionGraded` (1:1 via `attemptId`; `perSkill[]` = one clean KT observation per event, honoring #6's Question-atom); **retry = a fresh observation** (new `attemptId`); optional `AssessmentRunCompleted` summary. Finalizes #5's suggested Assessment-level kinds into question-grained ones. (3) **Mastery = derived projection cached in `masteryCache`** (calibrationCache precedent, keyed by `materialId`, non-synced), recomputable from synced `QuestionGraded.perSkill`; `MasteryUpdated` event **reserved** (not required) so #14 can opt into an audit trail without a migration. (4) **All grading server-side, no offline grading** (user chose the max-integrity/consistency option over the stakes-split recommendation) — redacted cache never holds answers for any surface; `QuestionAttempted` queues offline, `QuestionGraded` completes on reconnect (Practice trades instant offline feedback for integrity). (5) **Dexie v5→v6** adds only the two non-synced cache tables (`assessmentContentCache: 'assessmentId'`, `masteryCache: 'materialId'`); new event kinds are rows in the existing `events` table; practice-resume tables (`activePracticeSession`, `HintShown`) reserved to #16/#12. Resolution comment posted; #10 closed; decision recorded on map #4; new fog added (**Content cache lifecycle / eviction**). **#10 + #6 together unblock #14.**
  Next: **Wave 0 is complete.** Frontier = **Wave-1 #12 Inline-hint live guide (prototype)** — the highest-uncertainty centerpiece, unblocks #16 — and the **Wave-2 infra chain #9→#7→#8** (unblocks #13). #14 is now unblocked (#6+#10 done) but placed in Wave 3 to follow the generation shape. Run `/wayfinder 4 12` (prototype) or `/wayfinder 4 9`. Frontier query: `gh issue list --label wayfinder:phase2 --state open`.

- **2026-08-11** Resolved the Material library, detail, ingestion-progress & picker UI/UX ticket (live GitHub issue **#19**) through grilling + domain modeling. The resolution defines a dedicated library with contextual entry points; URL/YouTube/file/manual sources; list-first search/filter/sort; overview-first direct detail routes; staged live ingestion with actionable failures; ready-only generation eligibility; contentless planning materials; archive/restore rather than hard delete; replace-keep-id with stale dependent content; linked usage groups; responsive multi-select pickers; offline read-only behavior; and return-to-origin contextual flows. Resolution comment, close, and map Decisions-so-far pointer are recorded on GitHub map **#4**.
- **2026-08-11** Resolved the generation-quality evaluation harness ticket (live GitHub issue **#18**) on map **#4**. Locked four offline deliverables: groundedness judging over cited chunks, authored-vs-observed difficulty calibration, embedding-based assessment deduplication/diversity, and the #14 mastery-estimator bake-off on shared KT-bench folds. Groundedness/dedup require the implemented pipeline plus one gold sample; difficulty requires #15 plus at least five graded attempts per question; the estimator sweep requires #14 plus shared folds. Resolution comment, close, and map Decisions-so-far pointer are recorded.
- **2026-08-11** Resolved the assessment review / feedback surface UI/UX ticket (live GitHub issue **#20**) on map **#4** through grilling + domain modeling. Locked a summary-led, mistake-oriented review; compact responsive question navigation; progressive disclosure; objective, written, and coding feedback treatments; expandable source evidence; per-question and full-assessment retry; preserved attempt history; honest pending/partial/failed states; equal question weighting with family breakdown; accessible state communication; and shared question-review primitives for Practice. Resolution comment, close, and map Decisions-so-far pointer are recorded.
- **2026-08-13** Issue-number normalization across GitHub and `.work/`: title prefixes and every cross-reference (map #4, tickets #5–#21, this log, PLAN.md, handover, STATUS.md, TRACEABILITY.md, Learner Growth ticket #26) now use live GitHub issue numbers. Legacy prefixes #9–#26 removed; historical comments may still carry them (see map "Numbering note").
