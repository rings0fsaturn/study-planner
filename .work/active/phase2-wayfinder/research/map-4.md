> **Wayfinder map.** This issue is the shared map for Phase 2. It indexes the decisions made and points at the child tickets that hold their detail. Child tickets carry a `wayfinder:<type>` label; find the live frontier with the query below.
## Destination
A validated, feature-rich Phase 2 spec - Assessments (quiz / written / coding) plus an LLM-guided Practice area - **RAG-grounded** and **coupled to the knowledge-tracing / adaptation research pillar**, backed by **working throwaway prototypes** of the risky parts (above all the inline-hint live guide). Output: a spec ready for implementation planning, capstone-grade.
## Notes
- **Contract pack:** [Phase 2 Object, Provider, and Pipeline Contract Pack](https://github.com/rings0fsaturn/study-planner/issues/22) owns the draft artifacts in [services/intelligence/contracts/phase2](https://github.com/rings0fsaturn/study-planner/tree/main/services/intelligence/contracts/phase2). Its traceability index links the assessment, ingestion, vector, Gemini, persistence, generation, grading, practice, KT, and downstream UI decisions.
- **Domain:** `study-planner-web` M.Tech capstone. Stack: Astro marketing + Vite/React 19 SPA (`apps/app`) + FastAPI Intelligence Service (`services/intelligence`, JWT vs Supabase) + Supabase (auth/DB/storage) + local-first Dexie event store. Research tier: pyBKT + KT-bench + Pillar-A calibration/progress.
- **Standing principle (overrides defaults):** this is an M.Tech **capstone** - build **feature-rich and complete**. Do **NOT** propose ship-fast / MVP-first / "defer to a later phase" tradeoffs.
- **Model-layer economics — AMENDED by #9 (2026-08-01):** the "development cost is not a constraint" clause is **reversed for the AI/model layer only**. No funds for Claude, so the model-selection principle is now **cost-effective, accurate-enough** on the **Google Gemini free tier**. **Feature scope stays full** (full RAG, full KT coupling, all assessment families, live guide) - only the *models* go cheap. Ripples to #13, #14, #8.
- **Destination override:** this map carries **execution** (throwaway prototypes) into charting, not just planning. Prototype tickets produce clickable artifacts.
- **Architecture-refactor pivot (2026-08-01, #17) — RESOLVED 2026-08-02:** grilling #7 revealed the async-job / event-bus / microservice-split question is a **foundational spine decision**, so it was split into **#17**. **Resolved: async = pgmq (Kafka rejected on load grounds); two Python services (Ingestion worker-only + Intelligence API+worker); pgmq stage queues; Tine-C local-first spine with migration `004` content tables; both audit gaps fixed.** Invariant held: Supabase Postgres is the single source of truth; the frontend never talks to a bus — it stays local-first and reads derived state over HTTP. **#7 reopened, unblocked.**
- **open-notebook (`research/external/open-notebook`):** MIT-licensed NotebookLM clone. Decision from research: **port its patterns** (transformations = prompt-over-content engine; chunk→embed→vector-search RAG; context-builder; Jinja prompts) into our FastAPI Intelligence Service on Supabase/pgvector. **Do NOT run it as a service** (SurrealDB + single-user password auth clash with our stack). Findings: `research/external/open-notebook-findings.md`.
- **Skills every session should consult:** `grilling` + `domain-modeling` for decision tickets; `research` for AFK research; `prototype` for prototype tickets.
- **Tracker ops:** child tickets = issues labelled `wayfinder:phase2` + a `wayfinder:<type>` label. Frontier query: `gh issue list --label wayfinder:phase2 --state open` then exclude any with an open "Blocked by:" ref. Claim a ticket by assigning it to yourself before work.
- **Numbering note (2026-08-13):** titles and cross-references on this map and its tickets were normalized to live GitHub issue numbers (map #4, tickets #5–#21). Older comments may still contain the pre-migration numbers (#9–#26).
## Decisions so far
<!-- one line per closed ticket: gist + link. Charting-session decisions that pre-seed the map: -->
- **Phase 2 Object, Provider, and Pipeline Contract Pack** - approved canonical schemas, endpoint surface, provider normalization, pipeline ownership, durable events, telemetry, fixtures, and validation. ([#22](https://github.com/rings0fsaturn/study-planner/issues/22))
- **Destination shape** - spec **+ working prototypes** (execution rides into the map).
- **Guidance style** - **tiered Socratic**; the guide never hands over the answer.
- **When it helps** - **hybrid on-demand**: user asks + cheap signals (idle timer, failed test run) that *offer* a hint.
- **Moment-to-moment surface** - **inline hints** (Copilot/Cursor UX pattern, but ghost text is a hint/question, never finished code/prose).
- **Content grounding** - **full RAG now**: ingestion + chunking + embeddings + pgvector + async worker. No deferral.
- **Research coupling** - **fully couple to KT/adaptation**: outcomes → mastery signal → adaptive difficulty → roadmap feedback.
- **Code execution** - **hybrid**: client-side (Pyodide/JS) for instant feedback + server sandbox (multi-language) for authoritative grading.
- **Server code sandbox** - **self-host Judge0 CE** over HTTP (native grading verdicts, 90+ langs, `isolate` isolation; keep over-the-wire re GPL). Piston = fallback. ([#11](https://github.com/rings0fsaturn/study-planner/issues/11))
- **App IA & navigation** - **two top-level tabs** (Assessments, Practice), each a Roadmaps-style hub→detail dashboard; items are **material-scoped (1+ materials)** = RAG source + KT mastery target; mobile bottom bar demotes Settings to a header menu for a clean 6-tab bar; nested path-param routes (`/assessments`, `/practice`) under `/study`; inside `ProtectedRoute→RequireOnboarding→AppShell`, records local-first (Dexie+sync), generation online-only. ([#5](https://github.com/rings0fsaturn/study-planner/issues/5))
- **Assessment types, formats & scoring** - **Question is the gradeable atom**; an Assessment is a material-scoped container of possibly-mixed questions. Three families / closed `format` enum: objective (mcq, multi_select, true_false, cloze, numeric → auto-grade), written (short_answer, long_form → LLM rubric), coding (implement_fn, debug, output_prediction, complete_code → Judge0). **Every attempt reduces to `score∈[0,1]` + per-skill binary `correct` (≥τ≈0.6)** = the KT observation (#14 owns the mastery mapping). Difficulty = authored **1..5** band + reserved `observedDifficulty` slot for #15. ([#6](https://github.com/rings0fsaturn/study-planner/issues/6))
- **Persistence & local-first fit** - **three-layer split**: thin local-first `AssessmentCreated` pointer event (synced spine) + server-owned Supabase `assessments`/`questions` rows (fat generated content, incl. hidden answers/solutions; migration `004`, #13) + non-synced Dexie **`assessmentContentCache`** (redacted, answer-free, offline read). Attempts are **per-Question**, runId-grouped: `QuestionAttempted`→`QuestionGraded` (1:1 via `attemptId`; `perSkill[]` = the KT observation); **retry = fresh observation**. **All grading server-side** (no answer keys client-side; attempts queue offline, grades on reconnect). Mastery = derived projection in non-synced **`masteryCache`** (calibrationCache precedent), `MasteryUpdated` event **reserved** for #14. Dexie **v5→v6** adds the two cache tables only; new event kinds are rows in `events`. ([#10](https://github.com/rings0fsaturn/study-planner/issues/10))
- **Inline-hint live guide (prototype)** - **surface = C, anchored coach popover** (built + chosen HITL; 3 surfaces prototyped). Locked contract confirmed clickable: **line-anchored + line-aware** hints (anchor follows caret, leads with the line it reads); ladder `nudge→hint→targeted→gated-reveal` with copy that **orients-then-asks** (Socratic ≠ cryptic); **hybrid triggers OFFER** (I'm stuck / idle / failed-test); reveal gated in prompt **and** structurally (separate call). Guide = streamed server-side Intelligence-Service LLM call (question + answer + active-line + RAG chunks + tier); **model TBD = #9**. #16 build note: real editor needs Monaco/CodeMirror inline-widget API. Throwaway branch `prototype/wf17-inline-hint-guide` · route `/study/practice-prototype`. ([#12](https://github.com/rings0fsaturn/study-planner/issues/12))
- **AI backend home & streaming** - **single JWT-verified Intelligence Service** owns all model + embedding calls; **cost-principle reversed** → **Google Gemini free tier** via `google-genai`, behind a provider abstraction (local Ollama fallback): guide=`gemini-3.5-flash-lite`, generation+grading=`gemini-3.6-flash`, embeddings=`gemini-embedding-001`@768-dim `task_type`. **Streaming** = `POST`+`fetch` ReadableStream over `/v1/*` (**not** EventSource - header auth), provider-neutral SSE frames; guide streams deltas + **reveal = separate gated call**, generation streams per-question, grading req/resp. **Prompt arch** = git-versioned Jinja templates (ported open-notebook), SSTI variable rule, untrusted-as-data, native `responseSchema`. **Abuse hardening** = key in `services/intelligence/.env` server-side; **no endpoint runs the model on arbitrary text - every call bound to a user-owned artifact**; durable Postgres rate-limits + free-tier circuit breaker; audit logging. #9/#8 seam: #9 owns the embedding client, #8 bulk-drives it. ([#9](https://github.com/rings0fsaturn/study-planner/issues/9))
- **Data + service architecture refactor (spine)** - **async mechanism = pgmq / Supabase Queues** (Kafka rejected: no Kafka-shaped load; fan-out tiny; replay already owned by `public.events`; job state now *inside* the source-of-truth DB). **Two Python services**: **Ingestion Service** (worker-only: deterministic parsers → extract `full_text` → chunk) + **Intelligence Service** (API + worker: embed via Gemini per #9, LLM topics). #8 chunk/embed splits along the #9 seam. **pgmq multi-stage queues** `ingest.extract→embed→topics` (Postgres-trigger enqueue; visibility-timeout retry); one-image/two-entrypoints (web+worker). **Spine = Tine C / no bus to the frontend**: write path stays local-first & untouched (Dexie→`sync_queue`→`public.events`); migration `004` = server-owned content tables (`full_text`/chunks/`content_embeddings` pgvector) as Postgres SoT; pointer-events-only on the log/snapshot; Dexie = redacted cache. **Audit gaps fixed**: `sync_checkpoints` migration added (RLS `auth.uid()`); 5 MB snapshot cap = **enforce** client-side (skip+telemetry, full-replay fallback). ([#17](https://github.com/rings0fsaturn/study-planner/issues/17))
- **Content ingestion & storage** - material model = **A/unify + first-class server-owned library** (add materials in a store, attach to roadmaps; grain locked **1 material = 1 ingested body**; multi-source = N materials in one assessment → N clean KT signals). **Fork 2a**: material is a **server-first entity** (Postgres `materials` = SoT, direct supabase-js CRUD, RLS; **not** event-sourced), only a thin roadmap-attach pointer `{materialId,title,role,estimatedDuration}` rides the log (roadmap-engine untouched); `materialId` DB-minted; onboarding material-add becomes online-requiring. **Creation trigger-driven** (client INSERT + Storage upload → Postgres trigger → pgmq; workers service-role; no bespoke endpoint). **Migration `004`** = `materials` (nullable `source_type` → contentless allowed) + `content_chunks` with **folded `halfvec(768)`** HNSW cosine; RLS `auth.uid()=user_id`. **`material-raw`** bucket (private, `<uid>/<materialId>/<file>`, 25 MB, pdf/docx/pptx/txt/md; keep raw after extract). **Status = 5a** single `materials.status` state machine (`pending→extracting→chunking→embedding→ready`+`failed`; pgmq transport-only; read via Realtime; partial ingest keeps `full_text` displayable, RAG gates on `ready`). **Recovery** = retry (re-enqueue stage) / delete+re-add (cascade + client Storage remove) / replace-keep-id. Free tier: ~900–1000+ avg materials with halfvec; ⚠️ project pauses after 7 days idle. ([#7](https://github.com/rings0fsaturn/study-planner/issues/7))
- **Vector store, embeddings & async jobs** - embedding+retrieval design on the #7/#17 base. **Chunking** 400-token/60-overlap, structure-then-recursive, tiktoken, `start_seconds` col for YT citations. **Preprocess** (Ingestion, zero-quota): deterministic cleaning + local-ML transcript punctuation; YT transcript = `youtube-transcript-api` in the Python worker (metadata edge fn stays, can't fetch captions); contextual-aug/semantic-chunk rejected on quota→eval note. **Embeddings** `gemini-embedding-001`@768 **L2-normalized before store** (Gemini doesn't self-normalize <3072; beyond open-notebook), keep cosine; batch 100/per-batch-commit; **resume=idempotent NULL-scan**; **backpressure=auto pgmq-redelivery** on 429 (manual Retry only for real failures); **reserve RPM headroom for the query hot path**; `status_detail` via Realtime. **Retrieval** server-internal only; RPC `match_content_chunks(...)` under user JWT (RLS) + mandatory `material_id` scoping; top-k 10 + loose floor; **local `bge-reranker` on gen+grading, off for guide**; generation=full_text-in-context-when-it-fits (policy→#13). **Lifecycle** replace=drop+rebuild / retry=resume; HNSW 16/64/`ef_search=100`, single index + **`hnsw.iterative_scan` (pgvector ≥0.8)** for filtered recall. **Eval hook** retrieval logging + levers-as-config; gold-set recall@k/MRR+rerank-ablation named as future deliverable. Migration `005` pins vector≥0.8; RPC in `006`. ([#8](https://github.com/rings0fsaturn/study-planner/issues/8))
- **Grounded assessment-generation pipeline** - end-to-end generation on the #7/#8/#9/#10 base (**keystone**). **Topology** = two-stage **plan→generate** (ported open-notebook `ask` plan→fan-out shape, synthesis barrier dropped; stage-2 authoring = transformation-execution pattern + native `responseSchema`). **Grounding** = two-tier: **full_text-in-context when it fits** (~60-70% budget, sized from chunk `token_count`) else **topic-stratified coverage-sampling + MMR** (net-new beyond open-notebook); similarity-retrieval reserved for *targeted* "about topic X" slots; blueprint routes grounding per slot. **Adaptive difficulty** (#13↔#14 seam) = blueprint reads per-skill `p∈[0,1]` from a **client-supplied mastery snapshot** in the request (no server mastery store; #14 owns computing `p`); **ZPD "aim just above"** + calibration-spread cold-start + blended weak/coverage selection. **Composition** = user recipe `{count,family-mix}`, blueprint fills format/difficulty/topic; **groundedness-over-count** (fewer + `Warning[]`, never pad); coding gated on code-bearing material. **Typed gen** = per-format `responseSchema` (envelope + visible payload + **server-only hidden block**: objective answers / written rubric+reference / coding reference-solution+full test set); **Judge0 self-check** at gen (coding solution must pass its own tests). **Validation** = structural + **citation-id grounding verification** (cited `content_chunks.id` in-scope) + coding self-check; **online groundedness = citation-id only** (LLM faithfulness judge → offline #18); **one repair → else drop** + dedup; circuit-breaker + attempt cap. **Granularity** = **per-slot fan-out** (bounded concurrency), one Question per SSE frame. **Storage** (`007_assessments.sql`, co-owned #10) = `assessments` (row up-front `status='generating'` = LLM-artifact + resume anchor; `material_ids[]`,`recipe`,`blueprint`,`warnings`,`grounding_stale`) + `questions` (per-question `material_id` = clean KT signal; `hidden_block`,`citations`); **answer-key secrecy = column-level `REVOKE SELECT (hidden_block) FROM authenticated`** (service-role reads for grading); thin `AssessmentCreated` pointer; redacted `assessmentContentCache` = envelope+visible_payload; `grounding_stale` flag (#13 owns, #5 owns UX). **Eval** = cheap telemetry now; 3 offline metrics → tracked ticket **#18** (blocked-by #13+#15, written triggers). ([#13](https://github.com/rings0fsaturn/study-planner/issues/13))
- **Grading → mastery signal mapping** - **#14 fixes the observation→mastery INTERFACE + a Bayesian baseline; #15 swaps full BKT behind it**. Mastery keys on **`materialId`** (#6/#10; `masteryCache[skill]`→`masteryCache[materialId]`; per-`(material,skillTag)` refinement axis reserved for #15). Pluggable estimator `estimate(obs)→{p∈[0,1],confidence,n}` with **3 candidates** (EWMA / Beta-Bernoulli / Gaussian-clamped); **default + all constants chosen empirically by a `research/kt-bench` bake-off** (ECE / AUC-accuracy / cold-start-`k`, alongside pyBKT) - **nothing pinned by fiat**. Compute = server-side stateless **`/v1/mastery`** (`py-progress` authoritative, `calibrationCache` precedent; **no server store, no migration**); TS mirror for parity/offline-read. Seeds only: Beta-Bernoulli, soft-`score` update, `Beta(1,1)`, `λ=1`, difficulty recorded-not-weighted, `τ`≈0.6 (mastery is τ-independent). **Pure projection - no `MasteryUpdated`** (reserved). Mastery-estimator bake-off folded into **#18**. ([#14](https://github.com/rings0fsaturn/study-planner/issues/14))
- **KT model & adaptive-difficulty loop** - production uses concept-level BKT behind the #14 mastery interface; grades update immediately; neutral cold start with uncertainty; adaptive selection targets expected correctness around 0.7 with one-band movement; roadmap output is advisory and rebuildable. ([#15](https://github.com/rings0fsaturn/study-planner/issues/15))
- **Generation-quality evaluation harness** ([#18](https://github.com/rings0fsaturn/study-planner/issues/18)) - offline groundedness, difficulty calibration, deduplication/diversity, and the #14 mastery-estimator bake-off use explicit pipeline, gold-sample, attempt-count, and shared-fold gates; evaluation stays out of the generation hot path.
- **Material Library and Attachment UX (prototype)** - decisions Q1–Q22 locked 2026-08-13: responsive card-grid library (compact desktop, spacious mobile); readiness-led detail with state-led primary action (Practice / Retry); primary action + overflow card menu (archive / replace / delete); ready-only Assessment/Practice pickers with contentless materials staying attachable for roadmap planning; persistent picker footer (bottom sheet mobile / side panel desktop); inline replace keep-ID; permanent-source-removal delete warning; dedicated Practice-this config page; context-aware empty states; archived hidden by default (filter + detail restore). Throwaway prototype at `apps/app/src/prototype/material-library/` (route `/study/material-library-prototype`) refined to match. Implementation ticket #36 unblocked. ([#33](https://github.com/rings0fsaturn/study-planner/issues/33))
- **Assessment review and feedback UX (prototype)** - HITL choice 2026-09-03: **split-pane** review surface (desktop ≥1024px = persistent 280px question-navigator rail + summary/feedback pane; below 1024px it collapses to the single-column + sticky strip, so mobile needs no separate decision). Locked interaction vocabulary: per-question + whole-assessment retry with preserved attempt history (queued attempt = `aria-busy` skeleton + "previous attempt kept" copy; prior timeline collapses into an expandable `<details>`); honest processing/pending/failed states; family feedback treatments + expandable chunk/quote citations; `QuestionReviewCard(question, attempt)` is the shared primitive Practice #16 reuses. Throwaway prototype at `apps/app/src/prototype/assessment-review/` (dev-only route). Implementation ticket #40 unblocked. ([#34](https://github.com/rings0fsaturn/study-planner/issues/34))
- **Assessment taking and objective grading (implemented)** - learner takes the grounded objective question: answer online or offline (answer stored locally + server `question_attempts`, attempt event stays answer-free), deterministic server-side grading against `answer_block` (score in [0,1], per-skill binary at tau 0.6, `Correct.`/`Not correct.` feedback), third worker arm on the `assessment_grade` queue, honest UI states (answering → submitting → queued-offline → grading → graded/failed, retry = fresh attempt, append-only history). Live-verified 2026-09-09 incl. offline→reconnect walk; repair migration 026 (RPC `material_id` select). Unblocks #40 (review), #41 (written), #42 (coding), #43 (mastery). ([#39](https://github.com/rings0fsaturn/study-planner/issues/39))
- **Assessment review surface (implemented)** - the learner reviews a graded assessment on `AssessmentDetail`: summary band (verdict, family chips, warnings with jump, attempt timeline), responsive question navigator (280px rail ≥1024px, sticky strip below), per-question review cards with family treatments (objective full — your-pick + per-skill + expandable citations; written/coding shared grade shape + placeholders for #41/#42), preserved append-only attempt history (oldest→latest, latest expanded), whole-assessment retry rounds that collapse prior timelines into `<details>`, per-question + whole-assessment retry = fresh observations (never overwrite). Read route echoes the learner's own answer (owner-scoped, D-01) so your-pick survives fresh-device restore. Live-verified 2026-09-09 at 1280×800 (rail) + 375×812 (strip, no overflow); evidence `active/issue-40...→archive/.../plan/evidence/` + `e2e/assessment-review-40-live.spec.ts` kept as the durable artifact. Unblocks #41 (written), #42 (coding), #43 (mastery). ([#40](https://github.com/rings0fsaturn/study-planner/issues/40))
- **Coding assessment and sandbox grading (implemented)** - all four coding subtypes behind one sandbox arm on the shared `assessment_grade` queue: generation authors coding questions + hidden tests + reference solution (LLM-judged suitability; `code_not_derivable` warnings carry the reasoning; generation-time self-check runs the reference through the sandbox or the question drops), the learner takes them in a lazy CodeMirror editor with an in-browser Pyodide advisory (visible tests only, badged advisory - the server grade is authoritative), and the grading worker executes the authored tests per attempt in a self-hosted **Piston** sandbox (the Judge0 D-02 fallback - isolate is cgroup-v1-only and cannot run on this host) to compose `QuestionGraded` with `grader: 'judge0'`: hidden-test pass-rate score, `correct @ 0.6`, shared per-skill observations, veiled `Hidden test N` table, deterministic template words, compile/runtime/timeout fail-closed 0, infra errors redelivered. `output_prediction` grades deterministically without any sandbox contact. Migration 031 (coding columns + widened subtype CHECK + materials `has_code`/`code_languages`), contract pack coding schemas, coding review treatment, assessments-only (practice enablement belongs to #45). Live-verified 2026-09-19: `e2e/coding-assessment-live.spec.ts` 3/3 green (fail-closed compile-error drill, mid-grading reload resume, retry + history, hidden-veil redaction over responses + DOM, 375 px flow, no-sandbox-contact assertion); AC1-AC4 ticked. ([#42](https://github.com/rings0fsaturn/study-planner/issues/42))
- **Written assessment and rubric grading (implemented)** - written short_answer and long_form questions on the shared taking/review spine: typed visible payloads with a server-only rubric+reference hidden block (migrations 027/028), generation-time rubric authoring with citation-id enum-bound schemas, and an `llm_rubric` grading arm that composes per-criterion provider scores by authored weights into the shared `score`/`correct @0.6`/`perSkill[]` observation contract, with fail-closed vs redeliver split on provider failures. Retry keeps the stored recipe (never a hardcoded family) and creates a fresh observation. Live-verified 2026-09-11: `e2e/assessment-written-41-live.spec.ts` 4/4 green (desktop + mobile, reload restore, steered long_form, retry history); AC1–AC4 ticked. Unblocks #44 (written practice) and #43 (mastery). ([#41](https://github.com/rings0fsaturn/study-planner/issues/41))
## Tickets (chart-time frontier)
<!-- GitHub-native tracking of children; authoritative status is each issue's own state -->
### Frontier
- [x] [App IA and navigation](https://github.com/rings0fsaturn/study-planner/issues/5) - two hubs, material-scoped routes, local-first records
- [x] [Assessment types, formats and scoring](https://github.com/rings0fsaturn/study-planner/issues/6) - Question atom, three families, normalized KT contract
- [x] [Data and service architecture refactor](https://github.com/rings0fsaturn/study-planner/issues/17) - pgmq, two services, Tine-C spine, audit gaps fixed
- [x] [AI backend home and streaming](https://github.com/rings0fsaturn/study-planner/issues/9) - Intelligence Service, Gemini, authenticated fetch streaming
- [x] [Persistence and local-first fit](https://github.com/rings0fsaturn/study-planner/issues/10) - pointer events, server content, redacted caches, server grading
- [x] [Code execution sandbox selection](https://github.com/rings0fsaturn/study-planner/issues/11) - Judge0 CE
- [x] [Inline-hint live guide](https://github.com/rings0fsaturn/study-planner/issues/12) - Variant C anchored coach popover
- [x] [Content ingestion and storage design](https://github.com/rings0fsaturn/study-planner/issues/7) - first-class material library and ingestion state machine
### Newly resolved
- [x] [Phase 2 contract pack](https://github.com/rings0fsaturn/study-planner/issues/22)
- [x] [Practice session model](https://github.com/rings0fsaturn/study-planner/issues/16)
- [x] [Vector store, embeddings and async jobs](https://github.com/rings0fsaturn/study-planner/issues/8)
- [x] [Grounded assessment generation](https://github.com/rings0fsaturn/study-planner/issues/13)
- [x] [Grading to mastery mapping](https://github.com/rings0fsaturn/study-planner/issues/14)
- [x] [KT model and adaptive difficulty](https://github.com/rings0fsaturn/study-planner/issues/15)
- [x] [Generation-quality evaluation](https://github.com/rings0fsaturn/study-planner/issues/18)
### UI/UX cluster
- [x] [Roadmap-feedback UX](https://github.com/rings0fsaturn/study-planner/issues/21) - frontier
- [x] [Material library and picker UX](https://github.com/rings0fsaturn/study-planner/issues/19) - ✅ prototype resolved via #33 (2026-08-13); implementation #36
- [x] [Assessment review and feedback UX](https://github.com/rings0fsaturn/study-planner/issues/20) - ✅ prototype resolved via #34 (2026-09-03); implementation #40
## Not yet specified (fog)
- **Spaced-repetition scheduling** (SM-2/Anki-style *when-to-re-assess*) - in scope, not yet sharp. Distinct from KT adaptive difficulty. Graduates once the mastery-signal + KT loop are decided.
- **Roadmap-feedback UX** - ✅ **graduated to #21** (2026-08-03). How adaptive-difficulty / mastery results surface back into the roadmap & progress views. Now unblocked because #15 is resolved.
- **Capstone evaluation & metrics** - how we measure/validate guide effectiveness and the KT model - the research-write-up angle. Graduates once the KT loop exists. **(Candidate metric from #9: "grader robustness to adversarial submissions"; free-tier data-use is an ethics/limitations note.)** *The **generation-quality** slice (groundedness / difficulty-calibration / dedup) graduated to tracked ticket #18 by #13.*
- **Material library / detail surface** - ✅ **graduated to #19** (2026-08-03). Material is a first-class server-owned entity (#7); IA skeleton set (#5); `grounding_stale` handed off (#13). #19 owns the actual screens: library/detail/upload-progress, multi-material picker for New flows, "Practice this", and the **delete-referenced-by-roadmap** lifecycle UX.
- **Assessment feedback / review surface** - ✅ **graduated to #20** (2026-08-03). How graded results, rubric breakdowns, per-test-case tables, and per-question `explanation` render in the `/assessments/:id` review view. Gradeable contracts settled (#6 #10 #13); #20 owns the review UI; sharpens alongside #16's session UX.
- **Guide tuning dials** (tier count + how concrete tier 0 is; per-learner directness) and **Practice code-editor choice** (Monaco/CodeMirror for real inline-widget line-push) - surfaced by the #12 prototype; both are **inputs to #16**, not standalone tickets.
- **Content cache lifecycle / eviction** - when redacted caches are refreshed vs evicted, and how a cache miss offline degrades. Surfaced by #10. **#7 clarified the materials cache** = redacted metadata + `status` only (no `full_text`; hydrated from server via Realtime/read). **#13 resolved the `assessmentContentCache` projection** = envelope + `visible_payload` only (no `hidden_block`; = the column-granted client view; enables offline *taking*). Remaining fog = only the *eviction/refresh* policy for both caches.
<!-- GRADUATED (resolved by #9): "Prompt architecture & prompt-injection safety" - now decided: git-versioned Jinja templates, SSTI variable rule, untrusted-as-data delimiting, answer-key excluded from hint context, no-free-prompt/anchored-to-owned-artifact abuse rule. -->
## Out of scope
- **Multiplayer / peer features** (collaborative practice, peer review) - Phase 2 is single-user.
- **Voice / audio & podcast generation** (incl. open-notebook podcasts) - unrelated to assessment/practice.
- **Proctoring / anti-cheat** (webcam, plagiarism detection) - heavy and off-goal.
