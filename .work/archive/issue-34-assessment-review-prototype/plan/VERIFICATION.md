# VERIFICATION — issue-34-assessment-review-prototype

Running verification log for the #34 throwaway prototype (Assessment Review + Feedback UX). Canonical ticket is GitHub [#34](https://github.com/rings0fsaturn/study-planner/issues/34); this log tracks plan execution.

## Status

| Item | State |
|---|---|
| Planning docs (plan/PLAN.md, this file, research/decisions-34.md) | ✅ done 2026-09-01 |
| Ticket #34 claimed (assigned `rings0fsaturn`) | ✅ done 2026-09-01 |
| STATUS row added | ✅ done 2026-09-01 |
| Phase 1 — skeleton + fixtures + dev-only route | ✅ done 2026-09-01 |
| Phase 2 — summary + question navigation | ✅ done 2026-09-01 |
| Phase 3 — family feedback + citations + honest states | ✅ done 2026-09-01 |
| Phase 4 — retry + attempt history + recommendation record | ✅ implemented + verified 2026-09-03 (recommendation record pending HITL) |
| Redaction grep negative (answerBlock/correctIndex/referenceSolution/hiddenTest) | ✅ done 2026-09-01 (Phase 1 + Phase 2 combined; re-run negative after Phase 3; re-run negative after Phase 4) |
| Screenshots (plan/evidence/, 375×812 + 1280×800, both variants) | ✅ done 2026-09-03 (7 captures incl. retry states; PNG dimensions verified) |
| #34 closed + map #4 Decisions-so-far appended | ✅ done 2026-09-03 (comment `5520168706`, closed completed; #20 reference-commented; map #4 decision line + ticket checkbox updated) |

## Log

- **2026-09-01** Planning session: task opened via work-journal-orchestrator (`.work/active/issue-34-assessment-review-prototype/`), STATUS Active row added, ticket #34 claimed on GitHub, `plan/PLAN.md` + this file + `research/decisions-34.md` written. Next: implement PLAN.md Phase 1, then P2–P4, then HITL grill + record the recommendation on #34.
- **2026-09-01** Phase 1 implemented + verified: `assessment-review-fixtures.ts` (5 contract-shaped mocks, redaction-safe), `AssessmentReviewPrototype.tsx` (state toolbar + A/B variant shells + placeholder body), `assessment-review.css`, dev-only route in `App.tsx`, `PrototypeSwitcher` gained a `keys` prop. `pnpm typecheck` + `pnpm lint` green; redaction grep negative; `pnpm --filter app build` green; route string absent from `apps/app/dist/` JS (tree-shaken; `.js.map` reference matches the practice/roadmap precedents). Live check at 375px + 1280px: 5 states, A/B toggle, retried shows "Attempt 2 of 2", no horizontal overflow, zero console errors. Next: Phase 2 (summary band + QuestionNavigator).
- **2026-09-01** Phase 2 implemented + verified: `PlaceholderBody` replaced with `ReviewSurface` + `SummaryBand` (verdict line, equal-weighting family chips, attempt label, warnings `role="status"` with jump-to-question) + `QuestionNavigator` (`role="tablist"`, inline SVG status icons, `aria-current`, arrow-key nav, rail/strip layouts) + `QuestionPanelShell` (format/difficulty/status tags, honest skeleton `aria-busy` and pending states, failed empty state). CSS added for summary/navigator/panel; variant B desktop keeps a 280px vertical rail (summary + panel in the right pane), mobile both variants get a sticky horizontal strip. `PrototypeSwitcher` now ignores arrow keys when focus is inside `[role="tablist"]` (otherwise the global A/B switcher flapped variants while the navigator used arrows). `pnpm typecheck` + `pnpm lint` green; redaction grep negative; `pnpm --filter app build` green; route absent from dist JS. Playwright matrix (5 states × 2 variants × 375/1280): no horizontal overflow, correct verdicts per state, tab counts correct (failed = 0), `aria-current` tracks click/arrow, warning jump activates the linked question, rail/strip/sticky CSS verified, zero console errors. Screenshots captured to `/tmp` (evidence/ formal screenshots are Phase 4.3). Next: Phase 3 (family feedback treatments + citations + honest states).
- **2026-09-01** Phase 3 implemented + verified (PLAN.md 3.1–3.5). Context: a prior session had already written the Phase 3 code on disk without checkpointing (scratchpad stopped pre-implementation); this session discovered that state, then verified the phase end to end rather than re-implementing. Static gates: `pnpm typecheck` green, `pnpm lint` green, `pnpm --filter app build` green, route string absent from dist JS, redaction grep (3.5) negative. Live pass with the managed runtime (rule 10: `./full-app start full`, vite + `/health` verified before browser work; rule 15 `playwright-cli --browser=chromium`): ready state — objective options with the learner's pick marked + per-skill rows, written rubric (met tags + criterion scores + LLM explanation), coding compile-failed row with stderr excerpt + 3 fail test rows, citations expand to chunk+quote; partial — "2 of 3 graded · score so far 0.50", `citation_missing` warning `role="status"`, jump link activates the in-flight question (fixture `q-part-3` is `processing`, so the honest `aria-busy` skeleton renders — PLAN's "pending" wording maps to the fixture's processing state, recorded in state.md pitfalls); processing — "0 of 3 graded · scores pending" + skeletons; failed — "Grading blocked · no questions scored" + `safety_block` + "No questions to review" (whole-assessment Retry is Phase 4 scope per PLAN 4.2); retried — "Attempt 2 of 2", "3 of 3 questions correct · score 1.00", execution-pass envelope (compile not_run, runtime succeeded · 18 ms, all test rows pass). Responsive matrix measured via `getComputedStyle`/`getBoundingClientRect`: 5 states × 2 variants × {375×812, 1280×800} → horizontal overflow 0 everywhere; 1280/B rail = 280px column; 375 both variants sticky horizontal strip (`position: sticky`, row direction), 375/B rail element flows as a full-width row (single column below the 1024px breakpoint). Console: 0 errors, 0 warnings across the whole walk. Runtime stopped after verification. Next: Phase 4 (retry interactions, evidence screenshots, HITL grill, recommendation record, #34 resolution).- **2026-09-03** Phase 4 implemented + verified (PLAN.md 4.1–4.3; records checkpoint by the same session). Retry model (D-05/D-07): per-question Retry appends an `isQueued` `MockAttempt` to `question.attempts`; whole-assessment Retry appends a `processing` `MockAssessmentAttempt` and flips every question to `processing` with a queued attempt — all inside `ReviewSurface` local state keyed off the `seedAssessment` prop, so a state switch or refresh resets to seed (nothing persists). New UI: `AttemptCard`/`AttemptHistoryBlock` (history oldest→latest, latest attempt expanded, prior attempts collapsed-but-expandable, queued attempt renders an `aria-busy` skeleton; D-07 state copy "New attempt queued — previous attempt kept (Attempt #N · score X)"), `RetryQuestionButton` (btn-secondary + hint "Queues a fresh attempt — this one stays."), `AttemptTimeline` (collapsed `<details>` "Previous attempt timeline — N attempts · last score X" in the summary band), summary "Retry assessment" button, failed-state empty card keeps its Retry button and gains a "Attempt #N — started … · grading in flight" timeline line. `QuestionReviewCard` now takes an explicit `attempt` (one-card-per-attempt shape for #40). Static gates: `pnpm typecheck` green (needed a `useEffect` import restore + unused-type cleanup), `pnpm lint` green, `pnpm --filter app build` green, redaction grep (3.5) negative, route string absent from dist JS. Live pass (rule 10 `./full-app restart full`, vite + `/health` 200 verified; rule 15 `playwright-cli --browser=chromium`): ready/A desktop — Retry question → "New attempt queued — previous attempt kept (Attempt #1 · score 1.00)" with Attempt #1 preserved and Attempt #2 queued skeleton; ready/A — Retry assessment → "Attempt 2 of 2 · started …" header, collapsed "Previous attempt timeline — 1 attempt · last score 0.67", verdict "0 of 3 graded · scores pending", all navigator tabs `processing`, per-question queued attempts; ready/B desktop 1280 — rail layout with the same retry + timeline behavior, `Retry question` correctly absent while every question is `processing` (honest state; locator match confirmed empty); failed — Retry assessment → empty state + "Attempt #1 — started … · grading in flight" + `safety_block` retained; retried seed — two `AttemptCard` toggles ("Attempt #1", "Attempt #2") + "Attempt 2 of 2 · started 01:30 PM"; mobile 375×812 — sticky strip, horizontal overflow 0. Console: 0 errors, 0 warnings across the walk (only the React DevTools info line). Evidence: 7 screenshots in `plan/evidence/` — `variant-a-desktop.png`, `variant-a-mobile.png`, `retry-assessment-timeline-desktop.png`, `retry-question-history-mobile.png` (full-page), `variant-b-desktop.png`, `variant-b-mobile.png`, `variant-b-retry-timeline-desktop.png` — PNG header dimensions verified (1280×800 / 375×812). Runtime stopped after verification. Next: HITL grill A vs B → `research/decisions-34.md` recommendation → #34 resolution comment + close → map #4 append.
- **2026-09-03** HITL grill + resolution. The human drove the prototype live at both viewports (runtime restarted on request, left running after) and chose **Variant B — persistent split-pane**. Recommendation recorded in `research/decisions-34.md` ("Recommendation for #40": chosen variant, rationale, six interaction notes carried into #40, copy corrections = none, evidence pointers). Resolution comment posted on #34 ([comment 5520168706](https://github.com/rings0fsaturn/study-planner/issues/issuecomment-5520168706)) and **#34 closed as completed** (verified `CLOSED` via `gh issue view`). Map #4 updated in the same pass: "Assessment review and feedback UX (prototype)" decision line appended to Decisions-so-far (split-pane + locked interaction vocabulary + #40 unblocked), and the #20 UI/UX-cluster checkbox annotated "prototype resolved via #34 (2026-09-03); implementation #40" (the #20 issue itself was found already closed, so it received a reference comment instead). Final verification acceptance met: both retry flows work at both widths, attempt history preserved visibly, screenshots exist, decision recorded, #34 closed with the map pointer appended.
