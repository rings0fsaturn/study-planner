# Verification – issue-41-written-assessment-rubric-grading (#41)

_Ticket: #41 · Parent spec #32 · Plan: plan/PLAN.md · Updated: 2026-09-10_

## AC tick-down (all unchecked until proven)

- [x] AC1 — Written generation uses typed visible payloads and server-only rubric content. — 2026-09-11: `WRITTEN_SCHEMA`/`written_schema(context_ids)` carries the visible payload plus the rubric/reference block that only ever reaches `questions.answer_block`; the live graded page rendered the criterion table with no authored rubric text, no reference answer and no `maxPoints` (redaction grep clean on every scenario).
- [x] AC2 — Written grading returns normalized score, rubric feedback, explanation, citations, warnings, per-skill observations. — 2026-09-11 live: a real `llm_rubric` grade landed for each scenario, the review showed the weighted per-criterion breakdown with published shares, the explanation narrative, the learner's own answer, `Skills observed`, and the composed read path carried citations (`CitationList`) and warnings (`SummaryBand`).
- [x] AC3 — Partial, timeout, provider, retry, no-leakage behavior is tested. — 2026-09-11: P3 unit matrices (weighted partial credit, inclusive τ=0.6, retryable-vs-fail-closed, malformed redelivery) plus P5 live additions — the 146.6 s provider-error path was observed live and the spec now waits 300 s per authored attempt, the citation-gate drop path is exercised by the retry loop, retry-into-a-fresh-observation is verified in the browser, and the redaction grep runs on all four scenarios.
- [x] AC4 — Results render through the shared review surface. — 2026-09-11 live at 1280×800 and 375×812: the #40 review surface rendered the written treatment unchanged (criterion table, answer echo, history, no horizontal overflow at 375 px).

## Phase gates

- [x] P1 contract: `uv run pytest services/intelligence/contracts/phase2/tests -q` green — 20 passed 2026-09-10 (RED first: 6 failed / 14 passed before the amendment).
- [x] P2 generation: service pytest green (written schema/validation/accept/repair-drop, recipe gate, `_question` subtype); migration 027 applies clean on dev Supabase. — 2026-09-10: 471 passed / 7 pre-existing failures (test_retrieval_probe 2 + test_v1_integration goldens 5, re-confirmed alone); contracts 20 passed; ruff clean on changed files; 027 + 028 pushed to dev Supabase (dry-run first) and a `BEGIN … ROLLBACK` probe proved the written row round-trips with `subtype` + hidden rubric, the assessment flips `ready`, an objective row carrying a subtype is rejected, and `answer_block` stays out of the `authenticated` column grant. 028 repaired the uuid cast 027 dropped from 024 (see plan Notes (b)).
- [x] P3 grading: `uv run pytest services/intelligence/tests -q` green (rubric math, threshold edges, timeout/provider/partial, redaction). — 2026-09-10: focused set (rubric grader + grading worker + grading grader + worker main + assessments API) 76 passed; contracts 20 passed; full service suite 501 passed / 7 pre-existing failures (same `test_retrieval_probe` 2 + `test_v1_integration` 5 set as P2); `ruff check` clean on every changed file. Composed-grade assertions cover weighted partial credit (0.75 / 0.7 / 0.6 / 0.5 / 0.19), inclusive τ=0.6, weight renormalization, and the retryable/non-retryable/redelivery matrices. Live dry run (one real provider call, 565 tokens, 10.9 s): `RUBRIC_GRADING_SCHEMA` accepted under strict mode, partial answer composed to 0.70, grade contained no `referenceAnswer` / `rubricVersion` / `maxPoints`.
- [x] P4 client: `pnpm --filter app test` green; typecheck + lint clean; redaction grep clean (rubric/reference keys + #40 list). — 2026-09-10: focused set 64/64 (attemptFlow 12, AttemptTaker 7, AssessmentConfig 7, AssessmentDetail 13, ReviewSurface 13, reviewModel 12); full app suite 757 passed / 2 failed, the only 2 failures being the known WSL TZ pair in `src/dev/seedTestData.test.ts` (re-confirmed green 2/2 under `--pool=forks`); `tsc --noEmit` and `eslint src/assessments src/pages/assessments` both clean. Redaction held in the rendered DOM in four places (written review card, wired detail page, attempt history, config/taking surface) with the #40 key list extended by `referenceAnswer|rubricVersion|maxPoints`; the `answer` union carries only `{ text }` and the grade carries only `rubricBreakdown`. **Corrected 2026-09-11 by P5**: the retry path did re-request `objective` on a failed generation (plan Notes (a)) — the P4 eval did not cover the failed-generation retry, only the happy-path family rendering.
- [x] P5 live: `e2e/assessment-written-41-live.spec.ts` green `--workers=1` (short_answer + long_form, real LLM grades, desktop + mobile, retry, restore); evidence in `plan/evidence/`. — 2026-09-11: **4 passed in 4.3 min** with `--project=app --workers=1` (no `--` separator). Scenarios: graded written review at desktop + mobile with the criterion table and zero overflow; reload restores the echoed answer and the stored grade; the skill-tag steer authors `long_form` with the 8-row textarea and grades through the same rubric arm; retry mints a second graded attempt with both rows in history. Redaction grep clean on all four. Two product defects were found and fixed en route (plan Notes (a) recipe-losing retry, plan Notes (b) unbound `chunkId` enum), both RED-first and re-gated: service **503 passed / 7 pre-existing**, contracts **20 passed**, app **758 passed / 2 failed** (the WSL TZ pair, 2/2 under `--pool=forks`), `pnpm typecheck` + `pnpm lint` clean, `ruff` at baseline. Evidence: `plan/evidence/written-41-{graded-desktop,graded-mobile,restored-desktop,longform-desktop,retry-desktop}.png`.

## Live evidence

- `plan/evidence/written-41-graded-desktop.png` (2026-09-11) — graded written review, criterion table + learner answer echo.
- `plan/evidence/written-41-graded-mobile.png` (2026-09-11) — same review at 375×812, no horizontal overflow.
- `plan/evidence/written-41-restored-desktop.png` (2026-09-11) — reload restores the echoed written answer and its rubric breakdown.
- `plan/evidence/written-41-longform-desktop.png` (2026-09-11) — skill-tag-steered `long_form` question graded through the rubric arm.
- `plan/evidence/written-41-retry-desktop.png` (2026-09-11) — retry: fresh attempt appended, both graded rows preserved.

## Wayfinder resolution (exit)

- [ ] Resolution comment posted on #41; issue closed; Decisions-so-far line appended on map #4; STATUS row flipped to Done; folder archived.
