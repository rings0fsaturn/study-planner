# State – scoped-question-generation
_Spec: specs/phase2-tickets/18-scoped-question-generation.md · GitHub #62 · Plan: active/scoped-question-generation/plan/ · STATUS row: scoped-question-generation · Status: done (closed + archived) · Updated: 2026-09-20_

## Current state & next
- **P7 done 2026-09-20 (R4 closed, with the mechanism corrected by measurement).** The same-origin range route was built, served 206s, and then **reverted**: it did not move first ink on the non-linearised 572-page corpus (6,789 ms vs 6,772 ms), range-only mode was worse (134 requests / 15.8 s), and the production deploy is Vercel static with no nginx, so the route would have broken the deployed viewer. Shipped instead: `materials/documentCache.ts` (a 2-entry LRU of pdf.js loading tasks) + prefetch-on-intent on `MaterialDetail`'s "Open in viewer". A repeat open drops from one 22.9 MB GET to **3,433 B and 897 ms** (from 6.8 s); prefetch moves the record fetch from 1,888 ms to 897 ms. A viewer fit defect found on the way was fixed: the whole-page fit now uses the document's largest page box, so a mixed-size corpus no longer overflows the frame (and `pageAtTop`'s jump tolerance is 2px). Transcript: `research/2026-09-20-p7-live-verification.md`.
- **Next: the wayfinder exit** — resolution comment on #62, close it, map #4 line, STATUS flip to Done, archive this folder.
- **R3's phone pass is still the one item that needs the user** (Fit readability on their own device). It does not block the exit; it stays recorded.
- **P8 done 2026-09-12** — the extracted-content preview is gone (`7bd2f6d`) and the viewer is a reader: it opens on a complete A4 page, `Page`/`Width` modes with a zoom stepper and ctrl+wheel, plain drag pans, Shift+drag selects, and a pdf.js text layer makes the text selectable. Live spec **2/2** with `--workers=1` plus the user's own hands-on pass; four defects found and fixed on the way. Transcript: `research/2026-09-12-p8-live-verification.md`; R6-R8 ticked.
- **P6 done 2026-09-12, plus one defect it could not see** — the viewer revision (vertical scroll, single width source, Fit + zoom) verified live 2/2, and the hands-on pass's image report fixed: the user's own material carries 587 JBIG2 images that pdf.js was silently dropping because `wasmUrl` was never set. R1/R2/R5 closed. Evidence: `research/2026-09-12-p6-live-verification.md`.
- **P5 done 2026-09-11** — the pdf.js viewer at `/materials/:materialId/view`; closes **AC4**. Evidence: `research/2026-09-11-p5-live-verification.md`.
- **P4 done 2026-09-11** — the scope contract, range validation, scoped + spread retrieval, D-06 widen-and-warn and the chapter picker. Evidence: `research/2026-09-11-p4-live-verification.md`.
- **P2 done 2026-09-11** — page provenance rides ingestion end to end (migration 029). Evidence: `research/2026-09-11-p2-live-verification.md`.
- P1 done 2026-09-11 (prompt meta blocklist + steer fix); P3 done 2026-09-11 (outline at ingestion, migration 030).
- **Drift (resolved): the P6/P7 commits `ff3ab07`, `a9ec0d7`, `7bd2f6d` are on `project/phase-2` and `origin/project/phase-2`.** The earlier "HEAD is `ff3ab07` unpushed" note is stale.

## Done so far
- 2026-09-20 (P7): **the viewer stops re-downloading the whole book, and a mixed-page-size fit defect is fixed.** `materials/documentCache.ts` (new) holds a module-level 2-entry LRU of pdf.js loading tasks keyed by material id, evicting a failed load; `PdfViewer` adopts the cached task and no longer destroys it on unmount; `MaterialDetail`'s "Open in viewer" warms it on `pointerenter`/`pointerdown` through a dynamic import (pdf.js stays out of the main bundle). Measured with the throwaway CDP probe: repeat open 22.9 MB/6.8 s -> **3,433 B/897 ms**; record fetch 1,888 ms -> 897 ms via prefetch. The planned same-origin range route was built and served 206s but reverted (no first-open win; Vercel has no nginx); range-only mode was worse (134 requests / 15.8 s). Along the way: the whole-page fit now uses `largestPageBox(doc)` (page 1's CropBox 583x835 vs pages 2+ 595x842 had let A4 pages overflow by 4px), and `pageAtTop`'s jump tolerance is 2px. `e2e/material-viewer-live.spec.ts` 2/2; `pdfView.test.ts` 24 passed; throwaway probe deleted. Transcript: `research/2026-09-20-p7-live-verification.md`.
- 2026-09-12 (P8): **the viewer became a reader, and the extracted-content card is gone.** (1) `MaterialDetail` no longer fetches or renders extracted content — effect, card, failure banner, `previewClient.ts` + its test, `MaterialContentPreview` and `.material-preview-text` all deleted (`7bd2f6d`, −328 lines); the service route survives (R-D-10). (2) The default zoom is a **mode**: `Page` puts a whole A4 inside the frame (measured live 335x479 px in a 1080x480 frame at 1280x720, 343x492 in a 351x492 frame at 375x812) and `Width` is the old fit-to-width (1080x1545); a `-`/`+` stepper over `1/1.25/1.5/2/3` with an `× N` readout and ctrl/cmd+wheel re-rasterises the bitmap (375 arm: 1.5× → 515x738, body line 8 → 11 px). (3) A pdf.js `TextLayer` per rendered slot makes the text selectable, with the drag contract the user's own passes settled: **plain drag pans** (measured 220 px from the page margin and 220 px from a drag starting on a glyph, `window.getSelection()` empty) and **Shift+drag selects** the run under the pointer (cursor becomes a text caret while Shift is held). Verified: `e2e/material-viewer-live.spec.ts` **2/2** with `--workers=1`, the user's hands-on pass, `pdfView.test.ts` extended, and the gates recorded in `plan/VERIFICATION.md`. Transcript: `research/2026-09-12-p8-live-verification.md`.
- 2026-09-12 (P8): **four defects found while making the viewer's assertions real.** (1) A later page's text layer was sized from page 1's box while the corpus mixes page widths (page 1 is 583.255 pt, page 2+ are A4 595.22), so the library's layer overflowed its 1080 px slot by 22 px at Fit — the layer now shares the canvas's `.pdf-fit` cap. (2) A slot that left the rendered window and came back stayed blank, because "already drawn"/"has a layer" were keyed by page number while the elements are unmounted: both are keyed by element now. (3) The pan lost the pointer stream to the browser's selection gesture and to pointer capture (18 px of 220): the drag is tracked on `window` now. (4) Shift+drag selected from the top of the document — Shift+drag is the browser's *extend* gesture with the document start as anchor when nothing is selected; the viewer anchors the selection under the pointer first.
- 2026-09-12 (P6 fix): **every image was silently skipped, and the corpus could not see it.** The user's hands-on pass on their own material reported no images at all; the console showed `Unable to decode image "img_p104_1": "Jbig2Error: JBig2 failed to initialize"` per image. That material (`grokking-algorithms-2nd-edition-2nd_compress`, 315 pages) carries **587 JBIG2 images across 260 pages**; pdf.js decodes JBIG2 with a wasm module fetched at runtime from `wasmUrl`, nothing set `wasmUrl`, so every one of them was dropped with a console warning while the text and the other image types still painted. Fixed by `apps/app/scripts/sync-pdfjs-wasm.mjs` (copies `pdfjs-dist/wasm/` into the gitignored `apps/app/public/pdfjs-wasm/`; the app's `dev`/`build` run it) plus `wasmUrl` in `PdfViewer`. Verified on the user's material: `/study/pdfjs-wasm/jbig2.wasm` and `openjpeg.wasm` 200, decode warnings on pages 104/96/35/26 = 0, diagrams visible (screenshot-checked). The viewer spec now fails on any `Unable to decode image` warning, because ink sampling cannot see a page that lost only its figures. Transcript: `research/2026-09-12-p6-live-verification.md` §7.
- 2026-09-12 (P6): **viewer revision — fit, vertical scroll, mobile zoom.** `apps/app/src/materials/pdfView.ts` (new) holds the geometry as pure functions (`fitScale`, `displayScale`, `rasterScale`, `clampPage`, `pagesInWindow`, `pageAtTop`) and `pdfView.test.ts` pins it with 18 cases written before the module existed. `PdfViewer.tsx` is now one vertical scroll of 572 `.pdf-page` slots, each sized from page 1's viewport with `aspect-ratio` so the scroll height is stable before ink; a `ResizeObserver` on `.pdf-frame` is the single width source; the rendered set is the slots on screen plus one frame-height of margin, computed from slot rects on a rAF-throttled scroll handler (`rendered 1` of 572 at load); the PAGE input and CHAPTER select jump the scroll (`frame.scrollTop += slot.top - frame.top`) and the readout is the page at the frame's top edge; Prev/Next are deleted; zoom is `Fit, 1.25x, 1.5x, 2x, 3x`, re-rasterising the bitmap with a `MAX_RASTER_SCALE = 2.5` ceiling; the `max-width: 100%` guard is scoped to `.pdf-fit` so a zoomed page may overflow sideways. Live: `e2e/material-viewer-live.spec.ts` 2/2 with `--workers=1` (1280x720 27.1 s, 375x812 11.4 s), `pdfView.test.ts` 18 passed, app suite 787 passed / 2 failed (the documented WSL TZ pair), typecheck + lint clean, `pnpm build` green, neighbouring live specs 5/5. Transcript: `research/2026-09-12-p6-live-verification.md`.
- 2026-09-12 (P6): **two viewer defects fixed and one measurement defect found.** (1) Cancelling an in-flight `RenderTask` after its canvas width had been set left that page blank forever; renders now go through one serial chain and are never cancelled. (2) The scroll assertion first sampled the revealed page's top band, and page 2 of the corpus is a near-empty divider (33,886 dark pixels in the bitmap, ~76 in the band); the spec now samples the visible band for "ink in front of me" and the whole bitmap for "was rasterised". (3) The P5 spec's file-level `test.use({viewport: 375})` meant its "desktop" scenario ran at 375 — the P5 claim "2/2 at 1280 and 375" was 375 twice; the viewport is now scoped to the mobile `describe` and the desktop arm is real.
- 2026-09-11 (P5): **pdf.js viewer.** `pdfjs-dist@6.3.289` with the worker wired through `pdfjs-dist/build/pdf.worker.min.mjs?url`; `MaterialStorageLike.createSignedUrl` + `MaterialClient.getMaterialFileUrl(material)` signing `<ownerId>/<materialId>/<source>` for 600 s; `PdfViewer.tsx` (canvas render fit-to-width, Previous/Next + page input + chapter `<select>` from `materials.outline`, `Set first page`/`Set last page` → `Assess these pages`) on `/materials/:materialId/view`, lazily imported so pdf.js stays out of the main bundle; entry points on the material detail page and in the config's scope field-group; the config seeds its range from `?from=&to=` and drops it for a material with no `page_count`. Live 2/2, both at 375x812 — P6 found that the file-level `test.use({viewport: 375})` ran the "desktop" scenario at 375 too, so P5's 1280 arm was never real; corrected in P6. Toolbar at 2.58 s, first paint at 6.32 s, canvas 1080×1545 CSS px, no console errors. Transcript: `research/2026-09-11-p5-live-verification.md`.
- 2026-09-11 (P5): **found and rejected the naive range fetch.** With the signed URL, pdf.js sends one plain GET and downloads all 22,919,258 bytes on every open (4.6-7.5 s, no `Cache-Control`) because Supabase Storage does not expose `Accept-Ranges` through CORS, so its capability check never enables ranges — even though a browser `Range` request against the same URL returns 206/64 KiB. An explicit `PDFDataRangeTransport` was implemented and measured **worse** (123 requests, 30,807,072 bytes, 12.9 s, continued fetching while idle: pdf.js walks the file backwards from the trailer and v6 never passes `disableAutoFetch` into the transport stream). Reverted and deleted; the upgrade paths are recorded in §5 of the P5 transcript.
- 2026-09-11 (P4): **scope contract, picker and scoped retrieval.** `AssessmentRecipe.scope`/`AssessmentScope` in the contract (+ fixture, + 2 tests, 22 passed) with `additionalProperties: false` so an unknown key is rejected while the old tag-era request stays valid; `_validate_scope`/`_scope_range_failure` in the router (400 on shape, 409 on range against `materials.page_count`); the scope reaches the RPC's page bounds and the steer is `sectionLabel`; the no-steer case now **spreads** (`_spread_context`) instead of refusing, which is the D-08 follow-up; `_widen_thin_context` implements D-06; `ContextBuilder` is a required 3-arg callable with no default; the chapter picker replaced the free-text tag box (closing P1's `['core']` wart). Live: a Chapter 5 scope grounds every citation inside pdf 156..213, the route matrix (thin range widens and warns, over-572/pageless 409, inverted 400, page-only scope spreads) behaves as specified, and the click-through passes 3/3 at 1280 and 375. 762/764 app tests green (2 = the documented WSL TZ pair, green under `--pool=forks`), typecheck + lint clean. Transcript: `research/2026-09-11-p4-live-verification.md`.
- 2026-09-11 (P4): **found the e2e spec was stale**, not P4-broken — `assessment-generation-live.spec.ts` waited for `Difficulty band` and read citations out of `.card-large`, neither of which the detail page has rendered since the #40/#41 rework. Rewritten against the real DOM (the `Citations` heading is the ready signal), with a new scoped scenario and a shared `waitForQuestion` helper replacing the inline retry loop.
- 2026-09-11 (P3): **outline at ingestion.** `app/ingestion/outline.py` (deterministic contents-page parse + running-header offset derivation + bookmark rung + gated DeepSeek fallback), persisted as `materials.outline`/`page_count`/`page_offset` by migration **030**; the stored outline equals the offline parse of the same fixture entry for entry (16 chapters, offset -33, source `contents`). Transcript: `research/2026-09-11-p3-live-verification.md`.
- 2026-09-11 (P2): **page provenance through ingestion.** `PypdfTextReader.extract` returns per-page text; the `file` branch rebuilds `"\n\n".join(pages)` and cleans once (byte-identical `fulltext.txt`) while `_page_segments()` derives the page-tagged paragraph segments alongside it; `chunking.py` propagates `page_start`/`page_end` (including through the overlap tail and the oversized re-split); chunk rows persist the columns; `RetrievedChunk` carries them. Migration 029 adds the two columns and page bounds to `match_content_chunks`, replacing 016's 4-arg overload with a 6-arg one whose defaults keep every existing caller working.
- 2026-09-11 (P2): **verified, offline and live.** Real 572-page fixture: paragraph stream 1257 identical and all 754 chunk texts byte-identical to the pre-P2 path, 754/754 paged, 372 straddling. Live: 029 pushed, APM material re-ingested from scratch (`total=754 paged=754 min_p=1 max_p=572 straddling=372 out_of_range=0 embedded=754`); RPC unscoped chapter steer -> ordinals 203/271/204/269/202 (P1's number), scoped 124..170 -> only overlapping hits, single-page ranges keep straddling chunks, pageless material -> 0 hits. 126 focused tests pass; whole service 515 passed / 7 failed with the identical 7 failing on the untouched base. Transcript: `research/2026-09-11-p2-live-verification.md`.
- 2026-09-11 (P1): filed and claimed GitHub **#62** on the #33–#49 spine (`wayfinder:phase2` + `ready-for-agent`), cut branch `phase2/issue-62-scoped-question-generation` off `237faf8`.
- 2026-09-11 (P1): **steer fix** — `build_context` lost its `title` parameter entirely; the steer is the learner's tags (the section label rides there until P4 passes `scope.sectionLabel`). `prompts.py` gained a shared `META_BLOCKLIST` in both system templates, and both user templates carry `{title_line}` as document context separate from `{steer}`. `routers/assessments.py` dropped `DEFAULT_SKILL_TAGS`; `skillTags` stays optional per the contract. `worker_main.py`'s context builder follows the new signature.
- 2026-09-11 (P1): **D-08 discovered by measurement** — an empty steer embeds to a degenerate vector and returns fragments; `build_context` now refuses it (`validation_failed`, non-retryable) and the worker fails the assessment on a non-retryable context error instead of leaving it `generating`.
- 2026-09-11 (P1): live verification — probe pre-fix steer -> 5/5 front matter (753/0/67/49/24), chapter steer -> 5/5 Chapter 5 body (203/271/204/269/202); real DeepSeek A/B produced no exam question in either arm and a body-grounded question for the chapter steer. Focused suites 139 passed; service suite 509 passed / 7 failed with the identical 7 failing on the untouched base. Transcript: `research/2026-09-11-p1-live-verification.md`.
- 2026-09-11: diagnosed the reported defect end to end. 25 questions exist in the dev project and **25/25 ask about the examination or the document, 0 about the material's content**; 22 distinct skill-tag sets, so tags do not control the subject.
- 2026-09-11: proved the mechanism by replaying the exact failing steer through the real retrieval path — 5/5 retrieved chunks are front/back matter (cover, index, chapter-1 boundary, "The examination", "Study skills and revision guidance") while only 8.5% of the corpus is exam-meta. Removing the material title from the steer retrieves the technical chapters instead. Probe: `scripts/diagnose_generation_context.py`.
- 2026-09-11: measured page feasibility on the live corpus — 0 PDF bookmarks, the printed contents page is parseable (16 chapters + Index, pdf page 6), the printed→pdf offset is −33 (266/320 header-bearing pages agree), and the existing page→chunk mapping is recoverable 754/754. Probes: `scripts/probe_pdf_pages.py` (removed after use), `scripts/pdf_outline.py` (kept as the P3 seed).
- 2026-09-11: built and validated the outline parser on four real PDFs and four TOC layouts — APM 16 entries (exact match to the printed contents, 1.4 s), SICP 91, CSAPP 128, DDIA 188, deterministic; the DeepSeek fallback returned 16/16 correct entries in 16.2 s. `--self-check` pins the three layout rules.
- 2026-09-11: opened this task — `specs/phase2-tickets/18-scoped-question-generation.md`, `active/scoped-question-generation/{plan,research}/`, STATUS row under Active. No `state.md`/`SCRATCHPAD.md` existed before this session.

## Flow trace
1. The learner picks a material and asks for an assessment. `AssessmentConfig.tsx:124-157` builds the request with `recipe = {formats, questionCount: 1, difficulty, skillTags}` and posts it to `/v1/assessments/generate`.
2. `routers/assessments.py:37-53` validates the recipe (`questionCount` must be 1; formats must be `['objective']` or `['written']`), inserts the assessment row, and enqueues the generation job atomically.
3. The generation worker (`generation/worker.py:136-204`) reads the assessment, takes `difficulty` and `skillTags` from the stored recipe, and calls the context builder.
4. `generation/context.py:21-31` builds the steer as `" ".join((title, *skill_tags))` — **the material title is the query** — embeds it with the sidecar as a query, and calls `match_content_chunks` with `top_k = 5` and the same string as `query_text` for the hybrid BM25 half (migration 016's RRF function).
5. Because the title literally matches the cover, contents and index, those chunks win both halves of the hybrid retrieval; the worker passes them to the prompt as the only source.
6. `prompts.py:115-125` (objective) and `:172-188` (written) instruct the model to author one question grounded strictly in those chunks, with the same steer string repeated as "Learner need (topic steer)". Neither prompt forbids exam/syllabus/document questions.
7. The model therefore authors an exam-format question — the only genre the retrieved context supports. Validation passes (the chunks are real, the citations verify), so the question is accepted and stored; nothing downstream can detect that the question is about the document rather than its content.
8. Pages used to never enter this path: `extractors.py` joined all PDF pages into one string, `chunking.py` carried no page, `content_chunks` had no page column, and `match_content_chunks` could not filter by one. **P2 (2026-09-11) closed that:** the reader returns per-page text, the `file` branch tags the paragraph segments with their page, `chunk_segments` propagates the first and last part's page, chunk rows persist `page_start`/`page_end`, and the RPC takes `p_page_start`/`p_page_end` (overlap semantics). The steer, prompts and scope contract are still P4's.
9. The plan inserts a scope at three points: page provenance through extraction/chunking/storage (P2), an outline parsed at ingestion (P3), and `recipe.scope` carrying the learner's chapter or page range into the retrieval call (P4). The viewer (P5) exists so the learner can read the page numbers they are choosing.
10. Scope persistence is free: `assessments.recipe` is jsonb and the read route already echoes it (`routers/assessments.py:149-151`, the #41 P5 fix), so retry and restore round-trip the scope with no new plumbing.

## Files affected
- `apps/app/src/materials/documentCache.ts` (new, P7) – the 2-entry LRU of pdf.js loading tasks, with `workerSrc` and `wasmUrl` in one place.
- `apps/app/src/materials/PdfViewer.tsx` (P7) – loads through the cache, no destroy-on-unmount, `largestPageBox(doc)` for the fit, and the removal of the local `workerSrc`.
- `apps/app/src/pages/materials/MaterialDetail.tsx` (P7) – `prefetchViewer` on the "Open in viewer" link (dynamic import).
- `apps/app/src/materials/pdfView.ts` + `pdfView.test.ts` (P7) – `pageAtTop` tolerance 2px + a jump case (24 tests).
- `e2e/material-viewer-live.spec.ts` (P7) – the refit poll waits for the narrow frame and a height fit (it used to accept the desktop canvas).
- `.work/active/scoped-question-generation/research/2026-09-20-p7-live-verification.md` (new, P7) – the before/after table, the route's rejection, the fit defect.
- `.work/active/scoped-question-generation/research/2026-09-12-p8-exploration.md` (new, P8 planning) – the exploration behind Phase 8: the frame/page measurements, the two readings of "the height of an A4" (with the frame-hug feedback-loop trap), what each interaction costs read out of the installed `pdfjs-dist@6.3.289`, the exact preview consumer list, and the three open questions.
- `.work/active/scoped-question-generation/plan/PLAN.md` (P8) – Phase 8 (8.1-8.5), R-D-07..R-D-10, the sequencing line (P8 before P7), and R-D-01 corrected to the rAF scroll handler P6 actually shipped.
- `.work/active/scoped-question-generation/plan/VERIFICATION.md` (P8) – R6-R8 and the P8 gate, ticked with their measurements (2026-09-12).
- `.work/active/scoped-question-generation/research/2026-09-12-p8-live-verification.md` (new, P8) – the second revision round's transcript: the preview removal, the page/width measurements, the gesture contract and the four defects it exposed, the environment's port collision, and the deliberate simplifications.
- `apps/app/src/pages/materials/MaterialDetail.tsx` (P8, `7bd2f6d`) – the content-preview effect, the "Extracted content" card and the failure banner deleted; the rest of the page untouched.
- `apps/app/src/materials/previewClient.ts` + `previewClient.test.ts` (deleted, P8) – the client module and its tests, with the service route kept (R-D-10).
- `apps/app/src/materials/types.ts` (P8) – `MaterialContentPreview` removed.
- `apps/app/src/pages/materials/MaterialDetail.test.tsx` (P8) – the three preview cases collapsed into one absence assertion (13/13 green).
- `apps/app/src/materials/pdfView.ts` + `pdfView.test.ts` (P8) – the fit **mode** (`Page` | `Width`), `fitPageScale`, `stepZoom` and the `ZOOM_LEVELS` ladder, written tests-first (23 cases).
- `apps/app/src/materials/PdfViewer.tsx` (P8) – `Page`/`Width` buttons, the `-`/`+` stepper with the `× N` readout, ctrl/cmd+wheel, the window-tracked drag pan, the Shift-anchored selection with the caret cursor, and one `TextLayer` per rendered slot sized from the same viewport as the canvas.
- `apps/app/src/materials/materials.css` (P8) – `.pdf-zoom-value`, the pdf.js text-layer block (including the `--total-scale-factor` variable nothing in the library sets), the `.pdf-fit` cap the layer shares with the canvas, and the `grab`/`grabbing` vs text-caret cursors.
- `e2e/material-viewer-live.spec.ts` (P8) – `E2E_APP_URL` (two worktrees want 5173), the whole-page/width/zoom/selection/pan/refit assertions, the idle-canvas guard on the resize poll, and the scenario now ending at the range handoff (R-D-13).
- `e2e/material-ingestion-live.spec.ts` (P8) – asserts the `Extracted content` heading and `.material-preview-text` are absent.
- `apps/app/scripts/sync-pdfjs-wasm.mjs` (new, P6 fix) – copies `pdfjs-dist/wasm/` (jbig2, openjpeg, qcms + no-wasm fallbacks) into the gitignored `apps/app/public/pdfjs-wasm/`; run by the app's `dev` and `build` scripts so the decoders are served in dev and emitted into `dist/`.
- `apps/app/package.json` (P6 fix) – `dev` and `build` run the wasm sync first.
- `apps/app/src/materials/PdfViewer.tsx` (P6 fix) – `getDocument({ url, wasmUrl: ${import.meta.env.BASE_URL}pdfjs-wasm/ })`; without it every JBIG2 image is dropped with a console warning.
- `apps/app/src/materials/pdfView.ts` (new, P6) – the viewer's geometry as pure functions: `fitScale`/`displayScale`, `rasterScale` (`MAX_RASTER_SCALE = 2.5`), `clampPage`, `pagesInWindow`, `pageAtTop`, `ZOOM_LEVELS`.
- `apps/app/src/materials/pdfView.test.ts` (new, P6) – 18 unit cases for the above, written before the module (red then green).
- `apps/app/src/materials/PdfViewer.tsx` (rewritten, P6) – one vertical scroll of page slots, `ResizeObserver` width, windowed rendering through one serial chain, Fit + zoom, jump-only PAGE/CHAPTER controls; slot rects are the only geometry source.
- `apps/app/src/materials/materials.css` (P6) – `.pdf-frame` is the single scroll container (`calc(100dvh - 15rem)`, `overscroll-behavior: contain`), `.pdf-page` slots with `aspect-ratio`, `.pdf-zoom`, and the `.pdf-fit`-scoped `max-width: 100%` guard.
- `e2e/material-viewer-live.spec.ts` (P6) – the desktop viewport is real again (the file-level `test.use` was scoped to the mobile describe), plus visible-band ink sampling, whole-bitmap rasterisation sampling, the no-horizontal-overflow pair (`scrollWidth === clientWidth`, canvas ≤ frame), the scroll-reveals-next-slot step, the 1280 → 375 resize re-fit step and the 1.5x bitmap-growth assertion with a measured text-line-height log.
- `.work/active/scoped-question-generation/research/2026-09-12-p6-live-verification.md` (new, P6) – the P6 transcript: root cause, windowing, zoom measurements, the three defects, what was run, the deliberate simplifications.
- `apps/app/src/materials/PdfViewer.tsx` (new, P5) – the viewer: signed-URL load, canvas render fit-to-width (DPR-aware, capped at 2x), page navigation, chapter `<select>` from `materials.outline`, `Set first/last page` and the `Assess these pages` handoff; lazily imported by the router.
- `apps/app/src/App.tsx` (P5) – `/materials/:materialId/view` route inside the protected shell, `React.lazy` + `Suspense` so pdf.js stays out of the main bundle.
- `apps/app/src/materials/materialClient.ts` (P5) – `MaterialStorageLike.createSignedUrl`, `MaterialClientLike.getMaterialFileUrl(material)` (600 s TTL, `<ownerId>/<materialId>/<source>`).
- `apps/app/src/pages/materials/MaterialDetail.tsx` (P5) – `Open in viewer` action for `kind === 'file'` materials.
- `apps/app/src/pages/assessments/AssessmentConfig.tsx` (P5) – seeds the range from `?from=&to=`, clears it when the material has no `page_count`, and links to the viewer from the scope field-group.
- `apps/app/src/materials/materials.css` (P5) – `.pdf-toolbar`, `.pdf-scope-bar`, `.pdf-field`, `.pdf-frame`, `.pdf-canvas` (`clientWidth` of `.pdf-frame` is the fit-to-width measure, so it carries no padding or border).
- `apps/app/src/materials/testing/fakeMaterialClient.ts` + `materialClient.test.ts` + `AssessmentConfig.test.tsx` (P5) – the double's `getMaterialFileUrl`, 4 signed-URL cases, 2 handoff cases.
- `e2e/material-viewer-live.spec.ts` (new, P5) – 1280 and 375 scenarios asserting real canvas ink, the chapter jump, the range handoff and the resulting generation.
- `apps/app/package.json` + `pnpm-lock.yaml` (P5) – `pdfjs-dist@^6.3.289`.
- `services/intelligence/app/ingestion/models.py` (P2) – `TextSegment.page`, `ContentChunk.page_start/page_end` (NULL for pageless sources).
- `services/intelligence/app/ingestion/extractors.py` (P2) – `PdfTextReader.extract` returns per-page text; the `file` branch keeps `clean_text("\n\n".join(pages))` for `text` and adds `_page_segments()` for the tagged paragraph stream.
- `services/intelligence/app/ingestion/chunking.py` (P2) – `_page_bounds()`; `flush()` stamps the bounds, the overlap tail carries `page_end`, the oversized re-split carries `segment.page`.
- `services/intelligence/app/ingestion/repository.py` (P2) – chunk rows carry the two columns; `list_chunks` selects them.
- `services/intelligence/app/ingestion/worker.py` (P2) – the resume-on-retry reuse key is `(text, page_start, page_end)`.
- `services/intelligence/app/generation/models.py` + `context.py` (P2) – `RetrievedChunk.page_start/page_end`, mapped from the RPC rows for P4's citation-in-scope check.
- `apps/app/supabase/migrations/029_page_scoped_chunks.sql` (new, P2) – the two chunk columns; the 016 hybrid body verbatim plus page params (DEFAULT NULL) and the range predicate in both CTEs; the 4-arg overload dropped.
- `services/intelligence/tests/test_chunking.py`, `tests/test_extractors.py`, `tests/test_repository.py`, `tests/ingestion_doubles.py` (P2) – 6 new page tests plus the page columns in the doubles and the row-shape assertion.
- `services/intelligence/app/routers/assessments.py` (P4) – `_validate_scope` (shape: integers ≥ 1, start ≤ end, non-empty label, unknown keys) + `_scope_range_failure` (409 when `materials.page_count` is absent or `pageEnd` exceeds it).
- `services/intelligence/app/generation/models.py` (P4) – `AssessmentScope` (`from_recipe`/`widened`/`to_dict`) + `GenerationBlueprint.scope`.
- `services/intelligence/app/generation/context.py` (P4) – `build_context(..., scope, ...)`; steer = `sectionLabel` + tags; page bounds into `p_page_start`/`p_page_end`; `_spread_context` for the no-steer case.
- `services/intelligence/app/generation/worker.py` (P4) – scope off the recipe; empty context → non-retryable `validation_failed`; `_widen_thin_context` (D-06); `ContextBuilder` required 3-arg, default removed.
- `services/intelligence/app/worker_main.py` (P4) – the adapter passes the scope through.
- `services/intelligence/contracts/phase2/openapi.yaml` + `fixtures/assessment-recipe-scoped.json` (new) + `fixtures/README.md` + `tests/test_contracts.py` (P4) – the scope contract and its two tests.
- `apps/app/src/assessments/types.ts` + `src/materials/types.ts` + `src/materials/materialClient.ts` (P4) – `AssessmentScope` on the recipe; `OutlineEntry`/`MaterialOutline`/`outlineFromRow` and `outline`/`pageCount`/`pageOffset` mapped off the row.
- `apps/app/src/pages/assessments/AssessmentConfig.tsx` (P4) – chapter chips + two page inputs + "Whole material"; the tag box and the `['core']` default deleted.
- `e2e/assessment-generation-live.spec.ts` (P4) – ready-signal and citation assertions rewritten against the real DOM, a scoped chapter-chip scenario added, the retry loop extracted into `waitForQuestion`, the mobile block moved to 375 with chip fill/reset assertions.
- `services/intelligence/app/ingestion/outline.py` (new, P3) + `apps/app/supabase/migrations/030_material_outline.sql` (new, P3) – the deterministic outline and the three `materials` columns.
- `services/intelligence/app/generation/context.py` (P1) – `title` parameter removed; the steer is the tags; an empty steer is refused (D-08).
- `services/intelligence/app/generation/prompts.py` (P1) – shared `META_BLOCKLIST` in both system templates; `{title_line}` document context separate from `{steer}`; `_topic_steer`/`_title_line` helpers.
- `services/intelligence/app/generation/worker.py` (P1) – context builder called with `(material_id, skill_tags)`; `["core"]` default dropped; a non-retryable context error fails the assessment.
- `services/intelligence/app/worker_main.py` (P1) – context-builder adapter follows the new signature.
- `services/intelligence/app/routers/assessments.py` (P1) – `DEFAULT_SKILL_TAGS` removed; `skillTags` validated only when present.
- `services/intelligence/tests/test_generation_{context,prompts,worker}.py`, `tests/test_assessments_api.py` (P1) – steer/blocklist assertions, empty-steer and non-retryable-context tests, two new route cases (6 new tests).
- `.work/active/scoped-question-generation/research/2026-09-11-p1-live-verification.md` (new, P1) – probe + real-model A/B transcript.
- `.work/specs/phase2-tickets/18-scoped-question-generation.md` (new) – the ticket contract: AC1–AC6, the four locked scoping decisions, out-of-scope list.
- `.work/active/scoped-question-generation/plan/PLAN.md` (new) – P1–P5, decisions D-01–D-08, files-touched index, measured context.
- `.work/active/scoped-question-generation/plan/VERIFICATION.md` (new) – AC tick-down, per-phase gates, exit checklist; P1 gate ticked.
- `.work/active/scoped-question-generation/research/2026-09-11-evidence.md` (new) – the drift table, the retrieval replay table, page forensics, outline-parser results, reproduction commands.
- `.work/active/scoped-question-generation/state.md` + `SCRATCHPAD.md` (new) – this record.
- `.work/STATUS.md` – new Active row; `_Last reconciled_` bumped to 2026-09-11.
- Client material read needs no change: `materialClient` uses `select('*')` (`materialClient.ts:193,216`), so new `materials` columns (outline, page_count) arrive with the record `AssessmentConfig` already fetches.
- `services/intelligence/scripts/diagnose_generation_context.py` (new) – retrieval-drift probe; the regression tool for P1. Committed with P1.
- `services/intelligence/scripts/pdf_outline.py` (new) – outline parser prototype and P3 seed; `--self-check` becomes the unit-test seed. Committed with P1.

## Pitfalls & rules
- **A pdf.js text layer is sized by a CSS variable the library never sets.** `new TextLayer({ textContentSource, container, viewport })` creates spans positioned from `left/top` percentages but sized against `--total-scale-factor`; `pdf_viewer.css` references it and nothing in `pdfjs-dist@6.3.289` writes it, so the embedder must put `--total-scale-factor: <displayScale>` on the layer's style or the spans take no size. Our stylesheet is a ~25-line copy of the library's `.textLayer` block (importing `web/pdf_viewer.css` is 163,832 B and drags the annotation-editor theme in).
- **A drag on the page must arbitrate against the browser's own selection gesture explicitly.** With the text layer left selectable, a plain drag starts a *selection*: the browser takes the pointer stream over and cancels it (`pointercancel`), so a pan handler sees the first move and then nothing (measured 18 px of an intended 220). The shipped contract is `user-select: none` on the layer with `user-select: text` only while Shift is held, a text-caret cursor while Shift is held and `grab`/`grabbing` otherwise, and the reader told which gesture they are making.
- **Track a mouse drag on `window`, not with `setPointerCapture` on the scroll container.** A drag that starts on a glyph leaves the frame immediately (the frame is the page's box), and capture alone stopped delivering moves there — the same 18 px failure. `window`-level `pointermove`/`pointerup`/`pointercancel` listeners removed on release are what actually works; add them per drag, never as permanent handlers.
- **Shift+drag is the browser's *extend* gesture: with nothing selected its anchor is the document start**, which is why a Shift+drag in the viewer swept every line from the top of the document to the pointer. Placing a caret under the pointer on Shift+pointerdown first is what makes the selection start where the reader pressed.
- **"Already drawn" and "has a text layer" must be keyed by *element*, not by page number.** Slots are unmounted when they leave the rendered window, so a page number key is stale the moment a slot remounts: the page comes back blank (canvas) or unselectable. `WeakMap<HTMLCanvasElement, …>` is the fix; the live spec scrolls to page 20 and back to page 1 to pin it.
- **A corpus can mix page sizes, and slots are laid out from page 1's box.** The APM fixture's page 1 is 583.255 pt wide while page 2 onward are A4 (595.22 pt), so a text layer sized from page 2+ overflowed its slot by 2% (22 px at 1080) at Fit. Both the canvas and the layer carry the `.pdf-fit` `max-width: 100%` cap; a per-page slot width is the upgrade path.
- **Two worktrees of this repo want 5173 and 8000.** `../study-planner-web-issue-63` holds them, so `./full-app` will not start this checkout's stack and live specs silently hit the *other* checkout's app unless pointed elsewhere: use `E2E_APP_URL` (the viewer spec honours it) plus an ad-hoc `vite --port 5174`, and give the ad-hoc service `CORS_ORIGINS` including the new origin or every request fails its preflight with 400. An ad-hoc service instance also cannot serve `POST /v1/assessments/generate` — it answers `500 {"detail":"auth not configured"}` because the launcher injects that config — so a viewer spec must not depend on generation (R-D-13).
- **pdf.js needs its wasm decoders served, or JBIG2 images vanish silently.** `getDocument` must be given `wasmUrl` pointing at a directory (trailing slash required) that actually serves `jbig2.wasm`/`openjpeg.wasm`; the default is a relative `"wasm"` resolved against the page, which 404s in a bundler. The failure mode is a console warning per image (`Unable to decode image ... "JBig2Error: JBig2 failed to initialize"`) and a page that keeps its text and its other images, so pixel and ink assertions pass. Assets: `apps/app/scripts/sync-pdfjs-wasm.mjs` → `apps/app/public/pdfjs-wasm/` (gitignored, run by `dev`/`build`).
- **The frozen corpus is a poor fixture for image rendering: it has 0 JBIG2 images** (432 DCTDecode + 943 FlateDecode). The user's other material (Grokking Algorithms, 315 pages) has 587 JBIG2 images across 260 pages, which is where this class of defect shows up. A viewer change that touches image decoding must be checked on a JBIG2-bearing material, not only on the corpus.
- **The served viewer document may be the derived `view-<contentVersion>.pdf` copy, not the uploaded file** (`getMaterialFileUrl` prefers it). For Grokking that copy is 3,420,085 B against a 2,998,611 B upload; both carry the same JBIG2 images, so the repair is not the source of any decode behaviour.
- **A cancelled pdf.js render can leave its canvas blank for good.** pdf.js refuses a second `render()` on a canvas that is still drawing, so the tempting pattern (cancel in-flight `RenderTask`s in the effect cleanup, skip a page whose `canvas.width` already matches) permanently blanks any page whose render was cancelled after the width was set. Serialise renders through one promise chain, never cancel mid-flight, and mark a page "drawn" only when its render promise resolves.
- **A file-level `test.use({ viewport })` in a Playwright spec applies to every test in the file**, including tests declared before it. The P5 viewer spec therefore ran its "desktop" scenario at 375x812 for a whole phase. Scope the viewport inside the `describe` that needs it, and make the desktop scenario assert something only true at desktop width.
- **Page 2 of the APM corpus is a near-empty divider** (33,886 dark pixels in the bitmap, ~76 in the visible band at 1080 px wide). "The revealed page has ink" must not sample the top band of an arbitrary page; sample the exact visible band for a page known to carry text (1, 156) and the whole bitmap for "it was rasterised".
- **The pdf.js canvases are never unmounted once rendered** (R2 keeps a slot per page; only the bitmaps accumulate). At 1280 the rendered set grows with scrolling distance and the text selection/bitmaps stay resident; the upgrade path is releasing bitmaps for slots more than two viewports away.
- **Slot placeholders all use page 1's aspect ratio**, so a page whose own viewport differs may be a few pixels narrower than its slot (the corpus mixes 595 pt and 612 pt pages). Per-page aspect ratios are the upgrade path.
- **`window.innerWidth` is not the frame width.** At 1280 the materials page caps its content column at 1080 px, so `.pdf-frame.clientWidth` is 1080 — the fit/overflow assertions must compare against the frame, never the viewport.
- **Live specs can lose their first assertion to the event-store sync gate.** Right after sign-in the app renders "Still bringing things over" instead of the route, and with the shared dev account's history that routinely exceeds a default 5 s expect timeout, so `getByRole('heading', { name: 'Material library' })` fails with `element(s) not found` and the page snapshot shows the sync status only. Both failures of that shape in this session passed on re-run. Give the first post-sign-in assertion a 30 s window (as `e2e/material-viewer-live.spec.ts` does) rather than treating it as a product failure.
- **pdf.js will not range-fetch through Supabase Storage, and the reason is CORS exposure, not server support.** The signed URL answers a browser `Range: bytes=0-65535` with 206 and exactly 65,536 bytes, but the exposed header set is only `cache-control`, `content-length`, `content-type`, `expires`, `last-modified` — `Accept-Ranges` and `Content-Range` are not readable, so `validateRangeRequestCapabilities` never enables ranges and pdf.js silently streams the whole file (measured: one GET, 22,919,258 bytes, 4.6-7.5 s, no `Cache-Control`, repeated on every open). **Do not "fix" this with `PDFDataRangeTransport`:** it was implemented and measured *worse* (123 requests, 30,807,072 bytes, 12.9 s) because pdf.js walks the file backwards from the trailer and pdf.js v6 passes only `disableRange`/`disableStream` into `PDFDataTransportStream` — there is no `disableAutoFetch` off-switch on that path. The real levers are exposing `Accept-Ranges` on the storage CORS response or a same-origin range proxy.
- **pdf.js v6 render takes `{ canvas, viewport }`, not `canvasContext`.** `canvasContext` is deprecated-but-tolerated; the canvas must be `null` when the context form is used. And `pdfjs-dist` needs `GlobalWorkerOptions.workerSrc` (`pdfjs-dist/build/pdf.worker.min.mjs?url`) or the canvas renders blank with only a console warning — which is why `e2e/material-viewer-live.spec.ts` asserts canvas ink, not just a mounted element.
- **An idle canvas is 300×150, so `canvas.width > 0` is not a paint check.** Both the spec and any probe must sample pixels.
- **`materials.outline`, `page_count` and `page_offset` are written together** (`ingestion/worker.py:420-429`), so a material whose outline could not be derived has no `page_count` either: the config offers no page range at all, which is why a client-side `pdf.getOutline()` chapter list would be unreachable and was dropped from P5.
- **A viewer handoff (`?from=&to=`) must be dropped when the material has no `page_count`.** The range inputs only render when `page_count` exists, so a seeded range would otherwise reach `recipe.scope` unvalidated — `AssessmentConfig` clears it on load.
- **The viewer's range handoff is a typed range: numbers, no `sectionLabel`.** Only a chapter chip in the config carries a label; editing either page drops it (P4).
- **The RPC's page filter is an overlap, not a containment.** `page_end >= p_page_start AND page_start <= p_page_end` in both CTEs, because 372/754 chunks straddle a page boundary and containment would drop the chunk that carries the requested page's opening text. A verified live case: range 158..158 returns chunks (157,158) and (158,159).
- **`CREATE OR REPLACE` cannot change a function's parameters or return type.** Migration 029 `DROP`s the 016 4-arg overload first and creates the 6-arg one with both page bounds defaulted, so every existing 4-named-argument caller (context.py, routers/retrieval.py, both probes) resolves to it unambiguously — verified live by replaying P1's chapter steer for the same ordinals. Never add page params without re-checking that resolution, and never leave two near-identical hybrid overloads.
- **`content_chunks` returns 0 rows to a user token** (service-owned table, no client policy). Verify chunk state with SQL or the service role, not the browser's client.
- **`PypdfTextReader.extract` returns `list[str]`, one entry per page.** Any new PDF test double must return a list; `ExtractedContent.text` is still built by one `clean_text` over `"\n\n".join(pages)`, so never clean per page when building `text`.
- **An applied migration is never re-run.** 029 is live, so P3's `materials.outline/page_count/page_offset` columns must ship as migration **030**.
- **P2 attaches pages to the existing paragraph segments.** Page-sized segments would re-cut every chunk: chunking consumes segments (`document_segments`), and today's paragraph stream on the 572-page fixture reproduces the live 754 chunks exactly. Evidence: `research/2026-09-11-p2-precheck-page-identity.md`.
- **`ExtractedContent.text` stays built as one `clean_text` over the joined raw pages.** Cleaning per page and joining loses 735 chars of page-boundary blank lines (963,136 vs 962,401), and that text is the uploaded `fulltext.txt` (`worker.py:347-348`).
- **Pages must be propagated inside `chunk_segments`, including the overlap tail.** Only 3/754 chunks start at a paragraph boundary (`flush()` composes `carry + current`, `chunking.py:88`) and the tail is currently a bare `TextSegment(tail)` with no page (`chunking.py:127-130`); text-lookup page derivation does not work.
- **Never embed an empty steer.** `""` (or whitespace) embeds to a degenerate vector and `match_content_chunks` answers with fragments (measured ordinals 393 `'likes'.`, 259 `produced.`, 90 `GU.`, 532 `369`, 344 `necessary.`). `context.build_context` raises `validation_failed` (non-retryable) when there is nothing to steer with, and the worker fails the assessment on a non-retryable context error rather than leaving it `generating` (D-08). **Since P4 the no-steer case with a usable scope never reaches the embedder at all:** `_spread_context` lists the material's chunk ids/ordinals and picks `CONTEXT_TOP_K` evenly spaced ones inside the page bounds, then fetches their text.
- **The material title is front matter.** Any steer containing it retrieves the cover, contents page and index (5/5 measured); the title belongs in the prompt as document context only, never in the query.
- **A scope widens only below `CONTEXT_TOP_K` = 5, and 3 pages is already enough.** The page filter is an *overlap*, so a 3-page window collects the chunks straddling its edges: measured live, 156..158 returns exactly 5 chunks and does **not** widen, while 156..156 returns 4 and widens to 151..161 with `scope_widened`. Do not expect a widen from a multi-page range, and do not read its absence as a broken D-06.
- **The assessment detail page's ready signal is the review surface**, not the config page's copy: it renders `Difficulty 3` and a `Citations` heading, and the question is **not** inside `.card-large` any more. A live spec that waits for `Difficulty band` or scopes its citation lookup to `.card-large` fails forever — that is a stale spec, not a broken app (fixed in `e2e/assessment-generation-live.spec.ts` during P4).
- **The scope needs no column.** It lives in `assessments.recipe` (jsonb) and the read route echoes it, so it survives retry/restore untouched; adding a scope column or migration would be redundant plumbing for data that already round-trips.
- **`ContextBuilder` deliberately has no default.** It is a required 3-arg callable (`material_id`, `skill_tags`, `scope`), so any new construction site must pass a scope; a default would let the production adapter be dropped silently, which is exactly how the P1 defect hid.
- **The app suite's only failures are the documented WSL TZ pair** in `src/dev/seedTestData.test.ts` (an off-by-one date under the default threads pool); re-run that file with `--pool=forks` to confirm 2/2 before blaming a change.
- `.work/specs/test-login-cred.txt` **wraps each value in backticks** (`` `user@host` ``, `` `password` ``). A password grant that keeps them fails with `invalid_credentials` for a credential that is perfectly valid — strip backticks/quotes before use (the P1 run lost time to exactly this).
- A stale `.git/index.lock` (04:20, no git process alive) blocked git operations in this checkout; it was removed after confirming no git process was running. Check before deleting.
- The extracted text must stay **byte-identical** when pages are added: `ingestion/worker.py:391-397` reuses existing chunk rows when the re-extracted text matches, so any whitespace change silently invalidates the corpus's embeddings.
- Adding page columns requires **deleting the material's chunk rows** before re-ingest; a plain retry hits that byte-identical reuse and leaves the new columns NULL (D-07).
- Before `CREATE OR REPLACE`-ing `match_content_chunks`, copy the **most recent** body (migration 016) verbatim. Migration 027 already broke this repo by re-creating a function from a stale body and dropping a cast (42804 at live runtime, plpgsql bodies are only parsed at first execution).
- `AssessmentRecipe` is `additionalProperties: false` (`openapi.yaml:135`) — `scope` must be added to the contract or the request is rejected.
- `questionCount` is pinned to 1 in three places (`routers/assessments.py:44`, one message = one question in `generation/worker.py`, the single-row accept RPC in `migrations/028:44`) — do not attempt multi-question inside this ticket.
- A scope that returns fewer chunks than `CONTEXT_TOP_K` must widen and warn (D-06); a silently thin context is how the original defect hid.
- pdf.js needs `GlobalWorkerOptions.workerSrc` set (Vite: `pdfjs-dist/build/pdf.worker.min.mjs?url`) or the canvas renders blank with only a console warning.
- An `<iframe>` PDF viewer **cannot** report the page being displayed; that is why the viewer choice is pdfjs-dist and not the zero-dependency native option.
- Printed page ≠ PDF page (offset −33 on the APM corpus); the offset is per-document and must be derived from running headers, never hardcoded. Label the picker with the number the learner sees in the viewer.
- The printed→PDF offset has a ~2-page skew on chapter openers (the opener page carries no printed number); acceptable for scoping a chapter, not for exactness.
- The DeepSeek outline fallback returns titles without the `Chapter N` prefix, so parser-vs-model comparison must key on page numbers, never on title substrings.
- Per-prompt rule from #41 that still holds: prompts and response schemas are built per message; never reintroduce a module-level schema constant.
- Live runs need the managed runtime **plus** the detached ingestion worker (`bash scripts/run-detached-ingestion-worker.sh`; `full` does not run it) **plus** the GPU sidecar on :8200; the detached worker does not hot-reload, so restart it after any `services/intelligence/app/` change. Live mutation specs run `--workers=1` and without a `--` separator.
- `.work/` is tracked on purpose; never add it to `.gitignore` and never run `git clean -fdx` at the repo root.
- **A pdf.js text layer needs `--total-scale-factor` set by the embedder.** The installed build reads it (spans get `font-size: calc(var(--text-scale-factor) * var(--font-height))`, the layer box `round(down, var(--total-scale-factor) * <page px>, var(--scale-round-x))` — `pdf.mjs:1533/1534`, `setLayerDimensions` at `:1524`) and **never sets it**; pdf.js's own viewer does. Without it the spans exist but sit at the wrong size and the selection lands off the glyphs. The library stylesheet that positions them is `web/pdf_viewer.css` (163,832 B) and the layer is `position:absolute; inset:0`, so the slot needs `position: relative`.
- **Pan and text selection coexist by a pointer-events split, not a mode.** `.pdf-text-layer { pointer-events: none }` + `span { pointer-events: auto }`: a drag starting on a glyph selects, a drag starting on the page background pans. pdf.js's `TextLayer` defines no such rule itself, and without the split every drag over the page begins a selection and "drag the page" never fires.
- **A frame that hugs the page creates a measure loop.** `ResizeObserver` on `.pdf-frame` → `containerWidth` → scale → slot width → frame width → `containerWidth`. The frame must keep the column width as the fit input, or the fit reference must move to the frame's parent. (Found while sketching the "frame is A4-shaped" reading of the P8 report.)
- **The extracted-content preview is wired in six places, not one.** `MaterialDetail.tsx:140-161` (fetch effect) and `:349-356` (the card) and `:358-365` (the failure banner), `previewClient.ts` + its test, `MaterialContentPreview` (`types.ts:117`), `.material-preview-text` (`materials.css:654`), plus the assertions in `MaterialDetail.test.tsx` and `e2e/material-ingestion-live.spec.ts:128-130`. Deleting the card alone leaves a 4,000-character fetch on every open of the page.

## Decisions in force
- Scoping locked with the user 2026-09-11 (spec table): (1) viewer = `pdfjs-dist` custom canvas, not `react-pdf`, not an iframe; (2) index source = deterministic contents-page parse primary, LLM judge/fallback only; (3) chapter **and** page range in the same release; (4) re-ingest is free (local embeddings) so page columns land by forcing a re-chunk, no backfill script.
- **D-01:** the steer is the chosen section label (or empty), never the material title; the title stays as separate document context in the prompt (2026-09-11).
- **D-02:** both system prompts carry an explicit meta blocklist (no exam, syllabus, marks, duration, section structure, study guidance, chapter structure, or study-text questions) and require a concept-or-application question (2026-09-11).
- **D-03:** page provenance rides ingestion segment → chunk → column → RPC, with `page_start`/`page_end` (two columns, because chunks straddle boundaries) and nullable page bounds in `match_content_chunks` (2026-09-11). Implemented in P2: **overlap** matching, and 016's 4-arg overload replaced by a 6-arg one whose page params default to NULL.
- **D-04:** the outline is parsed deterministically at ingestion (`ingestion/outline.py`, stage 1, no new queue stage); bookmarks first, then the contents-page parse, then one DeepSeek call over front-page excerpts; persisted on `materials` (2026-09-11).
- **D-05:** one scope shape for both levels — `recipe.scope = {pageStart, pageEnd, sectionLabel?}`, validated against `materials.page_count`, stored in the existing recipe jsonb (2026-09-11).
- **D-06:** a thin range widens and warns; only a zero-chunk range is rejected (2026-09-11).
- **D-07:** existing materials re-ingest rather than backfill; the measured 754/754 page-recovery stays a verification probe, not a mechanism (2026-09-11).
- **D-08:** an empty steer is refused (`validation_failed`, non-retryable), never embedded; the worker fails the assessment on a non-retryable context error. The proper unscoped query ("no query -> an evenly spread sample inside the page range") needs the RPC's nullable page bounds and belongs to P2/P4 (2026-09-11, added during P1 after measuring the empty-steer fragments). **Delivered in P4:** `_spread_context` is that branch, so a page-only or label-less request samples evenly inside the scope instead of failing.
- **P4 decisions (2026-09-11):** (1) **scope pages are PDF page numbers everywhere** — recipe, chunk columns, viewer — and the printed→PDF conversion happens once at ingestion (P3), never again; (2) **no new migration** for the scope: it rides `assessments.recipe` (jsonb, echoed by the read route), which is D-05's bet paying off; (3) the **unscoped** thin case does not widen (there is nothing to widen into) — only a chosen range widens and warns; (4) `ContextBuilder` is a required 3-arg callable with **no default**, so the production builder cannot be silently dropped again; (5) `AssessmentConfig.tsx` no longer sends `skillTags` at all — the tag box is gone and the steer is `scope.sectionLabel`, so an unscoped browser request relies on the spread branch.
- **P5 decisions (2026-09-11):** (1) the viewer route is **lazily imported** so pdf.js (488 kB) and its worker (1.27 MB) stay out of the main bundle and out of the jsdom unit-test graph; (2) the range control is the learner's own page navigation — `Set first page` / `Set last page` capture the current page and the handoff is `?from=&to=`, which the config treats exactly like a typed range (numbers, no `sectionLabel`); (3) **no client-side `pdf.getOutline()`** — ingestion already owns the cascaded outline and a material without one has no `page_count`, so the client list would be unreachable; (4) the **whole-file download is accepted for now**, with the CORS root cause and the three upgrade paths recorded in `research/2026-09-11-p5-live-verification.md` §5 rather than shipped as a slower range transport.
- **P6 decisions (2026-09-12):** (1) the viewer's geometry lives in `pdfView.ts` as pure functions with unit tests, and the component keeps only the DOM wiring; (2) the rendered window is computed from slot rects on a rAF-throttled **scroll handler**, not an `IntersectionObserver` (same observable behaviour, testable in jsdom) — the one deviation from R-D-01; (3) rasterisation is serialised through **one promise chain and never cancelled**, because a cancelled render after `canvas.width` is set blanks that page permanently; (4) the `max-width: 100%` guard applies only in the fitted state (`.pdf-fit`), so zoom may overflow sideways and the frame scrolls horizontally; (5) zoom is `Fit, 1.25x, 1.5x, 2x, 3x` and re-rasterises the bitmap with a `MAX_RASTER_SCALE = 2.5` ceiling (a recorded `ponytail:` simplification); (6) canvases are never unmounted, only accumulated, with the bitmap-release upgrade path recorded in the code; (7) the pdf.js **wasm decoders are copied from the installed package into a gitignored `public/` folder by a script the app's `dev`/`build` run** — no binaries in git, no drift from the installed pdfjs-dist version — and `wasmUrl` is always passed so JBIG2/JPX images decode.
- **P8 decisions (2026-09-12, all in force):** (1) zoom is **mode + multiplier** (`Page` | `Width`, then `1.25/1.5/2/3`) because a 480 px frame at 1280x720 cannot show a whole page and readable text at once; (2) "highlight" is the **pdf.js text layer** (selectable/copyable/browser-painted) and persisted marks are their own ticket; (3) the drag contract is **plain drag pans, Shift+drag selects**, with the drag tracked on `window` — the pointer-events split originally planned (R-D-09) is superseded; (4) the preview is deleted client-side while the **service route stays**; (5) the viewer spec ends at the **range handoff** and no longer generates a question; (6) `E2E_APP_URL` lets a live spec run against another port when a sibling worktree owns 5173. Full reasoning: `plan/PLAN.md` R-D-07..R-D-13 and the two P8 transcripts.
- Inherited from map #4: the capstone principle (feature-rich, no MVP deferral) and #13's decision that similarity retrieval is for targeted "about topic X" slots — this ticket implements that targeted slot.

## Open
- **P8's three questions were answered 2026-09-12**: (1) highlight = a pdf.js text layer; (2) default zoom = `Page`; (3) the round rides #62. All three are implemented and verified.
- **Port collision with the sibling worktree.** `../study-planner-web-issue-63` holds 5173 (Vite) and 8000 (uvicorn), so `./full-app` refuses to start this checkout and `e2e/material-viewer-live.spec.ts` now prefers `E2E_APP_URL` (default unchanged). This session ran the app on **5174** and the intelligence service on **8001**, which needs `CORS_ORIGINS=http://localhost:5173,http://localhost:5174`; an ad-hoc service instance cannot serve `/v1/assessments/generate` (it answers `500 {"detail":"auth not configured"}` without the launcher's config), which is why the viewer spec no longer generates (R-D-13).
- P8 is complete; the surfaces it changed are listed under Files affected. Phase 7's surfaces are the viewer's document loader (`PdfViewer.tsx`, `materialClient.ts`), `apps/app/vite.config.ts`, `docker/nginx.conf` and the intelligence service's file route.
- GitHub #62 is **filed and assigned** (2026-09-11); the map #4 resolution line, the resolution comment and the close still come at the exit — now after P7.
- `services/intelligence/scripts/diagnose_generation_context.py` and `pdf_outline.py` are now **tracked** (committed with P1) instead of sitting untracked.
- Blocked-by sequence: issue-41's wayfinder exit (push `phase2/issue-40-41`, resolution comment, close #41, map line, STATUS flip, archive) is still open in its own task. This branch was cut before it, so `.work/` records diverge while the code does not; the ticket's code stands on `237faf8`.
- Not verified live (superseded 2026-09-11 by P4): the browser click-through was absent through P1 because that phase changed no UI. **P4 ran it:** 3/3 with `--workers=1` at 1280 and 375, including the scoped chapter-chip flow — `research/2026-09-11-p4-live-verification.md` §4.
- P4 to close a P1 wart: `AssessmentConfig.tsx:135` still sends `skillTags: ['core']` when the tag box is empty, so an untagged request still steers on a meaningless term. The tag box and that default are both P4's. — **Closed 2026-09-11 (P4):** the tag box and the default are gone; the steer is `scope.sectionLabel`, and an unscoped request takes the spread branch.
- Needed before a page-only scope ships (D-08 follow-up): the RPC must answer an unscoped, page-bounded request with a spread of chunks (no query) rather than a similarity query. **P2 landed the nullable bounds; the "no query" branch is still P4's.** — **Closed 2026-09-11 (P4):** `_spread_context` answers with `CONTEXT_TOP_K` evenly spaced chunks inside the bounds and never embeds an empty string (verified live with a label-less 156..213 scope).
- P4 to add `GenerationBlueprint.scope` — deferred from P2 deliberately (no consumer existed until the scope contract lands) and only the file index had placed it in P2. — **Closed 2026-09-11 (P4):** `GenerationBlueprint.scope` landed with its consumer (the worker's widen decision and telemetry).
- **Next: Phase 7 (streaming)**, then the wayfinder exit: push `phase2/issue-62-scoped-question-generation` (`ff3ab07` + P6 + P7), post the resolution comment, close #62, add the map #4 line, flip the STATUS row to Done, archive this task folder. P6 is complete; P7 is the last code phase.
- **The user's own phone pass is the one outstanding piece of R3** (2026-09-12): the viewer was revised for exactly the report that started P6 ("not readable in mobile screen format"), and the person who made it has to confirm the fix on their own device. The stack was left running for it (app 5173 restarted, intelligence 8000, detached ingestion worker, GPU sidecar).
- **Open finding, deliberately not fixed in P5; P7 is its carrier:** every viewer open downloads the whole 22.9 MB because Supabase Storage does not expose `Accept-Ranges` through CORS, so pdf.js never enables range requests; the explicit-transport alternative measured worse and was reverted. The fix is the same-origin range route (P7) — Storage CORS exposure is the alternative. Numbers and options: `research/2026-09-11-p5-live-verification.md` §5; the P7 baseline (first ink 6,761 ms, 22,922,615 B in one GET) is in `plan/VERIFICATION.md` under "Live evidence (revision)".
- The GPU sidecar is still running (P5 opened the viewer, which needs no embedding, but P4/P5 generations did). Stop `embedder-embedder-1` when no embedding work is pending — it burns about two cores.
- Out of scope, deliberately: `questionCount > 1` (needs an accept RPC taking a question array, a worker slot loop, telemetry and config changes — its own ticket); page scope for `url`/`manual` materials; YouTube timestamp scoping (cheap later: `start_seconds` exists and the IFrame API is already wired).
- Proved rather than assumed (was listed here as unverified by design): a chapter-scoped generation produces content questions — AC1's live gate passed 2026-09-11 (P4).
